# Todo list

> Reconciled against the Changelog (as of `27b91cf`, 2026-09-10): items listed in working documents but already landed no longer appear (e.g. json.Best ratio→closed by the opt parser, MT ratio retention→`a37ebaa`, streaming MT→`27b91cf`, table entry u32→`327bc99`). Falsified sub-directions are in [falsified directions](negative.md); do not resurrect them via this list.

## P0 · Structural

1. **Parallelize MT decode stage B** (reachback analysis + rep history prefix scan)
   - Leapfrog dimension: libzstd has no MT decode; done well this jumps from a 0.66-1.0× liability to exclusive leadership.
   - Status: mt2-16 has no scalability (serial stage B accounts for 45-80%; match copies account for 80% on skewed.zst9).
   - Approach: predict per-segment reachback depth to choose split points; replay rep prefixes ahead of time using stage A's sequence stream.
2. **MT decode path checksum verification**: currently explicitly unverified; the decoder already auto-verifies on the ST path (`81d2119`); complete the MT path and re-check mismatch error behavior.

## P1 · Encoding speed

4. **json.Fastest ~1.57×** (largest single-item speed gap; 1.78× → 1.59× via the two-phase scan loop + `inline(always)` chain helpers, then distance-resolve/idx-space rep probes on 2026-09-11 cut another −5.9%/−5.7% Ir json/text at flat wall, and the OR-folded pair rep prefilter (same day) moved wall for the first time: +2.7% (530→545 MiB/s, byte-identical) at +1.5% Ir — the expected trade in this regime — the steady loop is branch/latency-bound, not instruction-bound: IPC ≈ 3, ~14 cyc/iter vs a ~4-5 cycle port floor, of which ~4.6 cyc/iter is branch-mispredict cost (0.27 misses/iter; ~9 branches/pair vs libzstd's ~5). The wall levers left, in evidence order: (a) memory-resident emit state is FALSIFIED (2026-09-11, see negative.md — LLVM already rematerializes the Vec len/cap at the emit sites, so only two pointers were ever live; the seqStore-cursor rewrite is the same two pointers, +0.6% fastest Ir, fast −4% wall on pure layout; true memory residency would need an outlined emit, whose per-match call overhead on json's emit density likely exceeds any win — model before trying); (b) deeper hash pipelining for miss runs (falsified once via the dfast v0 carry) stays blocked on register headroom with no identified source. Rep-probe *policy* halving (libzstd's ip2 one-compare-per-pair import) is falsified — our every-position rep probe is the only ml∈[4,6) detector (hash5 + ml≥6; json +4.75% size when halved), see negative.md; its branch cost is already folded. insert_covered AVX-512 batching is falsified twice, see negative.md; the literals huff0 path is 5.4% of json.fastest cycles): profile first to localize.
5. **json.Fast ~1.24× residue** (2026-09-12: W21 window landed — json ratio now exactly libzstd parity by construction, speeds +4-12% across json/text/skewed; on the 100MB binary corpus fast is 8.1% *denser* than libzstd-3 at x1.47 speed, was 28% sparser at x2.28 — see [matchers](perf/matchers.md) Fast section for the dll decomposition): the per-sequence encode path is near-tight (per-code row-pointer tables; precomputed transition diffs in FSE entries; sequence-code histograms sized to the 64-entry code space). Remaining: the concat's variable shifts + the writer's shift/OR/flush (throughput-bound, ~10.7% of json.fast cycles; entry extraction is down to ~6 ALU/channel) and the three-channel SIMD histogram in choose_tables_fast — untried but its 2.2%-of-cycles share caps the win low. The matcher itself (dfast, ~76% of json.fast cycles) is still the bigger lever; its position-u64 carry and the seqStore-cursor emit rework were both falsified (see negative.md), so the next matcher step shares item 4's remaining (b) or needs a new register source.
6. **Balanced speed** (json 2.10× vs zstd-6 after the 2026-09-12 W21 window — but now carrying +23.7% density (7.12 vs 5.76), so the fair speed comparison is against zstd-8/9 territory; the W21 cost itself is colder far candidates: json solo 107→92 MiB/s at instructions +3.5%, L1-misses 1.57→1.77/B): the cost is the per-walk second-chance search (2 chain searches per non-improving walk); per-probe/per-position cost of the chain-walk beat-check/unpack and the emit path is the other half (localize with enc_prof + perf). Miss-segment strategy directions (stepping/insertion scheduling) were falsified twice and are closed.
7. ~~**random tiers**~~ — closed. Best/Opt/Ultra via the pre-match incompressibility gate (2026-09-11); Fastest/Fast/Balanced via the sticky gate hold (2026-09-12: 1.68→2.4-2.5 GB/s, ahead of libzstd at all six levels — fastest x0.88, fast x0.80, balanced x0.61; the exact histogram sat at the 3-mem-ops/byte machine floor (~1.04 cyc/B measured), so it could only be skipped, not cheapened — see encoding.md's gate entry). The gated path now rests on the raw-memcpy/page-fault floor; nothing left at the block level.
8. **Best/Opt/Ultra speed**: ratio already ahead/level; speed is the remaining dimension — text Opt 313 vs libzstd ~440; the per-block DP cost behind Best's 12-19 MiB/s; streaming MT per-job fixed cost at the best tier (text.best streaming 0.8× its own ST). W22/W23 (2026-09-12) added a json-specific cost: solo Opt 3.77→7.44s, Ultra 7.59→13.32s, Best 2.66→3.72s for ~zero json ratio change (the reach pays only on dll-class data); the Opt-side relief landed same day (bt ring C22 at W23: json −19.6%/skewed −22% for +0.16% dll — Opt solo now 6.05s, footprint 96MiB) — beyond that it is walk/DP cost over the wider candidate space (profile with enc_prof + perf). Ultra dll residue: 7.7% sparser than zstd-19 at same W23/H22/C23 knobs minus C24 (C24 proven byte-identical — not the gap's source; suspects: search/pricing subtleties, per-block DP cost).
9. **Small/mid payloads (4K-1M)**: the small end closed by the small-literal huffman fix (2026-09-12: literals ≤1024 went out raw at a flat ~35% block tax; json-4KiB now ±1-5% vs libzstd, dict-encode gap +13.3→+10.3%). Mid-size json/text ~2× remains: per-byte cost of scan+emit+table build, not fixed overhead. The table-build half is done (`build_from_weights` counting sort + package-merge scratch/sort rework); remaining: the scan/emit per-byte cost only (back-walk buffer reuse falsified, see negative.md).
9a. **Large-binary ST encode (100MB-class files)**: corpus established (`bench/gen_big.sh`, ELF concat; avg_ll 3.58/avg_ml 10.9 — literal-heavy vs json's 0.72/15.3). The window deficit is fully closed (W21 at fast/balanced 2026-09-12, W22/W23 at best/opt/ultra same day: dll Best −41.8%/Opt −43.4%/Ultra −43.7%, Best/Opt now denser than zstd-12/16); at matched windows the remaining gaps are pure per-byte cost: fast x1.47 (329 vs ~480-560 MB/s, ~54 vs ~29 inst/B — 3× more scanned positions on long literal runs × our dearer per-position body; L1-misses 1.72/B vs json's 0.79), balanced x2.24. Literal-path volume is the dll-specific tax: 25% literal fraction puts huff0+compress_literals at ~20% of fastest cycles (vs 5% on json).

## P2 · Ratio and features

10. **Dictionary training**: encoder+decoder dictionary use is done (encode: content history + seeded entropy tables + dictID, ST paths; MT falls back ST; +13% vs libzstd on the sub-2KB systemd fixture — the small-payload band of item 9 dominates). Remaining: the `dict/` trainer's known bugs (epoch buffer hardcoded `vec![0;100]`, scoring iterating `collection_sample` instead of the current epoch) or port the C fastCover.
11. **LDM**: gear-hash long-distance matching; decoupled from window expansion (bare widening now landed everywhere it pays — W21/W22/W23 cover the 1-8MiB reach where real redundancy sits; LDM is for beyond-window distance classes and remains untested); can reuse existing matcher fusion (C's optLdm long-distance candidate injection is a reference).
12. **Streaming MT residue**: the burst model serializes accumulate (one serial 16KiB-pump memcpy pass), per-burst `thread::scope` spawning (~0.5-2ms/burst) and the burst barrier against the encoding itself — after the epoch-grid fix (2026-09-12, equal-size bursts + finish-tail re-slice + epoch-sized MADV_HUGEPAGE reservation, see [mt-stream](perf/mt-stream.md)) this is what bounds text.fastest at 1.8× and json.fastest at 1.26× vs zstd (12%/44% of own bulk ceiling); the overlapped producer-consumer pool is the persistent-pool redesign. Also here: the full strip-prefill cost for early unpledged floor-size jobs; the per-job NT table-clear fixed cost behind zeros.Balanced 0.8× (RLE corpus, low priority). ~~stream-MT job-boundary ratio penalty~~ — closed by the growing grid (2026-09-11) and kept by the epoch grid within ±0.1%; the residual text.fastest stream-vs-bulk gap is ±0.1% noise, not boundary-count linear.
13. **Encoder ratio residues** (2026-09-12 re-sweep after the opt-window widening, updated same day after the Ultra-mt seeding landed): only cells we lose vs libzstd: text.Balanced **−0.61%** (367.9 vs zstd-6's 370.2); text.Ultra −0.53..−0.58% (ST and mt alike — a parser-side residue, not a boundary effect); skewed opt/ultra/stream-mt −0.03..−0.09% (16-byte-alphabet fine residue, low priority); everything else at parity or denser (json.Balanced +24.1%, dll Best/Opt denser than zstd-12/16, json.Ultra.mt −1.85%→−0.13% via the isolated strip-tail statistics seeding — the per-job `opt_state` reset was the entire boundary cost: sequential-carry decomposition json.ultra 4×8MiB = stats 104KiB (price-feedback basin: BASE-priced parses lock into near-offset/ll0 shapes that updateStats reinforces) vs entropy restart 1.3KiB + rep gate 2.6KiB + tree refill 3.1KiB; see [mt-stream](perf/mt-stream.md) and negative.md for the falsified reachable-strip seeding). The json.opt bulk-mt 0.18%-denser-than-st anomaly remains unexplained (favorable direction, low priority).
14. **Long-term**: superblock (small-data ratio), preSplit content-adaptive chunking, AVX2/SSE2 intermediate SIMD tiers (the stable pattern of nested `#[target_feature]` + non-always `#[inline]` is verified; the risk is codegen interference between the N instantiations), C FFI + zstd CLI argument-surface alignment.

## Shelved (definitive conclusions; do not restart blindly)

- **Aligning frame-header strictness with libzstd**: libzstd rejects nonzero FHD reserved bits (bits 4-3) ("unsupported frame parameter") while zstdx accepts permissively (fuzz crash-b5593b58, second frame FHD=0x38, is exactly this shape). Tightening is a behavior change; run the interop corpus first to confirm no collateral breakage before touching it.
- **Streaming decode decode_step rework** (json/skewed residue 1.05-1.3×): the register wall is proven (~20 live values > 15 GPRs; three structural reworks falsified in a row) — no headroom unless live values shrink first; the next order of magnitude requires an algorithm-level change like libzstd's 8-deep sequence ring-buffer pipeline; assess benefit/risk first.
- **read::Encoder pump double copy**: reading directly into spare capacity would require handing uninitialized buffers to arbitrary Read impls; the trait contract forbids it; evaluated and dropped.
- **BMI2 dispatch Intel measurements**: retained as neutral zero-cost on Zen4; add data when an Intel machine is available.
- **madvise(MADV_HUGEPAGE)**: redundant on THP=always machines; retryable on THP=madvise machines (the extern "C" declaration route with zero new dependencies is verified).
