# Todo list

> Only remaining workable items. Landed work lives in [perf/](perf/) and git history; falsified directions in [negative.md](negative.md) — do not resurrect the falsified *mechanism*; changing its premise (algorithm class, window regime, corpus shape) reopens the goal.
>
> Item numbers are stable labels: deleted numbers (5, 9-13) were landed or folded. Older pages citing "todo 9-13" refer to the archived list (see this file's git history).
>
> Gaps with no workable direction left (identified floors or falsified levers; attribution in [perf/](perf/), falsifications in [negative](negative/)): json.fast x1.04 — the concat/writer tails are latency-bound, the SIMD histogram ceiling is ~0.02% wall, the dfast scan's excess is register-budget-bound; json.best x1.40-1.45 / opt/ultra x1.10-1.11 — per-node codegen floor and event volume 1.2-2.1× by construction (short of an event-side rethink), skewed.best ceiling ~2.16-2.22; 100MB binary ST encode fastest x1.36 (insert_covered store floor + the FSE arithmetic round) / balanced x1.49 (latency-bound walk; fast x1.06 closed) — no removable live value left in either scan loop; random fastest/fast x0.80-0.84 (minor); text.balanced bulk — the DUBT head parse's ~30% post-abort share has no safe lever (a shallower head parses worse and hands the chain worse reps; the stream-mt tail landed with the far-dead strip cap, 735 MiB/s = 87% of its bulk ceiling). Gap numbers: 2026-09-19 release snapshot ([bench/snapshot](bench/snapshot.md)).

## P0 · Structural

1. **MT decode — per-job content-adaptive D.** Pieces fully unchain only at D ≥ window (zero crossing reads); between, ⌊D/J⌋ pieces chain back via overlap; below J the job is serial-chain. Ratio demands D under the shape's dominant match period, which text-9 (period 800 KiB < window 4 MiB) cannot satisfy at any D. Unpledged streams remain: the growing grid's tail split depends on the encoder's worker count. The landed walls (json mt8: verdict + execute) are memory-bound floors — no wall lever short of overlapping execute with the verdict, a falsified class. Mechanism in [mt-stream](perf/mt-stream.md); falsified variants in [negative](negative/mt-stream.md).

## P1 · Decoding speed

2. **Streaming decode core — instruction volume; the read-chain family is spent.** json x1.24-1.36, skewed x1.10-1.32, text x1.04-1.21, dll x1.34-1.37 (the streaming column is the honest core comparison — own stream-vs-bulk overhead ≤2%). Differentially attributed 2026-09-20 vs libzstd 1.5.7 ([dec-gap](bench/dec-gap.md)): the whole gap is instruction volume in the fused loop (json 174 vs 126 Ir/seq at equal IPC and equal L1d misses; +48 = entry unpack +10, offset-history +12, stack round-trips +15, read+splits +11, literals +2; text is the latency exception — fewer instructions than zstd, copy-tail bound at avg ml 311). The read-side serial-chain folds are measured out (2026-09-20): the reload-window and consumed-into-ip folds win the dense shapes ~2% but reproducibly cost text 3-5% wall (the pre-shifted window's count-from-n is load-bearing for the long-sequence mix); two surgical Ir cuts (real-offset rotation fold −8.7 Ir/seq; 5-bit nb fields) also lost to the re-roll — the first to a text placement tax, the second at Ir outright — so sub-10 Ir/seq attrition is closed as a standalone lever. What remains: (a) the entry-struct layout swap is falsified (2026-09-20 second round: the field layout cut −15.1 Ir/seq and bought ≈0 cycles — IPC fell 1:1 with the instruction cut on every shape, text gate fail; the 2×u32 field-group variant lost at Ir outright; [negative](negative/decoding.md)); (b) the offset-history fold (−8.7 Ir/seq proven) has no ≥5% bundling partner left; (c) text's copy tail is measured out (2026-09-20, [dec-gap](bench/dec-gap.md) T4): 94% of match bytes are ~150 tile copies at offsets 256K-2M running at the L3 ceiling (loop IPC 3.57); every kernel re-shape (2×/4× unroll, AVX2/AVX-512, memcpy, NT stores) ties or loses on the replayed trace, the absorb is exact parity and only dilutes the wall ratio (x1.071 checked vs x1.15 unchecked), so the unchecked residual belongs to family (a)/(b). With the read-side folds and both entry layouts falsified, the fused loop has no open instruction-volume lever — the residual is issue/latency-structural. One diagnostic remains un-run: a DSB/LSD occupancy + issue-stall event profile (the issue/latency saturation was inferred from IPC conversion, never measured front-end directly). Every structural lever beyond that is falsified ([negative](negative/decoding.md)); attribution in [decoding](perf/decoding.md).

## P1 · Encoding speed

3. **Fastest — json x1.44 (600 vs 858 MiB/s class); the branch and table-geometry avenues are measured out.** The steady scan loop is branch/latency-bound (IPC ≈ 3); the mispredict budget is fully attributed 2026-09-20 ([matchers](perf/matchers.md)): 2.22M misses/encode ≈ 12% of cycles, insert_covered's fill carried the only removable lump (26-28%) and its harvest is falsified (`DenseFlat` — the store floor protects the rolled fill), extends are ≤2% each, the OR-fold fires 56% of the 2.35M pair iterations (the dense scan is hit-path-dominated), nothing else exceeds 12%. A branch-side dense lever no longer exists; what remains is per-event instruction volume (1.5× libzstd per probe event) on the ratio-chosen L2-resident 2^15 table — the geometry trade is swept and closed (2026-09-20: 2^13/2^14 buy json −8.7/−5.6% solo cycles and −0.35/−0.16% size but cost text +5.99/+2.16% and dll100 +6.85/+4.41%; 2^16 inverts the trade at +6.5% json cycles; see [negative](negative/matchers.md)) — 2^15 is the ratio-chosen optimum and the per-event excess is its price. Any new lever must still ship as a dense-mode instantiation keeping the plain body byte-for-byte (miss-run stepping and batched probing falsified as unconditional; the price bar cannot live in the plain body). The dense band gates from 256 KiB up; single-block payloads stay plain. json.fastest.mt8's gap is this per-core deficit scaled through workers.
4. **Small/mid payloads (4K-1M)**: the open speed item is the fastest tier's 64K-1M cells (x~1.36-1.48 json/text) — the same steady-scan deficit and ceiling as item 3, plus the emit-side instruction-volume floor (register-budget-bound; C-parity restructures falsified). Below 64K the fixed-cost side is closed: the per-block Huffman build, FSE CTable build, weight materialization, table serialization and the pooled-table struct moves are all at/below C parity ([encoding](perf/encoding.md)); what is left on a json-4K call is the plain-body scan of item 3 and that emit floor. The dictionary-history residue is item 6.

14. **Numeric rows 10-12 — the lazy band's remaining json hole.** Rows 5-8 landed on the tagged row matcher ([matchers](perf/matchers.md), 2026-09-20): json 5-8 x1.39-1.61 at -7.4..-17% size, text x0.40-0.60, dll 15-25% faster than the chain rows at ratio parity — but rows 10-12 remain chain depth 32/64 with json x1.82-2.87 (the snapshot's old item 6 band). The same swap should transfer (H22/S5/S6 → rowLog 5/6 at libzstd's `BOUNDED(4,searchLog,6)`), needing its own two-axis calibration round: the json speed residue is the shared selection machinery (the chain l5/l6 x1.20-1.24 bound, instruction-volume attributed), so the row there buys the dll/text/dll-100 columns, not the json column.

## P2 · Ratio and features

6. **Dictionary — residues entropy-side or unattributed** (54-file systemd holdout, per-file frames vs the zstd CLI, formatted own/its dict): -4 +6.0-6.9% is parse fragmentation with no isolated mechanism (nseq 197 vs 155, avg_ml 12.4 vs 15.9); -5 raw greedy +3.8-3.9% (x16 walk depth buys 19 B and saturates; the CLI's greedy beats its own -9 here); -1/-2 +0.6-1.2% entropy-side. Same-class round owed on the byte-identity contract: the no-dict fast rows' between-block treeless candidates still pay the description-charged gates (libzstd's `preferRepeat` accepts any live table blindly below 1 KiB) — changing it re-baselines no-dict bytes, so it needs its own round. [encoding](perf/encoding.md), [pitfalls](pitfalls/encoding.md).
7. **Streaming MT — per-job NT table-clear fixed cost** behind zeros.Balanced 0.8× (low priority; the far-dead strip cap 2026-09-18 already removed the zeros tail's strip-fill share, −26%). The other floors are closed or vetoed: json.fast's two-wave epoch gate can only post the last epoch at `finish` (incremental posting would change the bytes — vetoed on the byte-identity contract); the pump's last copy is the Read-trait floor for an opaque source ([mt-stream](perf/mt-stream.md)).
8. **Long-term**: superblock (small-data ratio), preSplit content-adaptive chunking, AVX2/SSE2 intermediate SIMD tiers (the nested `#[target_feature]` + non-always `#[inline]` pattern is verified; the risk is codegen interference between the N instantiations), C FFI + zstd CLI argument-surface alignment.
