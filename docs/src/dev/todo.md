# Todo list

> Only remaining workable items. Landed work lives in [perf/](perf/) and git history; falsified directions in [negative.md](negative.md) — do not resurrect the falsified *mechanism*; changing its premise (algorithm class, window regime, corpus shape) reopens the goal.
>
> Item numbers are stable labels: deleted numbers (2, 5, 7, 9-14) were landed or folded (7: the chain head origin retired the per-job clear, 2026-09-23). Older pages citing "todo 9-13" refer to the archived list (see this file's git history).
>
> Gaps with no workable direction left (identified floors or falsified levers; attribution in [perf/](perf/), falsifications in [negative](negative/)): streaming decode json x1.24-1.36 / skewed x1.10-1.32 / text x1.04-1.21 / dll x1.34-1.37 — the fused loop's cycle gap is measured as pure dispatched-op volume under identical front-end service (2026-09-21 direct front-end profile: op-cache occupancy 99.5% on both sides, loop buffer unused, FE-stall differential ≤2.4 cycle-equivalent pp and unconcentrated, IBS fetch latency ~0 in the loop; [dec-gap](bench/dec-gap.md) T5); every instruction-volume, structural and layout lever is falsified or measured out — closed; the streaming-encode ST wrapper is likewise closed (2026-09-21: the ST core pool + the btlazy frame-start clear diet put every ST stream cell at its bulk core's own x — text.best stream x1.40→x0.82, json.best x1.46→x1.38 = the core deficit below; [mt-stream](perf/mt-stream.md)); json.fast x1.04 — the concat/writer tails are latency-bound, the SIMD histogram ceiling is ~0.02% wall, the dfast scan's excess is register-budget-bound; json.best x1.41 / opt/ultra x1.10-1.12 — per-node codegen floor and event volume, now measured to the factor (2026-09-21: 1.20× search count — the +2.5%/+12.8% json/dll density over zstd-13 — times 1.39× Ir per call at parity L1d/branch misses and better IPC; the codegen diet `41d73ac8` took its bite, the residue is distributed 2-3-instruction licks per tree event and the events ARE the density mechanism; see [matchers](perf/matchers.md)), skewed.best ~x2.05 (the same stream on a latency-bound walk: IPC 0.54 vs C's 0.74); 100MB binary ST encode fastest x1.36 (insert_covered store floor + the FSE arithmetic round) / balanced x1.49 (latency-bound walk; fast x1.06 closed) — no removable live value left in either scan loop; random fastest/fast x0.80-0.84 (minor); text.balanced bulk — the DUBT head parse's ~30% post-abort share has no safe lever (a shallower head parses worse and hands the chain worse reps; the stream-mt tail landed with the far-dead strip cap, 735 MiB/s = 87% of its bulk ceiling); dll100.balanced was the same trade measured to parity (Ir matches C's row matcher at L9, 2.91G vs 2.75G per dll32 pass, while the chain walk's scattered u32 hops stall against the row matcher's packed cache lines) — superseded 2026-09-24 by the row-9 LDM gap-parse consumer ([matchers](perf/matchers.md)): the duplicated mass left the chain walk entirely, dll100 balanced st 20.53 → 19.74 MB (−3.85%, 1.9% denser than zstd -9 --long=26) at −6.4% instructions, +13.0% dll100 density vs zstd-9 (19.74 vs 22.70 MB). Gap numbers: 2026-09-19 release snapshot ([bench/snapshot](bench/snapshot.md)).

## P0 · Structural

1. **MT decode — per-job content-adaptive D.** Pieces fully unchain only at D ≥ window (zero crossing reads); between, ⌊D/J⌋ pieces chain back via overlap; below J the job is serial-chain. Ratio demands D under the shape's dominant match period, which text-9 (period 800 KiB < window 4 MiB) cannot satisfy at any D. Unpledged streams remain: the growing grid's tail split depends on the encoder's worker count. The landed walls (json mt8: verdict + execute) are memory-bound floors — no wall lever short of overlapping execute with the verdict, a falsified class. Mechanism in [mt-stream](perf/mt-stream.md); falsified variants in [negative](negative/mt-stream.md).

## P1 · Encoding speed

3. **Fastest — json x1.44 (600 vs 858 MiB/s class); the branch, table-geometry, and instruction-volume avenues are all measured out.** The steady scan loop is branch/latency-bound (IPC ≈ 3); the mispredict budget is fully attributed 2026-09-20 ([matchers](perf/matchers.md)): 2.22M misses/encode ≈ 12% of cycles, insert_covered's fill carried the only removable lump (26-28%) and its harvest is falsified (`DenseFlat` — the store floor protects the rolled fill), extends are ≤2% each, the OR-fold fires 56% of the 2.35M pair iterations (the dense scan is hit-path-dominated), nothing else exceeds 12%. A branch-side dense lever no longer exists. The per-event Ir decomposition (2026-09-23, [matchers](perf/matchers.md): matcher+fill 2.25× C while the entropy side wins at 0.82×; the resolve block is the largest single lump at 13%) exhausted its structural levers too: the u32 idx-space resolve falsified on codegen, and the register-removal class (incremental idx/pos32 cursor maintenance, in both the pos32-IV and the pure-idx-IV+idx-space-emit forms) falsified 2026-09-23 — the base's pos-as-IV gets pos32 free in the register low half and pays one sub for idx, so every idx-IV form buys `win_base` back as an add plus a spill reload ([negative](negative/matchers.md)); the table-geometry trade is likewise swept and closed (2^13/2^14 buy json −8.7/−5.6% solo cycles and −0.35/−0.16% size but cost text +5.99/+2.16% and dll100 +6.85/+4.41%; 2^16 inverts at +6.5% json cycles). 2^15 is the ratio-chosen optimum and the 1.5× per-event instruction volume is its closed price; any new lever must still ship as a dense-mode instantiation keeping the plain body byte-for-byte (miss-run stepping and batched probing falsified as unconditional). The known residual defect: past 2^32 + max_window of a stream the u64 resolve rejects all hash candidates (documented at the resolve; repcodes keep output valid). json.fastest.mt8's gap is this per-core deficit scaled through workers.
4. **Small/mid payloads (4K-1M)**: the open speed item is the fastest tier's 64K-1M cells (x~1.36-1.48 json/text) — the same steady-scan deficit and ceiling as item 3, plus the emit-side instruction-volume floor (register-budget-bound; C-parity restructures falsified). Below 64K the fixed-cost side is closed: the per-block Huffman build, FSE CTable build, weight materialization, table serialization and the pooled-table struct moves are all at/below C parity ([encoding](perf/encoding.md)); what is left on a json-4K call is the plain-body scan of item 3 and that emit floor. The dictionary-history residue is item 6.
10. **text.balanced stream-mt8 — the finish tail after the far-dead build retirement (2026-09-23, r17)**: the far-class screen now resolves inside `build_spf`'s segment loop, so far-dead frames no longer build-and-drop the prefix fill — text +14.6%, zeros +43% ([mt-stream](perf/mt-stream.md); the r15 "grid_fill 66%" attribution was corrected: the whale was the caller-side build, invisible to jobdecomp's spans — the new `spf build` span closes the gap, see [pitfalls](pitfalls/workflow.md)). Remaining pieces, in wall order: (a) the **donation wait** — the probe's two 2 MiB parses (~25-35 ms on text, posted at the 8 MiB staging gate) outlast the pump and leave ~15 ms of exposed wait inside finish; the verdict gates posting, so the wait cannot overlap the tail jobs, and both levers (cheaper probe parses, an earlier posting gate) change which frames probe or which verdict lands — byte-affecting, untouched; (b) **post+drain** (9-18 ms): 8 jobs × reach-capped strip fill + gate replay + parse in parallel. The **far-alive** class (dll-shaped streams) keeps the serial build + adopter remainders: segment-parallel build at LDM batch-freeze boundaries (r8-strip's exactness lesson; the chain grid's head-table dependency makes segments non-independent — the researched repair is a head-snapshot handoff per segment). Bulk-side siblings, both measured with the new span and both deprioritized: the capture class (`plan_prefix_ldm`, dll-only in practice — text's head is narrow so no capture, json shrinks) has no far-repeat veto, so a wild far-dead wide-alphabet frame would pay the build + whole-window strips, but no corpus cell exercises it and the pledged-stream capture parity (`pledged_stream_matches_bulk_mt`; the donation arms at the staging gate while the veto's span is not buffered until the first job's grid line) blocks a trivial port; text.balanced **bulk**-mt8's own deficit is a different family — the jobs' strip fills run 8-way memory-bound on the cold per-call pool (job_sum ≈ wall, grid_fill 37-79 ms summed; dll32's caller build is 50 ms of its 258 ms wall and load-bearing there). Byte-identity + determinism contracts as ever; falsified neighbors in [negative](negative/mt-stream.md).

## P2 · Ratio and features

6. **Dictionary — btopt-row density remains** (systemd holdout, per-file frames vs the zstd CLI, formatted own/its dict): the -4..-8 parse residues closed 2026-09-21 by the <=16 KiB payload-row strategy swap; the opt-family rows 11-22 followed 2026-09-22 (ultra-priced small rows, tuned past the table: -11/-12 raw +4.6→+2.9, -13..-15 → −1.6..−2.7 ahead, -16/-17 +3.4/+3.3→+0.6, formatted ahead at every level), and the -5..-9 small-frame entropy tail closed 2026-09-22 by porting libzstd's lazy+ three-way sequence-table selection (exact integer costs, the real NCount bytes; -5..-9 raw +0.9..+3.5 → −1.8..+0.4%, formatted all ahead; [encoding](perf/encoding.md)). The opt-family residues (-11/-12 raw +2.9/+2.3, -16..-22 raw +0.6) did not reproduce on the 2026-09-22 R12 rebuilt fixtures (same-dict pairing: our rows ahead at every level; attributed to own-dict pairing variance, dict content quality swinging ~±440 B across train splits — falsified as a parse gap, do not re-scan); the swing itself was closed 2026-09-22 (R13) as two trainer defects: the k-sweep pick rides a noisy 22-file internal metric whose argmin is grid-geometry-bound (the compact 9-value ladder chased its noise; the CLI's 5-value ladder landed), and the formatted dicts carried entropy tables built from our densest parse's histograms (stats level 3 -> 4, all samples) — with both fixed the trainer is ahead of `zstd --train`'s content at every seed/level raw and in formatted totals (−1,862 B; 7/12 cells, seed-22 trailing 32-52 B/level) on the 3-seed fixture ([encoding](perf/encoding.md), [negative](negative/dict.md)), so future dict-row measurements should re-baseline from the R13 trainer; the -6..-8 depth headroom closed 2026-09-22 as three dict-row candidate-set defects (C14 ring aliasing into the dict head, H14 class dilution, missing libzstd catch-up/interior fill coverage — R12, [encoding](perf/encoding.md)): -6..-8 closed 2026-09-23 as falsified (R15) — on the R13-trainer re-baseline (3-seed 86/54 fixture, 22-level x 4-dict grid) -7/-8 are ahead at every pairing (cli-raw 0..-13 B, ours-fmt -26..-64 B); an 8-seed study puts the same-pairing differential's split-variance at +-20 B (the only behind cells anywhere: ours-raw on 1/8 seeds, +7..+10 B), and the per-file decomposition of those cells shows same-position alternate picks, not absent candidates (7/10 files with zero missed ref matches; the 3 events are a longer match we take instead / a farther 22 B libzstd's own depth-2 gain compare would reject) — the two-state candidate-set attribution is withdrawn ([encoding](perf/encoding.md), [negative](negative/matchers.md)); the -6-vs-7/8 pairing delta itself is row-power alignment (our tuned rows 6-8 emit identical bytes while the CLI's row ladder self-improves ~22 B from lazy to lazy2); the R13 frontier-skip attempt to close them (and the -4 residue) by skipping the covered fill below the scan's insert frontier was landed and reverted same round — it regressed every dict row on the 3-seed fixture (the rep-probe advance's never-inserted positions and the opt walks' interior fills lost their only cover; [negative](negative/matchers.md)), so the exact-once insert discipline (fill the rep-advance holes, never re-insert a head) is the remaining shape; -1/-2 +0.6-1.2% entropy-side did not reproduce on the 2026-09-22 rebuilt fixture (both rows ahead at −1.6/−2.1% formatted) — the preferRepeat hypothesis for it is falsified (2026-09-21): libzstd's blind treeless acceptance (rows below lazy, literals <= 1 KiB) loses 4-5 B on every dict row it touches and is byte-identical on the no-dict corpus, so the exact cost compare stays and the residue lives elsewhere ([negative](negative/entropy.md)). [encoding](perf/encoding.md), [pitfalls](pitfalls/encoding.md).
8. **Long-term**: superblock (small-data ratio), AVX2/SSE2 intermediate SIMD tiers (the nested `#[target_feature]` + non-always `#[inline]` pattern is verified; the risk is codegen interference between the N instantiations), C FFI + zstd CLI argument-surface alignment. (preSplit content-adaptive chunking landed 2026-09-23 — [encoding](perf/encoding.md).)
9. **Small-payload ratio residues**: the 2026-09-23 R14 round closed the fixed pieces — the pledged-stream empty closing block (−3 B/frame, the close-ahead-of-eager-drain fix), the lazy+ exact sequence selection on ≤ 128 KiB no-dict frames at levels 6+ (json l9/l19 at parity at 2-4K; multi-block frames keep the heuristic — the exact arm loses there with our parses, falsified in [negative](negative/entropy.md)), and the ≤ 4 KiB dense-bars join for fast/dfast (json 1K l1/l3 at parity or ahead; text l1 ahead at every size) — [encoding](perf/encoding.md). What remains is parse-side, not description forms: json l1 2K/4K +12/+27 B is literal volume (lit_bound +8.9% at 2K with sequence bounds at parity; the fast row's candidate set at sizes where the bars already match libzstd's rows), json 1K l9 +11 B is the same class one family up (269 lit/41 seq vs 239/46), and the text/json l19 tails are 1-3 B of stream ordering over equal-count parses — the accepted tie class. The "desc bytes at tiny sizes" attribution is falsified for these cells: literal Huffman weight descriptions measure at parity (3-5 B, weight-FSE tlog 5 both sides).
