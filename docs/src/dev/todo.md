# Todo list

> Only remaining gaps vs libzstd and the directions that could close them. Landed work lives in [perf/](perf/) and [completed](completed.md); falsified directions are in [negative.md](negative.md) — do not resurrect the falsified *mechanism*; changing its premise (algorithm class, window regime, corpus shape) reopens the goal.
>
> Coverage check (2026-09-16, fresh matrix at `dedd267d` — [snapshot](bench/snapshot.md)): every deficit ≥1.2× maps to at least one direction below. Honest exceptions: json.fastest ST x1.44 (the dense-mode hosting spent item 3's last angle; realistic ceiling ~x1.3-1.4), random fastest/fast (x0.84-0.90, minor).

## P0 · Structural

1. **MT decode stage B — piece-parallel landed for ramp frames (2026-09-16); residue open**
   - History: piece watermarks with exact per-sequence source gating were byte-correct but shallow near-boundary reads (~100-400 B offsets) chain pieces serially — see [negative.md](negative.md); the serial-B + A/B-overlap structure stayed. Landed instead (2026-09-15): wildcopy staged execution, pooled staging buffers, zero-seq block fix (json.zst3 mt4 1.03→1.56×, skewed.zst9 0.71→1.24× on the 32 MiB matrix), checksum verification absorbed inline in stage B (`frame_checksum.rs`).
   - The **decoder-side piece-parallel stage B landed (2026-09-16, `decoding/mt_pieces.rs`)**, env-paired with the encoder ramp (`ZSTDX_MT_RAMP_BYTES`): measured engagement (sentinel-history validation of candidate job grids from the formula lattice; lag gate 3/8 of the job size), in-order piece claims on a contiguous-executed watermark, ordered checksum absorb, serial-B fallback on validation failure. 32 MiB, D=2 MiB: json.lvl3 mt8 **3.2× the serial-B decoder / 3.5× own ST** at +0.00% size, dll32 mt8 2.6-2.9×, skewed ~1.1× (entropy-bound). Mechanism, the falsified restart-grid discovery, and open items in [mt-stream](perf/mt-stream.md).
   - Open: ~~the staging barrier (default-unsafe until serial execution overlaps staging with a mid-flight piece switch — the env gate exists for this)~~ **falsified (2026-09-17, [negative](negative/mt-stream.md))**: the hybrid overlap was implemented and measured — the caller's streaming execution evicts the workers' L3-resident staged set (validation round 0.63→7.25 ms) and delays the switch itself, losing on every literal-heavy cell (json mt8 5073→2369); the barrier form is optimal among tested variants, worker-side levers only remain (validation fused into staging per-piece, transition-wake shaving — mt8 json phase wall: stage 2.1 + validate 0.63 + exec 2.8 ms vs an ~2.8 ms memory-bound exec floor at ~11.4 GB/s); the `MIN_MT_INPUT` compressed-size floor keeps high-ratio shapes (text) off the whole MT decode path; per-job content-adaptive D; unpledged streams.
## P1 · Decoding speed

2. **Streaming decode core residue — x1.27-1.36 json, x1.09-1.31 skewed, x1.04-1.23 text** (09-14 pass, reproduced 09-16). The streaming column is the honest core comparison (zstd's bulk API is a slow wrapper; our own stream-vs-bulk overhead is ≤2%), so this is the fused sequence loop vs libzstd's pipeline, not a wrapper cost. The loop is proven **throughput-bound, not latency-bound** (IPC 3.45, flat cycle profile, prefetch-negative — attribution in [decoding](perf/decoding.md)): the 8-deep ring-buffer pipeline was built in two variants and lost on every shape, so the overlap it would add already exists for free via OOO and the structural class is closed ([negative.md](negative.md)). What remains is instruction-count attrition vs libzstd's hand-tuned loop (entry unpacking 6+ uops/sequence, shlx/shrx chains, ~20 live values with residual spilling) — uop-by-uop cuts convert ~1:1 into wall on a throughput-bound loop, but the easy ones are taken. **SIMD copy scheduling is assessed and falsified (2026-09-15, [negative](negative.md))**: libzstd's wildcopy issue structure on the existing kernels shrinks the exec half as designed (−0.5-0.6M Ir json) but the fused loop is one register-allocation domain — the saturated budget returns the saving as decode-half spills on every shape, and even removing the statically dead `ml > 0` branch nets +0.3-0.4% Ir. No structural lever is left; only decode-half uop attrition (entry unpacking, shlx/shrx chains) whose every source change re-rolls the whole loop's allocation (±600-800K Ir on json), which makes each further cut a lottery against a ~10 Ir/seq spill set.

## P1 · Encoding speed

3. **Fastest — json x1.52 vs libzstd-1** (2026-09-14; was 1.57 — the unconditional bit-writer flush removed the largest mispredict, json.fastest 541→562). The steady scan loop is branch/latency-bound, not instruction-bound: IPC ≈ 3, ~14 cyc/iter vs a ~4-5 cyc port floor, ~4 cyc/iter of it branch-mispredict cost (~8 branches/pair vs libzstd's ~5). **The differential loop study is done (2026-09-14, LBR per-branch attribution — see [perf/encoding](perf/encoding.md))**: the extra-branch inventory is named and now largely collected — the writer headroom branch (largest, ~18%) fell to the unconditional flush, the rep OR-fold's short-circuit compiled to 3 branches (fixed, one branch now), insert_covered's parity guard fixed by peel-then-pairs; extend_match loop exits and the two hash-match compares are inherent (libzstd pays the same class). Remaining mispredict budget is spread across the inherent set — no single lever left. Deeper hash pipelining for miss runs is blocked on register headroom, no identified source. json.fastest.mt8's gap is this same per-core deficit scaled through workers — it closes with the ST loop, not with MT scheduling. Both todo angles are now tried and falsified (2026-09-15, ratio-gated interleaved A/Bs — see [negative.md](negative.md)): **miss-run stepping** wins json both ways (−3.2% size, 567→617 MiB/s) but regresses text +2.3..+7.4% at every threshold and under parse-statistic gating (span-EMA, ml-EMA) — text's damaged short-match stretches are locally json-shaped, no in-loop discriminator exists; the dense-insert rescue hits the random-store floor (skewed −54% speed). **Search-side batched probing** (scalar pair-of-pairs, single exit branch, scalar-exact lane placement) is output-neutral but adds +3.8% Ir and −10..14% wall on json/text — json's miss runs are 1-2 pairs, so batch entry + dropout re-probe never amortize; the AVX-512 flavor would only add gather costs on the same structure. Positive residue from the stepping A/Bs: json.fastest's parse is weak-match-polluted (skipping mid-run weak matches improves it on both axes) — a shape-safe acceptance-bar/fragmentation lever on match-dense parses is the one unexplored direction left here. **Tried 2026-09-16** ([negative](negative/matchers.md)): the fed-back-literal-price bar (the chain store gate's own pricing, transplanted to the fast hash path) IS shape-safe and wins json.fastest −1.25% size in every mode with text/skewed unharmed — the pollution is recoverable — but the scan loop cannot host it: a dormant bar (code present, never taken, byte-identical output) costs text.fastest −12% and json.fastest −4% wall, and the outlined-cold form does not recover. Static flat prices are falsified outright (text +1.5% at 3 bits/B). Reopening requires a hosting that keeps the loop body byte-for-byte (e.g. a dense-mode scan instantiation chosen per block). **Landed as exactly that (2026-09-16, see [matchers](perf/matchers.md))**: the fast scan now instantiates `const DENSE` per block — decided from the previous block's parse (≥512 seqs, <4 lit-B/seq, fed-back literal table covering <32 symbols; parse stats alone cannot separate the classes — text's four match-dense blocks carry 99.5% of its sequences and overlap json's nseq/avg_ll bands, the literal alphabet is the separator) — and the dense body hosts the price bar plus miss-run stepping. json.fastest −3.3..−3.9% size in all four modes (bulk-st 5,466,381→5,252,606) at 559→590 MiB/s interleaved (x1.53→x1.44); every other cell byte-identical (120-cell sweep, full-ladder dump, dll100 l1/l3); text.fastest −1.8% wall at identical output (the plain instantiation's placement in the doubled-code binary, inside the cell's documented noise band).
4. **Fast — json x1.09** (was 1.11; the unconditional bit-writer flush moved it, 425→447). The per-sequence encode path is near-tight. Remaining: the concat's variable shifts + the writer's shift/OR tail (~10.7% of json.fast cycles before the flush; the 2026-09-16 cursor-loop form cut fastest/fast json −0.35/−0.42% Ir byte-identically but the loop is latency-bound — the two-level row-pointer loads plus the serial accumulator chain set its floor, and `nb` is per-entry (double-state split), so the row tables cannot carry the widths), the three-channel SIMD histogram in choose_tables_fast (2.46% of cycles, skipped: the pass is RMW-bound at 3 counter updates/seq, SIMD only reaches the 64-entry lane merge — a ~0.02% wall ceiling), and the dfast matcher itself (still the dominant share of json.fast cycles). Position-u64 carry and seqStore-cursor emit are falsified ([negative.md](negative.md)).
5. **Balanced — json x0.86 vs zstd-9 (ahead), carrying +21% density** (2026-09-15; was x1.34 behind). Closed model: the two steady chain-walk copies are a latency-bound chain-link chase (82% of cycles, 93% of L1 misses, ~66 cyc/step ≈ one serialized L3-class load) — Ir/register work no longer converts to wall ([negative.md](negative.md)). The LDM-carried-tiles premise was falsified (2026-09-15, item 11): the reach-shrink curve is a pure chain-reach effect, and no static reach serves both shape classes (json monotone-prefers W12: −10% size and 2.3× solo speed vs W22; dll100 needs W22: W20 +4%). **Closed (2026-09-15): the shape-adaptive reach landed as a prefix dual-parse probe** (`encoding/reach_probe.rs`; mechanism and data in [matchers](perf/matchers.md)) — each eligible row-9 frame (≥8 MiB declared, bulk st/mt or pledged streaming) parses its first 2 MiB twice (W22 vs W12) through the row's own pipeline and takes the cheaper pre-entropy cost: json-class frames shrink (json −1.14% st/−1.39% mt size, solo 76→140 MiB/s, matrix x1.34→x0.86, x0.76 on the 09-16 fresh matrix — the tier is ahead of zstd-9 on both axes), dll-class frames keep (dll32/dll100 byte-identical, dll100 106→101 MiB/s), text/skewed keep byte-identical. The probe's two parses were a fixed ~36 ms tax per ≥8 MiB frame; **dismantled 2026-09-17** (shrink side headless + the gate in the probe + the keep side donated into the frame/job zero with a feedback-calibrated verdict — mechanism and gates in [matchers](perf/matchers.md), output byte-identical across dump/120-cell/emitframe): text.balanced 499→867 st / 444→701 mt8 MiB/s, skewed ~1390→~1800, random 951→2076, dll100 +3%, json −5% (its donated keep parse is discarded). **The stream-MT core donates too (2026-09-17, r7)**: the staging gate posts the keep side to a pool worker as job zero's own blocks and the shrink side to a second worker concurrently (serial keep+shrink ≈ 32 ms measured — more than the cell's tail can hide), the verdict is consumed lazily where the schedule first needs it, and a process-global kit pool keeps the donation's state/driver warm across encoders (ephemeral pool threads measured 4-5x cold-table parse cost). text.balanced stream-mt8 366→445 MiB/s (x3.28→x2.16) at json ratio parity; output byte-identical (emitframe stream, 60-cell stream sweep). The cell's remaining distance to the bulk-mt8 ceiling (~700) is the finish-tail strip prefills, not the probe: an unpledged row-9 stream posts nothing during write (epoch 0 = 512 MiB at the whole-window strip), so at finish 8 tail jobs prefill strips [0,4k) MiB — 243 ms summed (jobdecomp), ~50 ms critical at ~0.5 GiB/s/worker vs bulk's 4 MiB strips. The strips are the cross-job LDM far-class carrier (byte-load-bearing; dll32 stream-mt is 20% denser than bulk-mt through them). **Closed (2026-09-17, r8-strip — the finish-tail shared prefix fill)**: one state fills the median tail prefix once, cut at LDM batch-freeze points (the only exactly-resumable boundaries — the asm gear4 c4 checkpoint diverges from the portable reference, see [negative](negative/mt-stream.md)); tail jobs above the median adopt the snapshot and fill only their remainder, jobs below keep stock — chain rows only, adopters prefix-bounded (`start <= overlap`; both release-safety gates were born as segfault/divergence repros). Output byte-identical (dump, 120-cell, emitframe stream incl. a 96 MiB probe); text.balanced stream-mt8 interleaved medians 537→568 MiB/s (+5.6%). Mechanism in [mt-stream](perf/mt-stream.md). In-loop far/near discriminators are falsified ([negative](negative.md)), only a real second parse measures the trade. The stream-mt core landed with its own staging gate (2026-09-16, see [matchers](perf/matchers.md)): a pledged stream decides at construction, an open-ended one probes once the frame holds 8 MiB (flush/finish ahead of the gate keeps the stock reach); only the unpledged ST core stays excluded (blocks flow at block size).
6. **Best/Opt/Ultra speed**: Best's finder is now libzstd's DUBT (`encoding/dubt.rs`, 2026-09-14 — see [matchers](perf/matchers.md)): fill O(1)/position, search-time batch sort; json x2.163→**x1.627** (ratio 6.25 vs 6.10), text x1.596→**x1.184** (−0.45%), skewed x3.709→x2.865; streaming best re-measured: json stream ST x4.05→x1.43, stream-mt8 x3.31→**x0.78 (ahead)**, text stream x2.60→x1.44 (09-16 fresh matrix). The Keep/STALE fill machinery retired with it. Remaining here:
   - ~~skewed.best x2.87~~ closed (2026-09-15): the taint-free cost model attributed it to the u64 entry form's 96 MiB random-access footprint (memory-latency-bound tree walk, one ~DRAM load per step; see [matchers](perf/matchers.md)) — u32 entries halve the working set, skewed x2.865→**x2.16-2.22**, json x1.627→x1.38-1.50, text x1.184→**x0.82-0.94 (ahead)**, output byte-identical (09-16 matrix: 2.15/1.43/0.95);
   - json's remaining x1.39-1.45: the pricing loops are no longer the differential (selection fused into the finder, 2026-09-15 — see matchers; the array round trip was 0.7G of a 9.6G encode, the old ~2.4G attribution was a toggle-collect artifact). Instrumented events vs C put the remainder in per-node codegen (register pressure/64-bit arithmetic; two restructurings falsified) and event volume (1.2-2.1×: the denser parse searches 21% more positions — cutting events cuts the +2.5% density itself). No identified safe lever; ~1.2-1.3× is this shape's ceiling short of an event-side rethink;
   - ~~keep-the-opt-row cost-cutting~~ closed (2026-09-16, fill-lag clamp — see [matchers](perf/matchers.md)): instrumented events vs C showed the DP driver, search side and rep loops already at parity (json 4 MiB slice event-identical to libzstd's); the whole text deficit was the tree fill re-indexing the previous block every block (block-tiled lags of exactly one 128K block; 52% of the file re-inserted, 8.6× C's fill bytes). C's `ZSTD_buildSeqStore` "limited update after a very long match" clamp (never ported) caps the carry-over at 192 positions — landed gated to the alphabet-gated population with all 120 ratio cells + dll100/dll32/dll16 byte-identical: **text.opt matrix x1.4→x0.622 (733 vs 456 MiB/s, 1.6× faster than libzstd-17), text.ultra →x0.706**; the unconditional and job-strip forms are falsified in [negative](negative/matchers.md) (dll100 −0.65%, stream-mt +16 B);
   - json's opt/ultra x1.30-1.33 is per-node codegen + event volume (the Best-tier residue analysis carries over: identical events, +13% instructions on the same work);
   - ~~streaming MT per-job fixed cost (opt rows)~~ decomposed and the largest head landed (2026-09-16): `zstdx-bench jobdecomp` (the `job_trace` spans) put the strip tree-fill at 40-73% of summed job time on every shape, ultra's seed parse #2 on text only (32-38%), the stats reset at noise; the opt rows' tree fill now covers only the tail half of the job strip (LDM keeps the whole strip — halving both cost dll100 +4.13%, falsified) and ultra's job seed span halved to one block. Gates: corpus worst +0.011% (json.opt mt), dll100 −0.002%, dll32/dll16 denser, st byte-identical; interleaved mt8: json.opt stream +44% (9→13 MiB/s), json.opt bulk +36%, dll100.opt bulk +20%, text.ultra stream +10%, text.opt stream +3% (skip-assisted fill, pump-bound cell), json.ultra flat. `matrix enc-stream` covers the opt/ultra rows now. The fill's per-insert lever is landed too (2026-09-16, u32 tree entries on per-state monotone coordinates — see [matchers](perf/matchers.md)): opt/ultra table working sets halve (96→48 / 160→80.5 MiB) at byte-identical output everywhere; jobdecomp json.opt fill 5→6-7 MiB/s with the walls −16..−22% (json) and text.opt/ultra −46..−52% (the skip-assisted fill and the walks both ride the halved traffic); the fresh-state table-build span halves with the footprint (the MADV_HUGEPAGE advice on the fresh allocs is falsified in [negative](negative/mt-stream.md) — direct-compaction stalls doubled it on this box). Ultra dll residue closed by optLdm (ultra now 9.8% denser than zstd-19; --long=26 still 6% ahead of us — its 128 MiB tree).
7. **Small/mid payloads (4K-1M)**: small end closed (json-4KiB ratio ±1-5% since the small-literal huffman fix; the dict residue is item 10). Mid-size re-measured under checksum parity (2026-09-16): the recorded "~2× json/text" was partly a bench artifact — `small` timed our checksummed frames plus the ≥256 KiB sidecar thread against libzstd's plain bulk path (~8-12% of the gap; the bench now runs both sides checksum-off). The honest band: fastest json x1.39-1.53 / text x1.52-1.72 at 64K-1M, fast x1.22-1.35, balanced and above at parity to 6× ahead — so the open speed item is the fastest tier's 64K-1M cells, which are the same steady-scan deficit as item 3 (scan+insert 60% of cycles; the two hash-match compare branches were 9.5-12.6% of samples) plus the entropy stage's per-block share (~17%, already 0.55-0.72× libzstd per byte). The compare OR-fold landed (2026-09-16, see matchers): json-256K x1.53→1.40, json-1M x1.51→1.41, text-256K x1.72→1.65, text-1M x1.60→1.55, at a json-64K −5% cost (the merged branch mispredicts in the L1-resident regime). Post-fold the scan's branch inventory is flat (max 1.6%) — item 3's ~x1.3-1.4 ceiling applies to the band; below 64K the cells are fixed-cost-bound (our per-call fixed ≈7 µs vs libzstd's ≈1.9: allocator churn, per-block entropy builds — memmove+memset+malloc ≈ 11% of a json-4K call, entropy ≈ 50%), a different lever than the scan's. The fixed-cost cut landed (2026-09-16): callgrind on the json-4K call attributed ~15% of Ir to build memsets (dominated by the FSE `vec![0u32]` transition tables) and ~3% to malloc/free; the transition/codes/package-merge buffers are now pooled in `BlockScratch` and recycled through table adoption, raw fallback and frame reset (see [perf/encoding](perf/encoding.md)) — json-4K −13.6% Ir / wall x1.96→1.83, text-4K −11.1% / x1.91→1.87, 8K/16K −4..6% wall, skewed-4K x0.90→0.80, 1K cells −13-15%; 64K-1M and the 32MiB matrix flat (json) to ahead (text). The deterministic gungraun small bench landed with it as the band's regression gate. The follow-up landed the same day — **the FSE transition tables moved to libzstd's arithmetic `FSE_CTable` form** (tt deltas + u16 state table, ≤10 KB pooled vs the 218 KB materialized matrix; the build walk drops from ~54K to ~1.4K stores per fresh-table block and the per-block row-pointer setup disappears; see [perf/encoding](perf/encoding.md)): small band json-4K x1.83→**1.62** / text-4K →1.75 (−9.4/−6.1% wall), the whole 4K-1M band −1.6..−9.3% (json-64K fast crosses to x0.98, json-1M fastest x1.365), and the spill-over pays on the per-block-heavy big shapes too (matrix json.fastest −2.0%/json.fast −3.9%/text fastest-fast −2.4/−3.3%; dll100 fastest +1.6-2.2%, balanced +1-5%) — the json 1 MiB Ir +1.8-3.5% (the arithmetic step costs ~25 Ir more per sequence than the packed entry) does not convert to wall. The dense-mode scan (item 3) reaches the band from 256 KiB up (single-block payloads stay plain): json-1M −2.72% size at 608→630 MiB/s interleaved (x1.425→1.374), json-256K byte-identical (its first block keeps raw literals, so the fed-back table stays the 256-wide default and the gate never fires), 4K/64K dormant at ±0.5%.
8. **Large-binary ST encode (100MB-class)**: windows closed; the remaining gap is pure per-byte cost. Fresh interleaved baselines vs the zstd crate's bulk ST on dll100 (2026-09-16): fastest x1.55 (352 vs 545 MiB/s, carrying +1.88% size), fast x1.21 → **x1.16** (RAMPED instantiation, see matchers), balanced x1.70 → **~x1.55** (emit_chain inline, see matchers). **The 2×-sequence-count differential is falsified on the regenerated corpus** (the recorded avg_ml 10.9 shape died with the 2026-09-15 regeneration; `seqstats --ref-frame`): nseq ours/libzstd = 4.87/5.00M (l1), 5.51/6.01M (l3), 3.39/4.76M (l9 — 29% fewer at avg_ml 26.4) — no sequence-count lever exists. The emit/entropy side is per-byte at parity or ahead (fastest ~27% of cycles ours vs ~34% C; fast 3.03G vs 3.45G Ir, we win); the differential is matcher-side instructions: fastest 16.8G vs 12.2G Ir total (IPC 2.66 vs 2.93, 1.52× branches), scan 1.38× C's with insert_covered (dense short-match interior fill, C pays zero) at 8.1% of cycles; fast's whole gap is the dfast scan body at 1.59× C's. Balanced carries json's closed model on dll too (L1-miss samples track cycles in the chain scan, 65.4%/64.9%) — the walk is latency-bound and only the emit side converts. ~~Left: the fastest tier's insert_covered policy lever~~ closed (2026-09-16, density-gated stride-2 fill — see matchers): the dense fill buys only +0.46% dll100 size over C's dual anchors, and stride-2 keeps its phase coverage at +0.042% cost (the pair-pipelined scan spans both stride parities), so blocks following a ≥4 lit-B/seq, ≥64-seq parse now fill stride-2 — dll100 fastest 354→358 MiB/s interleaved (+1.1-1.7%) at **−0.043% size** (48,825,383→48,804,295), corpus byte-identical (full-ladder dump + all emitframe modes), small-band dormant Ir +0.2-0.5% (the gate branch lives inside the outlined `insert_covered`). The gated dual-anchor extreme (+3.7% wall at +0.54% dll size) is measured and rejected ([negative](negative/matchers.md)). The fast/dfast per-position scan residue: the const-generic log instantiation landed (2026-09-16, dfast scan 1.42×→1.36× C's Ir on a dll 4MiB slice, dll100/json fast +3.4/+3.6% interleaved; see matchers); the remaining ~14 Ir/miss-iter over C are register-budget-bound (rep probe bounds, hash5 mask form, per-position reach, spill shuffles) and the C-parity restructures of each are falsified ([negative](negative/matchers.md)) — no identified lever short of shrinking the emit live set. The fastest tier sits at ~x1.44-1.52 (json x1.44 after the dense-mode scan hosting, item 3; dll100 pays the FSE arithmetic-form round's +1.6-2.2% on top of the stride-2 fill — its parse never gates dense). Corpus: `bench/gen_big.sh` (ELF concat; current shape facts in matchers).

## P2 · Ratio and features

9. **Encoder ratio residues** (2026-09-16, third update): the entropy-stage serialization class is CLOSED for json and text opt/ultra (weight-FSE normalization aligned with `FSE_normalizeCount` + libzstd's treeless exact-cost rule + the <256 single-stream rule — see [encoding](perf/encoding.md); 120-cell sweep −120 KB total, 0 worse). ~~(a) literal/seq block splitting~~ **landed (2026-09-16)**: libzstd's post-parse splitter ported (`blocks/split.rs` — recursive bisection on real-build size estimates, gate `Strategy::Opt` && window ≥ 2^17, btlazy excluded like C; see [encoding](perf/encoding.md) for the estimator, the rolling repeat-carry across the cut, and the restart-instead-of-rep-rewrite deviation). json.opt bulk-st −0.060% (the whole recorded gap — now 12 B ahead of the ref), json.opt mt/stream +0.07%, json.ultra −1,167 B st; text opt/ultra −4/−10 B (its blocks rarely split — ref splits 2-5 blocks there); full-ladder dump byte-identical outside json/text l17/l19, sweep worst cell unchanged (text.best −0.23%). Open from it: matrix opt/ultra walls re-measure (the estimation tax), and the ref actually carries a SECOND, dev-branch pre-splitter (`zstd_preSplit.c`, active at lazy2/btlazy2) — it is what splits the zstd-13 ref's blocks (and loses ratio on text: 86,951 vs 86,792 B off), so (b)'s old "plus block splitting as in (a)" premise was wrong; the pre-splitter is todo 13's preSplit item, not this port. (b) **text.best −0.15..−0.23%**: the first 512 KiB head parse leaves +1,149 raw literals vs zstd-13 (the parse itself is not at parity at this level, unlike opt/ultra), minus our better zero-tail (4-byte RLE blocks vs their 8-9 B compressed ones). (c) skewed opt/ultra −0.04..−0.07% and json.fast mt −0.17..−0.19% (near-ties, low priority); json.opt bulk-mt 0.18%-denser-than-st anomaly unexplained (favorable direction, low priority). **text.Opt/Ultra closed (2026-09-16, raw literals size-format ladder — see [encoding](perf/encoding.md))**: the residue was never parser-side — `seqstats --ref-frame` showed the parse at parity (12 sequences of 27k differ, entropy bound ours 12 B better) and a block-aligned payload diff put +2 B on every near-empty literals block (`0c 00 00` vs libzstd's `00`: `raw_literals` hardcoded the 20-bit size format); text opt/ultra st 82,092→81,832 / 81,333→81,073 (ahead of the zstd CLI's 81,844/81,079; −0.011/−0.019% vs the crate ref), mt cells +0.07-0.08% where the per-job fixed cost (item 6) owns the gap, no cell worse in the 120-cell sweep. **text.Balanced is closed (2026-09-15, cold-start DUBT head — see [matchers](perf/matchers.md))**: the deficit was localized by `seqstats --ref-frame` to the first cold-start tile (we left 32,045 literals in the first 768K vs zstd-9's 24,192; ~0.6pt of it miss-step skip density, falsified; ~2.1pt the chain's newest-first selection). The head parses row 9's first 1.125 MiB through the btlazy2 driver and hands off to the chain with the region dense-indexed: text −2.49→+2.24% st / −2.73→+8.8 mt on the regenerated corpus, json/skewed byte-identical (alphabet gate), dll100 −0.07%. Whole-row DUBT-balanced is falsified (json density is selection-class — negative.md). Costs refreshed (2026-09-17, the probe-side cuts — see [matchers](perf/matchers.md)): the head parses once per frame (its in-probe copies are gone) and a shrunk frame never runs it; text speed 867 st / 701 mt8 MiB/s, mt job zero pays only the head+handoff (~15 ms). A cheap cold-start discriminator beyond the alphabet gate is still open — the head runs on any ≥48-symbol wide-alphabet start, including random (harmless: those blocks gate raw) and dll (−0.07% size).
10. **Dictionary trainer — toolchain complete, encoder gap decomposed (2026-09-16)**: content quality closed (2026-09-14, fastCover port — see [pitfalls](pitfalls/encoding.md) for the sorted-concatenation lesson; holdout parity with libzstd's trained content on the systemd fixture: 6,030 vs 6,085 B at 16 KiB, referee libzstd -9). **(a) landed**: the `ZDICT_finalizeDictionary` port (`dict/finalize.rs`, `train --formatted`) — first-block parses against the content as raw history feed lit/ll/ml/of histograms, normalized tables + huffman description + {1,4,8} reps serialize in libzstd's layout; interop verified both directions through the zstd CLI (54/54 files × compress/decode cross), and libzstd compresses with our finalized dict exactly as well as with its own (5,256 vs 5,258 B on the 54-file systemd holdout at -9). **(b) decomposed and the entropy component landed**: the old +17% end-to-end gap splits (same-encoder cross-fed dicts) into trainer content (0 — ours slightly ahead), entropy-table handling (was net-NEGATIVE for our encoder: the seeded tables lost to the tuned between-block heuristics — treeless `diff>5`, predefined thresholds preempting repeat, no single-stream form; now the dict-seeded streams select on measured bit costs per libzstd's lazy+ path, see [pitfalls](pitfalls/encoding.md)) and a residual small-band parse gap. Fixture: zstdx -9 with our own dict 5,917→5,413 B vs zstd's 5,256 (+3.0%, was +11.6% on this fixture); no-dict output byte-identical (full-ladder dump). The r5-entropy merge supersedes the dict-gated treeless/single-stream machinery with the ungated exact-cost rule and serializes the formatted dict's huffman header through the libzstd-exact weight path (smaller descriptions free content budget inside the size cap); on a regenerated 54-file systemd fixture the merge measured 8,290→8,280 B (each build training its own dict, zstd-CLI cross-decode 54/54) — the original fixture was not kept, the recorded numbers stay on the old one. Remaining: the +3% is the item 7 small-payload band over dictionary history (raw-content dicts gap +7.8% the same way — parse behavior at 1-3 KiB payloads with a 16 KiB history window, concentrated on the largest holdout files), a parse-side lever, not an entropy one.
11. **LDM — landed for row 9 (2026-09-15, `encoding/ldm.rs`; see [matchers](perf/matchers.md))**: gear-hash candidate source for offsets beyond the chain reach; row 9 ships window W26 / reach W22, dll100 −9.75% (−11.4% unlatched) at ~5% speed, corpus cells untouched (size gate + quiet latch + canary revival — the always-on form taxed json/text/skewed −15..−43% for nothing, all variants in [negative](negative.md)). **Mid-size extension landed (2026-09-15)**: the frame-continuous paths arm at a clamped window ≥ 32 MiB (`LdmArming`, see [matchers](perf/matchers.md)) — dll32 bulk-st 5,462,510 → 4,384,966 (−19.7%, ≡ the always-on figure, +6-8% speed); corpus 120 cells byte-identical, dll100 st/mt byte-identical, mt stays at the 64 MiB bar (per-job strip fill would tax far-less shapes in full). W24 arming falsified (dll16 −0.55%, its 4-16 MiB class is too sparse). **The cheap split pass landed (2026-09-15)**: the window fingerprint is an 8-lane multiply-xor fold replacing the per-split XXH64, the gear chain runs re-associated 4-byte groups (one dependent add per group, asm-pinned), and the bucket checksum scan is AVX-512 — dll100 balanced −3.9% instructions, solo 90.5→93.5 MiB/s (median of 8 interleaved rounds), latched 21,186,488→21,180,504, unlatched 20,806,075→20,797,673, corpus byte-identical; the fold-derived fingerprint variant that regressed the unlatched size is falsified in [negative](negative/matchers.md). **opt/btlazy2 integration landed (2026-09-15, see [matchers](perf/matchers.md))**: rows 16-19 take C's optLdm (window W26, tree domain via chain_reach, one-active-span cursor; dll100 ultra −10.5%, 9.8% denser than zstd-19); rows 13-15 take C's lazy-family gap-parse (candidates emitted wholesale, lazy loop bounded to the literal gaps + limitTableUpdate; dll32 st −20.1% at 4.2→52 MiB/s, dll100 st −13.6% at parity with `zstd -13 --long=26`) — the depth-0 price-competition form is falsified in [negative](negative.md). Open: unpledged-stream gating semantics, dll32-mt (needs a far-class-preserving job model, see matchers).
12. **Streaming MT residue** (2026-09-16: the job-queue rewrite landed — see [mt-stream](perf/mt-stream.md); the burst barrier is gone, text fastest/fast stream-mt8 +11%, json.fast +5%, json.fastest flat at its encode-span floor, output byte-identical, and the historical sporadic hang is root-caused as a wait-snapshot race and fixed; 2026-09-17: the reach probe donates into the core — see item 5 and [mt-stream](perf/mt-stream.md); 2026-09-17: the read-pump round landed — the pump is single-copy (`pump_direct`, init-tracking direct reads), the buffer doubles at every recycle point so the steady state never wraps/quiesce-waits, worker states pool globally, and `finish` posts the tail re-slice before draining: interleaved solo A/B text.fastest +42%/text.fast +38%/json.fastest +30%/json.fast +12%, matrix json.fastest 3405 = 91% of own bulk ceiling, output byte-identical everywhere — attribution and the falsified chunk-resize premise in [mt-stream](perf/mt-stream.md); 2026-09-17: the worker threads pool across encoders — spawn+join (~130 us per 8-thread round, clone + guard-page dominated) became a ~10 us slot handoff, with join-equivalent drop waits, panic retirement, and 5 s idle recycling; short streams gain most (4-8 MiB fast-tier cells +6-17%, 32 MiB +1-4%, slow tiers flat), output byte-identical — see [mt-stream](perf/mt-stream.md)). Remaining floors: json.fast's two-wave epoch gate (the last epoch can only post at `finish` — incremental posting would change the bytes; ramp read unhidden ~1.5 ms), the pump's last copy is the Read-trait floor for an opaque source, the matrix interleaving costs the memcpy-bound cells ~20% vs controlled solo A/B on this box (context tax, both builds), the row-9 finish-tail strip prefills are closed (2026-09-17: the shared prefix fill, item 5). The per-job NT table-clear fixed cost behind zeros.Balanced 0.8× stays (low priority). ~~zeros.balanced/best regressed 2.9×/5.1× vs 09-14~~ **closed (2026-09-17, attribution overturned the covered-fill suspect)**: the regressions were three per-frame fixed costs landing on RLE-class frames whose encode is O(1) per block — the DUBT rows' eager per-reset table clear (48 MiB, `dubt_stale` now defers it to the first searching block), the reach probe's Keep/Shrink alternation re-zeroing same-sized chain tables through `apply_level`'s whole-params realloc key (now keyed on table lengths), and the LDM alphabet gate dropping the pooled `LdmState` every frame (2 MiB churn; the gate now parks it, and `restart`'s table clear is deferred to the first fill/generate). zeros matrix enc-st: balanced 16,080→41,768 (09-14: 46,548; the residue is the probe's by-design 2×2 MiB head parses), best 8,515→42,918 (09-14: 43,534), opt 30,874→41,208, ultra 29,958→39,633. Output byte-identical everywhere (full-ladder dump, 120-cell sweep); see [matchers](perf/matchers.md). **P0 closed (2026-09-17, pre-existing on ee083bc8 — not introduced by r8-strip)**: the job-start seed/LDM probes compared candidates against a stale scan index after the rep probe's one-byte pos-advance — an offset of exactly one resolved onto the position itself and the store gate priced the zero offset with `ilog2(0)`, panicking the worker. Two amplifier defects turned the panic into the observed corruption: the stream's drain assembled the aborted jobs empty and shipped the frame (unpledged row-9 stream-mt past 32 MiB: decoded short by whole jobs, first wrong byte at block starts; w2/w4/w8, fresh-state rep[0]=1 matching the period-1 run — w1 and the opt rows were immune), and the bulk pool's assembly deadlocked on unclaimed slots (the futex hang). Fixed: strictly-older probe guards (byte-neutral — full-ladder dump and emitframe stream-mt8/4/2 + bulk-mt8 byte-identical vs ext), poison surfaced at drain, bulk assembly resumes the panic; `strip_tail_period_run_roundtrips` reproduces. Mechanism row in [negative](negative/mt-stream.md).
13. **Long-term**: superblock (small-data ratio), preSplit content-adaptive chunking, AVX2/SSE2 intermediate SIMD tiers (the nested `#[target_feature]` + non-always `#[inline]` pattern is verified; the risk is codegen interference between the N instantiations), C FFI + zstd CLI argument-surface alignment.
