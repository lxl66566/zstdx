# Todo list

> Only remaining gaps vs libzstd and the directions that could close them. Landed work lives in [perf/](perf/) and [completed](completed.md); falsified directions are in [negative.md](negative.md) — do not resurrect the falsified *mechanism*; changing its premise (algorithm class, window regime, corpus shape) reopens the goal.
>
> Coverage check (2026-09-16, fresh matrix at `dedd267d` — [snapshot](bench/snapshot.md)): every deficit ≥1.2× maps to at least one direction below. Honest exceptions: json.fastest ST x1.44 (item 3's angles are spent; realistic ceiling ~x1.3-1.4), random fastest/fast (x0.84-0.90, minor).

## P0 · Structural

1. **MT decode — piece-parallel stage B landed (2026-09-16, `decoding/mt_pieces.rs`); residue open.** Ramp frames (encoder gate `ZSTDX_MT_RAMP_BYTES`) decode piece-parallel with measured engagement — sentinel-history grid validation fused into staging (r9-p1), json.lvl3 mt8 3.2× serial-B / 3.5× own ST at +0.00% size, dll32 2.6-2.9×; mechanism in [mt-stream](perf/mt-stream.md), falsified variants (hybrid overlap, restart-grid discovery, caller-side validation) in [negative](negative/mt-stream.md). Post-landing walls (json mt8: verdict 2.6 + execute 3.1-3.4 ms) are memory-bound floors — no wall lever short of overlapping execute with the verdict, a falsified class. Open: the `MIN_MT_INPUT` compressed-size floor keeps high-ratio shapes (text 32 MiB → ~100 KB) off MT decode entirely — a floor rethink is its own lever; per-job content-adaptive D (a fixed D must stay under the shape's dominant match period); unpledged streams (the growing grid's tail split depends on the encoder's worker count).

## P1 · Decoding speed

2. **Streaming decode core residue — x1.27-1.36 json, x1.09-1.31 skewed, x1.04-1.23 text** (09-14 pass, reproduced 09-16; the streaming column is the honest core comparison — our own stream-vs-bulk overhead is ≤2%). The fused sequence loop is proven throughput-bound, not latency-bound (IPC 3.45, flat cycle profile), and every structural lever is falsified (ring pipeline, SIMD copy scheduling — [negative](negative/decoding.md)); attribution in [decoding](perf/decoding.md). What remains is decode-half uop attrition vs libzstd's hand-tuned loop (entry unpacking 6+ uops/sequence, shlx/shrx chains) — uop cuts convert ~1:1 into wall on a throughput-bound loop, but the loop is one register-allocation domain and every source change re-rolls it (±600-800K Ir on json), so each further cut is a lottery against a ~10 Ir/seq spill set. No structural lever is left.

## P1 · Encoding speed

3. **Fastest — json x1.44** (was 1.52; the dense-mode hosting landed 09-16 — json.fastest −3.3..−3.9% size in all four modes at 559→590 MiB/s, every other cell byte-identical, [matchers](perf/matchers.md)). The steady scan loop is branch/latency-bound (IPC ≈ 3, ~4 cyc/iter of branch-mispredict cost); the mispredict inventory is collected — the writer headroom branch fell to the unconditional flush, the rep OR-fold and insert_covered's parity guard are fixed, and the remaining budget is spread across inherent branches (extend_match exits, hash-match compares — libzstd pays the same class): no single lever left. Miss-run stepping and batched probing are falsified as unconditional levers, and the price bar cannot live in the plain loop body ([negative](negative/matchers.md)) — new levers must ship as dense-mode instantiations that keep the plain body byte-for-byte (the hosting class that works). Realistic ceiling ~x1.3-1.4; json.fastest.mt8's gap is this per-core deficit scaled through workers.
4. **Fast — json x1.09.** The per-sequence encode path is near-tight; the measured-out remains: the concat's variable shifts + the writer's shift/OR tail (the 09-16 cursor-loop form cut json −0.35/−0.42% Ir byte-identically but the loop is latency-bound — the two-level row-pointer loads plus the serial accumulator chain set its floor, and `nb` is per-entry (double-state split), so the row tables cannot carry the widths), the three-channel SIMD histogram in choose_tables_fast (skipped: RMW-bound at 3 counter updates/seq, SIMD only reaches the 64-entry lane merge — a ~0.02% wall ceiling), and the dfast matcher itself (the dominant share of json.fast cycles). Position-u64 carry and seqStore-cursor emit are falsified ([negative](negative/matchers.md)).
5. **Balanced — closed: ahead of zstd-9 on both axes** (x0.76-0.86 carrying +21% density; the shape-adaptive reach probe, its tax dismantling, the stream-MT donation and the finish-tail shared prefix fill all landed — [matchers](perf/matchers.md), [mt-stream](perf/mt-stream.md)).
6. **Best/Opt/Ultra — the json per-node residue is the open core.** json.best x1.39-1.45, json opt/ultra x1.30-1.33: instrumented events run at parity with C while instructions run +13% on the same work (per-node codegen — register pressure, 64-bit arithmetic; two restructurings falsified), and event volume is 1.2-2.1× by construction (the denser parse searches 21% more positions — cutting events cuts the +2.5% density itself). No identified safe lever; ~1.2-1.3× is this shape's ceiling short of an event-side rethink. skewed.best x2.15 is the memory-latency tree walk at the halved (u32) footprint — ceiling ~2.16-2.22. Everything else landed: the DUBT port, u32 entries, the fill-lag clamp, the tree-fill bounds, u32 tree entries, optLdm ([matchers](perf/matchers.md), [mt-stream](perf/mt-stream.md)).
7. **Small/mid payloads (4K-1M)**: the open speed item is the fastest tier's 64K-1M cells (x~1.37-1.65 json/text) — the same steady-scan deficit as item 3 (post-OR-fold branch inventory flat, so the ~x1.3-1.4 ceiling applies) plus the entropy stage's per-block share. Below 64K the cells are fixed-cost-bound; the pooling, arithmetic-FSE, lane-zeroing and package-merge-stack rounds cut most of it, and the per-block Huffman build itself is now closed below C (2026-09-18: the two-queue `HUF_buildCTable` port replaced package-merge — build self 26.7K→5.9K Ir/call vs libzstd's 10.5K, json-4K x1.394→1.224 / text-4K →1.182 Ir, wall json/text-4K −7.8/−20.1%, sweep +8.67% unchanged, [encoding](perf/encoding.md)). Fresh callgrind residue on a json-4K call (191.6K Ir vs C's 156.5K): the FSE builds (`build_table_from_probabilities_into` 13.3K/call over 4 invocations vs C's `FSE_buildCTable_wksp` 8.1K — the next build-side attrition item), `build_from_weights_slice` ~3.9K/call (codes+packed+aligned materialization — three parallel forms where C fills one CTable), then the scan/emit items of 3/8. The dense-mode scan reaches the band from 256 KiB up (single-block payloads stay plain). The dictionary-history residue is item 10.
8. **Large-binary ST encode (100MB-class)**: fastest x~1.44-1.55 (dll100 pays the FSE arithmetic round's +1.6-2.2% on top of the insert_covered store floor; the density-gated stride-2 fill landed at −0.043% size), fast x1.16 (const-generic log instantiation landed; the emit-live-set pass landed 2026-09-18 — the two match tables fused into one allocation so the dfast scan runs one base register with displacement-addressed small-table slots: dll4 fast scan −6.0% Ir to 1.350x C, whole encode 1.099x C, dll100 fast wall parity-to-+1% — the removed spill reloads were L1 hits under the shape's 1.72 L1-misses/B floor; text.fast +2.2%, skewed.fast +3.8%; the const-hash-log instantiation also landed scoped to the fastest tier's dense body, json4 −3.4% scan Ir), balanced ~x1.55 (the walk is latency-bound, only the emit side converts). The 2×-sequence-count differential is falsified on the regenerated corpus (nseq ours/libzstd 4.87/5.00M l1, 5.51/6.01M l3, 3.39/4.76M l9); emit/entropy is per-byte at parity or ahead (fast 3.03G vs 3.45G Ir), so the differential is matcher-side instruction volume: fastest 16.8G vs 12.2G Ir (1.52× branches, scan 1.38× C's), fast's whole gap is the dfast scan body at 1.59× C's — the remaining excess over C is register-budget-bound and the C-parity restructures are falsified ([negative](negative/matchers.md)); the emit-live-set shrink itself is now spent — the seqStore-cursor form was falsified earlier, the plain-body const-log re-roll lost while the dense body's won (scoped landing), and after the fused-table cut no further removable live value is identified in either scan loop's set (the dfast base still reloads twice per miss iteration, but that is famine, not a removable value). Corpus: `bench/gen_big.sh` (shape facts in [matchers](perf/matchers.md)).

## P2 · Ratio and features


9. **Encoder ratio residues**: (c) closed by decomposition (2026-09-18): json.fast mt — the boundary-loss premise is inverted (our bytes are monotone in boundary count: st 6,336,067 > mt2 6,335,591 > mt4 6,333,597 > mt8 6,331,112 on json l3 32 MiB; job-local entropy restarts win); the −0.17..−0.19%-vs-st residue = our st edging libzstd's st while libzstd's mt recovers 0.23% over its own st — our per-block entropy re-adaptation already banks that win, and the strip-grid/seed levers are analytically dead ([negative](negative/matchers.md)); the honest gap vs libzstd-mt is 6.2-8.7 KB, global; skewed opt/ultra — the parse is **bit-identical** to libzstd's (seqstats: 14,259/14,259 sequences same, zero divergences, identical order-0 bounds) — the whole −0.04..−0.06% (+7-10 KB) is literal-section serialization on the 16-symbol alphabet, ~29 B/block spread uniformly over the 256-block grid, entropy-stage territory already worked by the r10 rounds; json.opt bulk-mt 0.18%-denser-than-st anomaly unexplained (favorable direction, low priority); the splitter's estimation tax re-measured (2026-09-18): 0.53/1.37/0.20/0.47% of json/text opt/ultra cells (gungraun vs a splitter-disabled build), ~15-20% of it cut byte-identically (estimate threading down the bisection + right halves from `whole − left` histograms, [encoding](perf/encoding.md)) — the json opt/ultra wall gap is parse-side, not splitter-side. Closed: entropy-stage serialization, post-parse block splitting, the raw-literals size ladder (text opt/ultra ahead of the zstd CLI), text.Balanced (cold-start DUBT head), text.best (cold-head probe step at libzstd parity: the whole −0.15..−0.23% was the miss-ramp's skipped searches in the first 448 KiB) — [encoding](perf/encoding.md), [matchers](perf/matchers.md). The ref's dev-branch pre-splitter (`zstd_preSplit.c`, active at lazy2/btlazy2 — it loses ratio on text) is item 13's preSplit item, not a gap here.

10. **Dictionary — parse-side fill landed; residues small and entropy-side**: content quality, formatted-dict emission (`ZDICT_finalizeDictionary` port), dict-seeded entropy selection and the dense dictionary fill (dict-grid prefill + libzstd-parity chain walk depth, [encoding](perf/encoding.md), [pitfalls](pitfalls/encoding.md)) all landed. On the rebuilt 54-file systemd holdout at -9 the formatted-dict cells sit at parity or ahead of the zstd CLI (-0.0% own dict / -2.0% its dict; was +3.7/+1.2), raw-content controls +1.9-2.1% (was +5.5-5.9), -1 raw +0.3-0.5%. Remaining (2026-09-18): the -1 formatted entropy residue is closed — the literals gates' flat description term was repriced for treeless candidates ([encoding](perf/encoding.md)); fast-row cells now -3 formatted −1.2% (ahead), -1 +0.6/+1.2%, -5 +0.3/−0.8%, -9 −2.4/−3.9%. What is left is parse-side: -1/-2 +0.6-1.2%, -4 +5.1-6.8% (CLI fast/dfast leaves 2/3 of the literals our rows leave on dict history; our sequence sections are −310 B ahead), -5 greedy +3.3-3.5% (no single mechanism isolated; x16 walk depth buys 19 B and saturates). Same-class leftover behind the byte-identity contract: the no-dict fast rows' between-block treeless candidates still pay the description-charged gates (libzstd's `preferRepeat` accepts any live table blindly below 1 KiB) — changing it re-baselines no-dict bytes, so it needs its own round.
11. **LDM — landed in full**: row 9 (W26/reach W22, dll100 −9.75%), ≥32 MiB clamped windows on frame-continuous paths, optLdm rows 16-19, lazy-family gap-parse rows 13-15, dll32-mt via prefix-strip jobs + the shared prefix fill — [matchers](perf/matchers.md). Closed (2026-09-18): the unpledged stream keeps the unclamped W26 semantics — it dominates bulk-mt at every dll scale (dll16 2,929,187 vs 2,937,032, dll32 4,390,251 vs 4,390,255, dll100 20,869,254 vs 23,459,589) and the quoted 1,453 B gap was a dev-build artifact unreproducible on landed builds; the actual gap was the pledged path missing the capture entirely (dll32 5,460,884, chain-only) — fixed by re-gridding pledged mid-size keep-class frames onto the bulk capture at the reach verdict ([matchers](perf/matchers.md)).
12. **Streaming MT residue**: the job-queue rewrite, single-copy read pump, global state/thread pools and the reach-probe donation all landed ([mt-stream](perf/mt-stream.md)); the 09-17 P0 stale-index corruption class is fixed with regression coverage (`strip_tail_period_run_roundtrips`). Remaining floors: json.fast's two-wave epoch gate (the last epoch can only post at `finish` — incremental posting would change the bytes, vetoed on the byte-identity contract), the pump's last copy is the Read-trait floor for an opaque source, the matrix interleaving costs memcpy-bound cells ~20% vs solo A/B on this box (context tax, both builds), and the per-job NT table-clear fixed cost behind zeros.Balanced 0.8× (low priority).
13. **Long-term**: superblock (small-data ratio), preSplit content-adaptive chunking, AVX2/SSE2 intermediate SIMD tiers (the nested `#[target_feature]` + non-always `#[inline]` pattern is verified; the risk is codegen interference between the N instantiations), C FFI + zstd CLI argument-surface alignment.
