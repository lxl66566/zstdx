# Performance Optimization · Encoding (excl. matchers)

> Matcher and level strategy: see [Matchers and Compression Levels](matchers.md); multithreading and streaming pipeline: see [Multithreading and Streaming](mt-stream.md).

## Entry and state management

- **zero-copy slice entry** `compress_slice_to_vec`: the match window directly borrows the input (raw-pointer window + unsafe Send), eliminating the read pass, window compaction, and per-call window allocation; output upfront-reserved, blocks written straight into the final output. text +40%, zeros +60%. `51b019e`
- **thread_local state pool** (hash tables/default FSE tables/BlockScratch, take()-style reuse across calls, nesting-safe): 1KiB payloads 3-7×, 4KiB +30-80%. `cb02c32`
- **emit path rework**: entropy tables take/restore by value instead of deep-cloning 3×FSETable+Huffman per block (the original several hundred allocs per block was the biggest black hole, ~30%); `start_matching_into` buffer direct-write replaced the callback closure (the closure captured 2×Vec + matcher state → register spills, one store sampled at 40%); matcher window reads unchecked. Itemized +2%/+2%/+11%, cumulative json +24%, text +39%. `68ae651` Lesson: **unchecked-read gains (+11%) far outweigh structural rework (+2%×2); bounds checks on byte-wise paths are the prime target**.
- block contents written in place into the frame output (reserve the 3-byte block header first, backfill after compression) + literals/sequence/precomputed-code buffers moved into the BlockScratch pool: random -6.8% instructions. `95b2006`
- zero-sequence blocks skip literal staging (matcher skips the whole-block copy, encoder reads the window directly): random 64K-1M +13%. `15d127d`
- **arithmetic tANS transition tables (2026-09-16, the pooling round's recorded build-walk residue)**: the encoder's FSE tables materialized a full `(max_symbol+1) × table_size` packed-entry matrix — ~54K u32 stores per fresh-table block (LL 36×512 + ML 53×512 + OF 32×256) plus the 3×256 row-pointer setup in `encode_sequences`. The tables now carry libzstd's `FSE_CTable` arithmetic form in ONE pooled buffer (≤10 KB total vs 218 KB): `tt[256]` u64 entries packing `(delta_nb_bits, delta_find_state)` and a `table_size`-wide u16 `state_table` of biased next states (`table_size + index`, ascending-position symbol-major rows). One step is `nb = (state + dnb) >> 16`, `emit = state & mask(nb)` (the run baseline is a multiple of the run width, so the biased state's low bits ARE the old precomputed diff), `next = state_table[(state >> nb) + dfs]`; the build writes O(table_size) entries and the row-pointer machinery disappears with the rows. Equivalence with the old run walk is structural (same spread, same cumul rows, doubles-first-in-index-order) and pinned by the rewritten `soa_matches_reference` differential (400 random tables, every (symbol, state) step vs the reference runs) plus `check_tables`; output byte-identical everywhere (30-file full-ladder dump, 120-cell ratio sweep, all modes). Effects: gungraun small json-4K −7.0% Ir / text-4K −5.2% / 16K −2.2..−2.4% / 64K −1.2..−2.0%; the 1 MiB encode slices put the hot-loop price at ~+25 Ir/seq on sequence-dense json (fastest/fast +1.8/+3.5% Ir) and −0.6..−1.6% on text — and the wall follows the build side, not the Ir: small band json-4K x1.79→1.62 (−9.4%), text-4K −6.1%, the whole 4K-1M band −1.6..−9.3% (json-64K fast crosses to x0.98); matrix json.fastest −2.0% (581→594 MiB/s), json.fast −3.9% (445→469), text fastest/fast −2.4/−3.3%; dll100 fastest +1.6-2.2% (367→373-375), fast +1-2%, balanced +1-5%. Lesson: the materialized form's per-sequence packed entry saved ALU but kept a 218 KB L2-resident working set behind every block — the L1-resident arithmetic tables win on wall at every measured shape even where Ir rises.

- **entropy-build scratch pooling (2026-09-16)**: the per-block FSE/huffman table builds allocated and zeroed their buffers on every build — on a json-4K fastest call that was ~15% of Ir in memsets alone (the `vec![0u32; probs.len()*table_size]` transition tables ×3, package-merge level lists, huffman `codes`, wire weights) plus the malloc/free churn (~3%). `FSETable`/`HuffmanTable` now return their transition/codes buffers to pools held in `BlockScratch` (`FseBuildScratch`/`HuffScratch`), and the builds write into recycled buffers: a buffer's `len` is its initialized prefix, so growth (the only zeroing left) happens once per size, not once per table — live rows are fully overwritten by the build walk and dead rows stay stale-but-unread (wire codes never exceed the histogram's `max_symbol`; `bulk::tests::pooled_calls_byte_identical` pins the repeated-call outputs). Recycling sites: table adoption in `compress_fastest`, the raw-block fallback, `reset_slice_state`, and the huffman weight-table temp inside `write_table_with`. Measured on the new gungraun small-payload bench (`zstdx-gungraun/benches/small.rs`, deterministic per-call Ir): json-4K −13.6% Ir, text-4K −11.1%, json/text-16K −7..8%, −7% at 64K. Wall (interleaved A/B): json-4K x1.96→1.83, text-4K x1.91→1.87 (abs +8%), json/text 8K/16K −4..6%, skewed-4K x0.90→0.80, json/text-1K x2.30/2.48→1.95/2.17; 64K-1M and the 32MiB matrix stay flat (json) to ahead (text fastest/fast +11-20%: per-block builds amortize there too). Byte-identical output: 5 shapes × levels 1/3/9/16/19 emit-diffed against the pre-change build. Residue: the 12 KB lane-zeroing in `choose_tables_fast` (the build walks and the row-pointer setup are gone with the arithmetic-form round above).
- **SoA flattened tables (probs/start/transitions in contiguous memory) replace 256×Vec + double sorting.** `81afece`
- **CTable build restructured to libzstd's shape (2026-09-18, small-payload build-side round 4)**: the recorded residue was `build_table_from_probabilities_into` at 13.3K Ir/call over the 4 builds of a json-4K encode vs C's `FSE_buildCTable_wksp` 8.1K — attrition, not algorithm: a full `owner` 255-fill per table, a separate -1 placement round, a per-occurrence spread walk paying a skip check per step, and a cumul + cursor u32 pair (2 KB stack traffic). Now C-shaped: the -1 placement rides the row-start pass (`start[s]` doubles as the state-table walk's cursor and transforms into the biased start state inside the tt fill, whose running total re-derives each row start — cumul and its copy are gone); builds without -1 weights (the small-payload norm: weight tables normalize with `useLowProbCount=0`, sequence tables below 2048 symbols) take C's fast path — symbol runs staged contiguously at eight bytes per unaligned u64 store into a pooled high-water spread buffer, then a fixed-step two-way unrolled scatter of constant trip count (`step` is odd for every table_log ≥ 4, so the walk covers each position exactly once; `spread[k]` is the k-th symbol-major occurrence, making the placement provably identical to the classic walk); `owner`/`spread` recycle without any per-build clearing (every slot up to `table_size` is written; the sum invariant is debug-asserted in the row-start pass). `soa_matches_reference` grew to 600 cases with every third banning -1 weights, pinning the fast path against the classic-walk reference. gungraun small: json-4K −1.34% Ir (x1.224→1.208 vs libzstd), text-4K −1.06%, 16K −0.4/−0.5%; wall-neutral on its own — the cut is loop-overhead Ir, not memory traffic (the lesson the round's huff0 half below completes). Full-ladder dump byte-identical.
- **FSETable thinned to one pooled buffer (2026-09-18, small-payload build-side round 6)**: the struct carried `probs: [i32; 256]` + `start: [u16; 256]` inline (~1.5 KB), so EVERY hop memcpys it — the build return, the `FseTableMode` enum wraps, `FSEEncoder::new`, `compress_fastest`'s `old_tables` array (3×`Option<FSETable>` = one 4.6 KB memcpy per block), `replace_previous` ×3, the `BlockOutcome::Encoded(BlockTables)` return, `reset_slice_state`'s takes — callgrind put the whole memcpy class at 8.9K Ir of a 184.8K json-4K call (4.8%), where C pays ~0 (its literals staging is inline wildcopy and its CTables are workspace pointers). The fat arrays moved into the existing pooled `tab` buffer behind fixed offsets (`[tt: u64×256][probs: i32×256][start: u16×256][state_table: u16×table_size]`, +2.5 KB per pooled table), the struct drops to 32 B, and all those hops become register moves; the `probs_full` staging copy inside the build disappears the same way. Recycled tails stay stale-but-unread under a new `nsym` bound: `write_table`'s serialization and zero-run scans stop at `nsym`, and `symbol_bit_cost` returns `None` past it (the repeat-coverage check that reads a dead symbol — the one reader that could observe stale bytes; caught by `pooled_calls_byte_identical` before the bound went in). Byte-identical (full-ladder dump); gungraun small: json-4K −4.45% Ir (x1.181→1.128 vs libzstd), text-4K −3.47% (1.148→1.109), rand-4K −4.9%, 16K −1.0..1.2%, 64K −0.3..0.4%; the memcpy class itself 2.28M→0.29M Ir on the 256-call json-4K bench. Gotcha: the region offsets must be derived in ONE unit — the first cut mixed u16-index and byte arithmetic (`START_U16_OFF` 256 slots low), overlapping start into probs and smearing the serialization; the symptom (write_bits value-overflow on the legacy roundtrip path only) pointed far from the cause.

## FSE

- SoA flattened tables (probs/start/transitions in contiguous memory) replace 256×Vec + double sorting. `81afece`
- per-symbol spread precomputation; flat transition table `(next<<13)|(nb<<9)|baseline` O(1) state transitions + LL/ML code LUT: json 189→245 MB/s. `5619e80`
- 6 write_bits per sequence merged into 2 u64 spellings (transition bits ≤27bit, add-bits ≤51bit, one each): another +4%. `bbde2ea`
- transition bits and add-bits merged into one hot_push (combined ≤56bit safe cap, falls back to double push beyond it). json -0.4% instructions. `2d18813`
- **encode_sequences upfront reserve + raw pointers**: one reserve of `nb_seq*11 + 32` bytes (≤51 add bits on the last sequence, ≤87 per earlier one, plus the 8-byte flush-store overshoot) replaces the per-push capacity probe (a Vec field load + branch per sequence); the seqs stream and the three FSE transition-row tables move to raw pointers — the slice/Vec fields rode the stack through every iteration and were the function's hottest single instructions. Byte-identical output; gungraun json.fastest a further -1.3% (cumulative -4.0% with the emit split), json.fast -0.9%.
- **encode_sequences per-code row-pointer tables**: the loop-invariant half of each row address (`rows + (code << shift)`) is resolved once per block into 256-entry tables of raw pointers; the hot loop reaches a transition entry with one rsp-addressed stack load plus one indexed load through it. The first generation stored u32 row offsets and OR'd the state in, but that kept a third live base register per channel — registers the loop does not have, so all three row bases reloaded from the stack every sequence. Byte-identical output (full-ladder dump gate); gungraun fastest/fast −0.7..−0.8% Ir on json/text, balanced −0.15..−0.19%, skewed.fast −0.18% (skewed's low tiers stay on RLE one-state tables and miss the path). A SeqWord 24→16 B shrink trading the separate `add_nb` load for a packed width byte was falsified: the packing ALU lands in the hotter matcher emit (see negative.md).
- **precomputed transition diffs in FSE entries**: a flat row is indexed by the state itself, so the emitted value `state - baseline` is a build-time constant per row position — entries now pack `(next << 16) | (nb << 12) | diff` with the run offset written at build time, and every consumer (encode_sequences, `FSEEncoder::encode`/`encode_interleaved`) reads the diff straight from the entry. Kills one sub plus its feeding mov per channel per sequence, and the three end-of-loop state copies disappear (entries land in registers that update in place via `shr 16`). Byte-identical output (full-ladder dump gate); gungraun json fastest/fast −1.3%/−1.5% Ir, text −0.9%/−1.2%, skewed.fast −0.3%.
- **table mode selection** (port of the libzstd `selectEncodingType` fast branch): RLE single-code / Predefined threshold / Encoded + normalizeCount M2 normalization + optimalTableLog + last-sequence count decrement. json ratio 5.22→5.37. `1a17db3`
- **repeat mode (mode 3)**: when the previous block's table covers all active codes and the bit-cost estimate (Σ c·log2) ≤ new-table description + entropy lower bound, the whole table is reused with zero description bytes; a Predefined/RLE choice invalidates the remembered table (`PrevTable::{New,Keep,Clear}` three states guard against table-state desync with the decoder). json -2.2% instructions, ratio 5.99→6.00. `7769bf8`
- RLE degenerate single-state FSE table: table_size=1 takes exactly the same path as normal modes, zero special-case branches.
- **fastest-tier mispredict differential study (2026-09-14, LBR `perf record -j any` + `/M` flag on AMD)** — per-branch attribution of json.fastest's ~3.1M misses/pass (~17% of cycles): the single largest mispredicted branch is encode_sequences' bit-writer headroom check `nb+bits ≤ 63` (~18% of misses; the next target, and the branchy counterpart of the already-landed writer work above), then the scan_fast rep OR-fold — which the short-circuit `a == 0 || b == 0` had compiled into *three* unpredictable branches (a-cmp, b-cmp, combine, ~19%), extend_match's u64-compare loop exits (~9%), the two hash-match compares (~9%), and insert_covered's mod-2 unroll parity guard (~4%). Fix with the study: the fold as `(a == 0) | (b == 0)` — one branch, `a`-disambiguation on the taken path. Byte-identical on the full-ladder dump; branch-misses −1%, wall neutral (json −0.5%±noise) — the mispredict inventory shrank, making encode_sequences' headroom branch the clean next cut.
- **unconditional bit-writer flush (`hot_push_raw`, 2026-09-14)**: the headroom branch `bits + nb >= 64` — the study's largest single mispredict (~18% of json.fastest misses) — is gone by adopting libzstd's BIT_flushBits discipline: every push flushes its whole pending bytes (`k = bits/8 ≤ 7`, one unaligned u64 store, `acc >>= 8k`) before OR-ing the payload, so `bits < 8` on every entry and any nb ≤ 56 still fits the accumulator. Traded +0.4-0.6% Ir (the extra stores fire even when the old branch fell through) for the branch elimination: wall json.fastest 541→562 MiB/s (+4%), json.fast 425→447 (+5%), skewed.fastest 2758→2873 (+4%), text +1%, skewed.fast flat; full-ladder dump byte-identical. Lesson: **a ~18%-of-misses branch is worth ~5× its instruction-count price in stores**; the reverse trade (store folding to save <1% Ir — see negative.md's write_bits_64_cold row) never pays.
- **insert_covered peel-then-pairs** (with the study above): the `while p < end` insert loop auto-unrolled mod 2 behind a per-entry parity guard (~4% of json.fastest mispredicts); peeling the odd tail once and inserting in explicit pairs keeps the unroll without the guard. Byte-identical on the full-ladder dump; gungraun Ir json +0.05%/text flat vs the guard form (the two alternatives that *lost*: plain counted `for k in 0..count` stays rolled, +3.3% Ir; a hand-unrolled counted loop with per-iteration `start + k` addressing, +4.3% — see negative.md). Wall neutral.
- **encode_sequences output cursor** (2026-09-16): the per-sequence loop kept `pos` as a byte offset and reloaded the output base pointer from its stack slot every iteration (register pressure — sixteen live values) just to form the flush store's address, and spilled the loop counter on top. The loop now carries ONE output cursor pointer (advanced by each flush's byte count; the exit position is derived once at the end) and terminates on the SeqWord pointer instead of the counter. Byte-identical (full-ladder dump); gungraun fastest/fast json −0.35%/−0.42% Ir, fast text −0.39%; wall neutral on interleaved A/B (x-bands overlap every round — the loop is latency-bound: the two-level row-pointer loads and the serial accumulator chain set its floor, the cut is uop-side). The concat's variable shifts are at their floor for this entry form: `nb` is per-entry, not per-row (a symbol's double-states split nb/nb+1 — `fse_encoder.rs`'s baseline walk), so the per-code row-pointer tables cannot carry the widths to shorten the post-entry extraction chain.
- **NCount serialization pair at C parity (2026-09-18, small-payload round 7)**: todo 7's recorded fresh residue — `FSETable::write_table` + `normalize_count` at ~3.3K + ~2.9K Ir per json-4K encode — is closed. `write_table` paid a `BitWriter::write_bits` call plus an ilog2 width-ladder recompute per symbol; it now ports libzstd's `FSE_writeNCount_generic` shape onto the writer's hot state: `threshold`/`nb_bits` track ilog2(remaining) incrementally, the value/width arithmetic is libzstd's (`max = 2·threshold−1−remaining`, widen at ≥ threshold, one bit less below max), zero runs emit 24-zero chunks as one 0xFFFF pair, and the description accumulates in a register u64 flushed as unaligned 8-byte stores (`hot_state`/`set_hot_state`, one reserve of 4+15·nsym bits). `normalize_count` reslices both slices to the live alphabet once so the per-symbol loop indexes known-length slices bounds-check-free — the zip-enumerate form cost MORE than the checks it removed (zip bookkeeping replaced them; plain indexing over resliced slices is the form that elides). Per json-4K encode: write_table 3.29K→3.01K Ir (C's `FSE_writeNCount_generic` 3.32K), normalize_count 2.86K→2.29K (C 2.22K). gungraun small vs pre-change baseline: json-4K −0.51% Ir (x1.110→1.105), text-4K −0.44% (x1.082→1.077), 16K −0.14/−0.15%, 64K −0.05%, random unchanged; wall direction-positive (json-4K 536→540, text-4K 367→394 MiB/s, Ir is the gate on this box). Full-ladder dump byte-identical, 223 tests pass. The entry-flush bug the tests caught is in [pitfalls](../pitfalls/encoding.md).

## Huffman

- **boundary package-merge optimal length-limited codes** (Larmore-Hirschberg) replacing the initial rank-weight ladder (the old scheme depended on count order, not magnitude — large losses on skewed histograms): all levels benefit, json Ultra 7.33→7.52, text fastest 196.6→206.0. `aa07308`
- `build_from_weights` counting sort: package-merge caps weights at 11, so 12 stack buckets with symbols scattered in ascending order reproduce the comparison sort's weight-ascending/symbol-ascending order exactly (byte-identical output, gate: full-ladder dump); removes the per-block heap Vec and the O(n log n) sort. text-4K small-payload encode +5% wall-clock, gungraun instruction counts drop on every text-shaped cell, no regressions elsewhere.
- `package_merge_lengths` scratch rework: only the previous level list is ever read, so two swapped buffers replace the `Vec<Vec<Ent>>` (was two allocations per level, 11 levels per table); `Ent` weight u64→u32 (12→8 bytes, block literals are capped far below 2^32 so package sums cannot overflow). Byte-identical output; gungraun text fastest -0.7%, text fast -1.2%, skewed fastest -0.5%; cumulative text-4K +8%.
- **package-merge on fixed-capacity array stacks (2026-09-18, small-payload fixed-cost round 2)**: callgrind pinned the per-block huffman build at 52K Ir/call, flat across the 4K-64K band (21.6% of a json-4K fastest call) with ~24K of it Vec machinery — push capacity checks, length bookkeeping, bounds checks on list reads (the vec/raw_vec/ptr/non_null rows inlined into `build_from_counts_into`) — around a ~19K walk plus the 1.9K leaf sort. The seven working lists (leaves, arena, two ping-pong level lists, packages, two ping-pong backtrack id lists) are now fixed-capacity array stacks in `HuffScratch` under structural bounds (leaves ≤ 256 distinct symbols, level lists ≤ 2(n-1), arena ≤ 256 + 11·255) and the three hot loops (package formation, level merge, backtrack) index them unchecked with register-carried lengths; the `core::mem::swap`'d Vec pairs become ping-pong buffer pairs selected by level parity. `package_merge_optimality` already covers every alphabet size 2..=256 with Kraft-exactness; full-ladder dump byte-identical. gungraun small: json-4K −4.7% Ir (cumulative with the lane cut −9.4%, x1.533→1.389 vs libzstd), text-4K −7.0% (cumulative −10.2%), json/text-16K −1.6%/−2.8%, 64K −0.5%/−1.0%; the random cells drift +0.1-0.5% (no huffman build to win; the one-time ~40 KB Box zero-init of the pooled stacks amortized over the bench). Wall interleaved A/B vs libzstd: json-1K x1.897→1.720, text-1K 2.346→2.076, json-4K 1.639→1.553, text-4K 1.837→1.674, 64K −1.5/−4.7%, 1 MiB flat. **Superseded (same day, round 3 below) by the algorithm swap — the stacks are gone with the walk.**
- **two-queue tree build replacing package-merge (2026-09-18, small-payload fixed-cost round 3)**: the walk itself was the residue — a fresh attribution put `build_from_counts_into` at 26.7K Ir/call (merge loop 12K + package formation 5.9K + backtrack 5.6K) against libzstd's whole `HUF_buildCTable_wksp` at 10.5K, a 2.5× algorithm-class gap: package-merge pays max_len levels of 2(n-1)-wide 8-byte-entry merges, C builds an unlimited-depth Huffman tree in O(n) (count bucket-sort scatter + two-queue merge over the sorted leaves with a count barrier at slot 0 and 2^30 markers for uncreated nodes) and only pays a depth-limit pass (`HUF_setMaxHeight` Kraft repayment) when the unlimited tree exceeds 11 bits — where unlimited depth fits, both are optimal, and the repayment's excess over package-merge measures ≤0.1% on every non-adversarial histogram (~2% on a fibonacci-count adversary C pays identically; pinned by `build_lengths_matches_optimal_and_is_kraft_exact`, which keeps package-merge as a cfg(test) optimality reference and checks Kraft-exactness + ≤2% cost over alphabet sizes 2..=256 × descending/fibonacci/random shapes). `HuffScratch` shrinks with the algorithm: the seven package-merge stacks (~38 KB) become one 513-entry `NodeElt` table + a 192-slot rank-position array (~5 KB). Output moves only on fresh json/text tables (dump: +2..+92 B per file, ≤0.002%; random/zeros/skewed byte-identical), all 30 dump files roundtrip bit-exact through the C decoder; the 120-cell sweep stays +8.67% geo-mean with the same pre-existing losing cells (json.fast mt −0.14/−0.12%) and text.best holds its +0.13% lead. gungraun small: json-4K −12.1% Ir (x1.394→1.224 vs libzstd; the build self 26.7K→5.9K/call, 0.57× C's own), text-4K −20.3% (x1.484→1.182 — deeper text tables paid more package-merge), json/text-16K −3.9%/−7.5%, 64K −1.3%/−2.6%, random ±0.1%; the 1 MiB encode bench is flat-to-better everywhere (text fast −2.7%, fastest −1.7%). Wall interleaved A/B vs libzstd: json-4K x1.563→1.442, text-4K 1.716→1.419, json/text-16K 1.416→1.311 / 1.258→1.160, 64K 1.487→1.432 / 1.479→1.390. Lesson: **before attrition-ing a hot loop, diff the algorithm class against the reference implementation — a 2.5× instruction gap in intrinsic-per-block work was structural, not codegen.**
- per-level package sort removal: packages are disjoint adjacent-pair sums of the (weight, node)-sorted previous level, so pkg[k+1] ≥ pkg[k] follows from w[2k+2] ≥ w[2k] and w[2k+3] ≥ w[2k+1], and ties break by strictly increasing arena id — the list is provably already in the sorted order, making the sort a no-op (verified by a 2M-random-vector brute force with a sortedness debug-assert + full-ladder dump byte-identical). The merge levels are now pure O(n) merges; gungraun text fastest/fast −0.34% instructions, skewed fastest −0.19%, text-4K +3% wall-clock.
- **one-form materialization + pooled aligned table (2026-09-18, small-payload build-side round 5)**: the weight→code pass materialized THREE parallel forms per live symbol (`codes: Vec<(u32,u8)>` + `packed` u16 + `aligned` u64) where C fills one CElt — and the struct literal zero-initialized `packed` + `aligned` inline (2.25 KB memset per build, 2.6K Ir — the largest single remaining build-side fixed cost), with the `codes` pool (take/fill/recycle) on top. `codes` is gone: every consumer reads the `nb` nibble straight from `packed` (`estimate_compressed_size`, `can_encode`, `write_weights_into`; the alphabet length moves to an `nsym` field, and entries past either alphabet count zero either way), the pool machinery and the per-build fill disappear with it. `aligned` moves behind a pooled `Box<[u64; 256]>`: dead symbols are never read (the stream encoder only indexes symbols the table covers), so boxes recycle without clearing and every byte stays initialized (clone-safe); only the 512 B `packed` still needs its zeroed dead prefix. The uniform-code detection folds into the bucket loop (one non-empty weight bucket ⇔ every live symbol shares a length) instead of two branches per symbol. `build_from_weights` is allocation-free now, and `HuffmanTable` shrinks ~2.6 KB inline to ~0.5 KB + box, so the per-block adopt/replace moves copy a fifth of the bytes. HuffmanTable-shaped memset residue: 4.1K → 1.6K Ir/call; `build_from_weights_slice` 4.4K → 1.9K. Combined with the FSE round above, gungraun small: json-4K x1.224→1.181 (−3.6% Ir), text-4K 1.182→1.148 (−2.8%), 16K −1.0/−0.9%, random-4K −1.6%; 1 MiB encode 60 benchmarks, 0 regressed. Wall (alternated base/new binaries, ruz medians — the drift-free column): json-4K 461→484 MiB/s (+4.6%), text-4K 376→390 (+3.7%), json-16K +2.7%; the wall converts at MORE than the Ir rate because the removed work was memory-bound (rep-stosb on never-read bytes, 2.6 KB struct moves) — the counterpart lesson to the FSE half: **Ir cuts in the small band only convert to wall where they also cut memory traffic**. Full-ladder dump byte-identical; 120-cell sweep +8.67% unchanged; `pooled_calls_byte_identical` and the rewritten packed-based tests (201) pass.
- scalar 4-symbol batch accumulation: packed u16 `(code<<4)|nb` code table; flush to <8bit first, then branchlessly accumulate 4 symbols into a u64; a single unaligned store flushes whole bytes. skewed +71%, json +4%. **Superseded (2026-09-12)** by the dual-accumulator loop below for the generic path.
- **left-aligned dual-accumulator stream loop (2026-09-12, port of libzstd `HUF_CStream`)**: entries move to a parallel `[u64; 256]` form — code in the top `nb` bits, `nb` in the low nibble — so per symbol one load feeds `c >>= nb; c |= e; b += nb` (the OR of the raw element dirties the four low bits; every following `>>=` discards that dirt before it can reach the pending window, so only each round's last add needs a cleaned operand). The pending window grows downward from the container top; a flush shifts it into the low bytes with one unaligned store and leaves the container untouched. Rounds are 5 symbols per container with two containers alternating (merge = one shift+OR), giving two independent shift/OR chains; with codes ≤ 11 bits the dirt/capacity margins hold at 7 + 4×11 fast / +11 clean = 62 ≤ 64. A BMI2 runtime dispatch (the decode side's pattern) is load-bearing: on the baseline target the legacy `shr r, cl` costs one `mov`+`and` per symbol and short-code corpora regress (skewed.fastest +4.9% total Ir measured without it — recovered to +0.3% with shrx). Byte-identical output (full-ladder dump gate; the differential unit test covers 1..11-bit codes × entry alignments × uniform/non-uniform). callgrind dll32 fast: huff0 stream 88.8M → 50M Ir, at libzstd's own 45.7M — the 2x per-symbol gap closed (14.5→10.7 Ir/symbol on dll codes, ~7.2 on skewed's short codes), dll total −3.7%; gungraun encode: json fastest/fast −3.9%/−1.8%, text −2.6%/−1.1%, balanced json/text −1.1%/−0.4%, best/opt/ultra ≤0.15%, zeros/random +0.1-0.4% (the extra 2KB aligned-array init per table build). Wall within noise on the 32MiB shapes (the loop is a ~5%-of-cycles share there and latency-tolerant); the win is instruction-side and literal-volume-side (dll-shaped corpora, streaming small blocks).
- **AVX-512VBMI 4-bit code packing kernel** (uniform 9..16-symbol alphabets): 64 symbols/batch, `permutex2var_epi8`×2 indexing a 256-entry LUT + two permutes to un-interleave paired nibbles. skewed instructions -48%, throughput +43%. `2be2207`
- RLE literals mode (all literals the same byte → type1 header + 1 content byte). Gotcha: in the 5-bit size format, size_format occupies only 1 bit (see the pitfalls notes).
- **huffman weight-table serialization aligned with libzstd (2026-09-16 — todo 9's remaining entropy-stage residues)**: the per-block weight FSE description was built by a legacy hand-rolled normalization (min-count subtract, integer divisor scaling, an `avoid_0_numbit` redistribution) whose output bore no relation to `FSE_normalizeCount` — block-level desc dumps (a frame parser + a Python `FSE_readNCount`/weights decoder) showed e.g. norm `[16,9,1,1,1,1,1,1,1]` against libzstd's `[17,2,1,1,3,3,2,2,1]` on identical weight histograms, +2-8 B per fresh-table block, streams within 1-2 B. The write path now ports `HUF_compressWeights`/`HUF_writeCTable_wksp` exactly: `optimal_table_log(6, n, maxWeight)` + the shared `normalize_count` port (`useLowProbCount=0`), libzstd's FSE-vs-raw gate (`hSize > 1 && hSize < maxSymbolValue/2`), and the raw form's 128-weight header cap. Two encoder bugs the new tables exposed: (1) `build_table_body`'s encode start state picked the smallest-baseline run, which for probabilities above half the table is a ZERO-bit entry — the bit-driven stream termination of the interleaved encoders then overdecodes past the end (weights streams decoded +2 symbols; C's `FSE_initCState2` picks the lowest-index state, always >=1 bit — sequence tables never reach a wrapping probability so only the weight tables could hit it); (2) the scratch `BitWriter` for the description dropped its unflushed trailing byte. Alongside, the treeless decision replaces the `sum(|dNb|) > 5` heuristic with libzstd's exact comparison (`oldSize <= hSize + newSize || hSize + 12 >= srcSize` via `HUF_estimateCompressedSize`), and single-stream literals now engage below 256 literals (libzstd's `ZSTD_compressLiterals` rule; the jumptable never pays there) instead of below 6; a dictionary-seeded stream additionally keeps the single-stream form below 1 KiB whatever the table outcome (C's `repeat_valid && lhSize == 3`, the flag exists only while the frame still carries dictionary statistics — the merge of the r5-dict-gated form onto the exact-cost rule). json.opt bulk-st 4,495,370->4,484,055 (−0.31%->−0.06% vs the crate ref — mostly the treeless rule keeping tables through literal-heavy runs), json.balanced stream −8.9 KB, json.best/fast −3.3-3.6 KB each, text best/opt/ultra st −22/−20/−20 B (opt/ultra now ahead of the ref, +0.01%); 120-cell sweep: 0 cells worse, 72 better, −120 KB total. Remaining per-block gap vs libzstd on fresh tables is ~1 B of code-length tie-breaking (package-merge vs `HUF_buildCTable` order) — the streams themselves are at parity.
- **literals entropy bound skips its exact loop on bounded alphabets (2026-09-18, small-payload build-side round 6, part 2)**: `compress_literals`' near-incompressible reject computed the exact Shannon sum `Σ −c·log2(c/T)` over every live symbol before every huffman attempt — on compressible literals (the common case) the verdict is a foregone accept, and the sum's `f64::log2` calls run through libm (`__log2_fma`, 58 Ir each; 39 calls ≈ 2.3K Ir per json-4K call, 1.2% of the whole encode — C pays nothing here, its `HUF_compress` just attempts and compares real sizes). Entropy over k distinct symbols never exceeds `T·log2(k)`, so a distinct-count pre-check runs the exact loop only when even that bound can reach the reject floor; otherwise the loop is skipped with the verdict already determined (decision-exact: the exact sum ≤ the bound in real arithmetic, and the nearest integer-k bound clears or misses the floor by ≥ ~1e-3·T against f64 rounding noise ≤ ~1e-12·T — a uniform-k histogram where the sum EQUALS the bound is also the case where the floor arithmetic moves in lockstep). Byte-identical (full-ladder dump); gungraun small: json-4K −1.57% Ir (x1.128→1.110 vs libzstd), text-4K −2.43% (1.109→1.082), 16K −0.4..0.75%; random pays the distinct pass it cannot use (+2.2% on rand-4K Ir — the exact loop is its target case and still runs; the cell stays 1.6× ahead of libzstd). Round 6 wall (both parts, interleaved A/B vs libzstd): json-4K 488→535 MiB/s (+9.6%, x1.356→1.233), text-4K 359→390 (+8.6%, x1.393→1.271), skewed-4K 833→908 (+9.0%), 16K band +2.4-2.5%, 64K +0.7-1.9% — the Ir converts at ~2× on the 4K cells because the removed memcpy churn was memory traffic, the round-5 lesson holding; 120-cell sweep +8.67% geo-mean unchanged.

- **raw literals size-format ladder (2026-09-16 — todo 9's text Opt/Ultra residue)**: `raw_literals` had hardcoded the 20-bit size format (`0b11`), paying a 3-byte header where libzstd's `ZSTD_noCompressLiterals` uses `flSize = 1 + (size>31) + (size>4095)` — the exact ladder `rle_literals` beside it already implemented (the RLE gotcha above is why the smallest form writes one size-format bit, not two). Root cause found by decomposition, not by the parse: `seqstats --ref-frame` on text l17/l19 showed the parse at parity (12 sequences differ of 27k, entropy bound ours 12 B *better*), and a block-grid-aligned payload diff (decoder-instrumented per-block `dend`/section sizes) localized +2 B on every near-empty literals block — the tiled/zero tail's ~130 one-match blocks whose sections are otherwise byte-identical to libzstd's (`00 01 00 fdff39 0002` vs our `0c 00 00` + the same). Fix mirrors the ladder: text opt/ultra bulk-st 82,092→81,832 / 81,333→81,073 (zstd -17/-19 CLI: 81,844/81,079 — ahead of the CLI, −0.011/−0.019% vs the crate ref whose straight-128K grid is the honest pairing), mt −218/−184 B, text.best −178 B, text.fast −2 B, json.opt −6 B, dll100 opt/ultra −327/−368 B; 120-cell sweep: no cell worse; speeds in band (text.opt x0.632, text.ultra x0.716). The remaining text opt/ultra st bytes (~13-19 B) sit in the few big huffman literal blocks (+2-3 B each, same regenerated size — a small huffman-stream/table gap, shared with text.best's residue).

## Block splitting (opt+ rows)

- **libzstd's post-parse block splitter ported (2026-09-16, `blocks/split.rs`)**: one 128 KiB block's staged sequences are recursively bisected (libzstd's `ZSTD_deriveBlockSplits`); a split commits when the halves' estimated sizes beat the whole range's, and each committed range is emitted as its own block. The estimator runs the real table builds and the same selection the emission runs against the block-start entropy state (huffman desc via `write_table_desc`, FSE desc via `write_table` into a scratch buffer, stream costs via `estimate_compressed_size`/`repeat_bit_cost`), so estimates track emission rather than bounding it. `MIN_SEQUENCES_BLOCK_SPLITTING` 300, split cap 196, gate exactly `ZSTD_resolveBlockSplitterMode`'s auto rule (`Strategy::Opt` && window ≥ 2^17, post-shape-adjust — `Matcher::block_splitting_enabled`). Emission rolls the remembered tables across the cut: a later partition repeats the table the previous one wrote, so the treeless/repeat savings survive; the net per-stream outcome (New/Keep/Clear vs block start) feeds the caller's stock adoption. Speed tax: ~(2·splits+1)·3 estimates per block, each one huffman build + ≤3 FSE builds (measured below: 0.2-1.4% of the cells).
- **Deviation from C**: libzstd reconciles parser-vs-decoder repcode histories per partition (`ZSTD_seqStore_resolveOffCodes`) so any partition may fall back raw mid-split; here an incompressible partition instead restarts the whole block unsplit from the block-start entropy state (recycling the abandoned tables). Correct by construction — no partition was emitted, so the decoder's repcode history never diverged — and unreachable in practice: every partition carries ≥150 sequences whose encoded form cannot lose to its raw bytes. The rep-rewrite machinery stays unported until a corpus actually fires the restart (traceable via a counter if ever needed).
- Measured: json.opt bulk-st 4,484,059 → 4,481,374 (−0.060%) — the recorded json.opt st residue closed (sweep: 4,481,370 vs ref's 4,481,382); json.opt mt/stream cells +0.07%; json.ultra −1,167 B st (+0.02..+0.11% cells); text opt/ultra −4/−10 B (its 128 KiB blocks rarely split — ref only splits 2-5 blocks there). Full-ladder dump: every file byte-identical except json/text l17/l19; the 120-cell sweep's worst cell stays the pre-existing text.best −0.23% (btlazy rows are not gated — C parity). Ref decomposition (probe against the vendored zstd-sys source): the ref carries TWO splitter stages — the post-splitter above plus a dev-branch pre-splitter (`zstd_preSplit.c`, active at lazy2/btlazy2 rows); json-17's 447 ref blocks are all post-splitter, while the pre-splitter accounts for text's extra blocks at 9/13 where it LOSES ratio (86,951 vs 86,792 B with it off). The pre-splitter stays unported (todo 13's preSplit item).
- **estimation tax re-measured and cut byte-identically (2026-09-18, todo 9's owed debt)**: deterministic attribution — gungraun Ir against a splitter-disabled build — puts the whole tax at 0.53% (json.opt) / 1.37% (text.opt) / 0.20% (json.ultra) / 0.47% (text.ultra) of the cell; `derive_splits` is essentially all of it (the partition-emission delta vs the single-block path measures ~0: the per-partition table rebuilds are offset by the smaller ranges' cheaper encodes), and the per-estimate profile is literals histogram 26%, `select_from_counts` 21%, huffman build 13%. Walls json/text opt+ultra x1.107/x0.596/x1.129/x0.680 — the splitter is not what keeps json opt/ultra at x1.10+ (that gap is parse-side). The cut: the whole range's estimate threads down the recursion (a child re-derived the parent's exact range) and the right half is costed from `whole − left` histograms — adjacent ranges add up entry-wise, so counts subtract exactly, uniformity (`is_uniform` over the bytes) becomes `exactly one live symbol`, and the left halves are the only O(range) scans left. Full-ladder dump byte-identical; gungraun json opt −0.108%, text opt −0.178%, ultra −0.040%/−0.060%, skewed flat. Gotcha: `histogram_literals` requires at least one byte (its tail scan assumes a live symbol); a splitter half of pure matches has an empty literal range and must keep the all-zero histogram — the debug build caught it as a subtract-overflow panic, the release build would have indexed out of bounds.

## Histogram and incompressibility detection

- **pre-match incompressibility gate (all strategies)**: a block that is near-certainly raw skips the match search entirely — one strided content-tag probe pass (slot = high 15 hash bits of a sampled 8-byte window, entry = low 32 bits, ~2000 samples per 128 KiB block; any hit keeps the matcher running, so duplicated-but-uniform-entropy data still parses) plus, only when the probe is clean, an exact 4-lane byte histogram gated at Miller-Madow-corrected ≥7.97 bits/byte. Ratio-safe by construction: the opt tree's fill is lazy from `next_update`, so a skipped block stays available as later match history (`update_tree` clamps its catch-up fill to the live window); the table strategies (fast/dfast/chain) track a `gap_start` watermark opened at the gated block's start, and the next scanning block's `catch_up_insertions` dense-fills `[gap_start, block_start)` (window-clamped, oldest-to-newest — exactly the state a full scan's miss-path inserts would have left) before its first probe. The watermark is written from the gate side only, so the three scan-loop bodies stay byte-identical to the ungated build — any growth there re-rolls the fast tiers' code layout (see negative.md: the placement lottery entry). Two sampled distinct micro-screens (reject at <32 distinct after 64 samples, <96 after 512; uniform bytes expect 57/221) keep never-gating corpora out of the full pass: 16-letter skewed and json exit at 64 samples, text-like ~76-symbol alphabets at 512, so the gate costs ~0.1-0.5% instructions on compressible shapes. The entropy decision must be exact, not sampled: a 2016-sample Miller-Madow estimate of max-entropy input has std ≈ 0.11 bits (the correction fixes the mean, not the spread — see negative.md), so the 7.97 bar would coin-flip per block; a wrongly kept block forces a full scan plus catch-up fill, hundreds of times the histogram cost. random.best 12→1574 MiB/s (libzstd-12: 615), random.opt 1573 vs 14 (112×), random.ultra 1542 vs 6 (257×); extending to the table levels: random.fast 1469→1678 (x1.39→1.25 vs libzstd-3), random.balanced 1405→1688 (x1.29→1.09 vs libzstd-6), random.fastest 1652→1681 (x1.33→1.32); full-corpus full-ladder dump byte-identical, 120-cell ratio sweep flat. 2026-09-12, sticky `gate_hold` (same precedent as the literals gate's): once a block clears the exact pass, subsequent blocks gate on the probe alone until any rejection clears the hold — the histogram runs once per incompressible run instead of once per block (it sits at the x86 mem-op floor: 3 mem-ops/byte — byte load + RMW load+store — at 3 mem-ops/cycle, ~1.04 cyc/B measured via perf-annotate, so the pass could only be skipped, never cheapened; u64-load extract feeds would be undone by load-narrowing, unrolling drops only non-bound ALU). Risk model: a mis-held block needs ≥208 distinct sampled bytes + zero repeated 8-grams in the strided probe + exact entropy < 7.97 directly after a gated block, and what is lost is only the literals huffman saving — which at ≥208 distinct symbols cannot beat integer code lengths plus the table description. Measured byte-identical on the full-ladder dump, on a random→text transition file (L1), and even on a crafted flat-232-symbol adversary (16MiB random prefill to arm the hold) at L1 and L19 — the old path's own raw-fallback economics already emitted those blocks raw. random tiers 1.68→2.4-2.5 GB/s, ahead of libzstd at all six levels (fastest 2464 vs 2157, fast 2569 vs 2044, balanced 2505 vs 1525, best 2452 vs 218, opt 2427 vs 9, ultra 2253 vs 7); solo prof enc1 instructions −61% (4.58G→1.79G), the gate symbol gone from the profile (was 37.9% of cycles); 120-cell ratio sweep flat. 2026-09-17: the probe is the job's own history, not its residue — `prefill_window`/`adopt_strip_snapshot` replay the per-block sampling over the strip (block-sized segments, per-segment stride and phase, mirroring what a from-scratch scan of the strip would plant) and `reset` clears the filter, so pooled matchers gate like fresh ones. Before, the probe accumulated across jobs un-cleared and un-seeded: mt verdicts (and bytes) depended on which worker ran which job, and a block whose only twins live in the matchable strip gated away — a far-class kill for window strips the armed LDM serves and a near-class loss for reach strips. The replay must copy the scanner's sampling geometry exactly (see pitfalls: a single whole-strip stride is a modular lottery). Gated blocks also leave `fed` behind `block_start`, which the head-phase fill then tripped on a stale rolling hash; `LdmState::fill` re-arms at its base now (the prologue `generate` always had).
- literals histogram **4-way sub-tables** (split by position mod 4, avoiding store-forwarding serialization on the same counters): skewed cycles -7%, throughput +10%.
- ≤16 alphabet **AVX-512 exact histogram**: 16-slot `cmpeq_epi8_mask` + popcount accumulation; the popcount sum doubles as 17th-symbol detection (scalar fallback only when uncontaminated). skewed instructions -18%, +28%. `e76bb60`
- **Miller-Madow entropy sampling rejection gate**: strided sampling (~1024 points) + Miller-Madow bias correction + distinct>208 pre-screen + sticky `gate_hold`; rejects only at ≥8 bits/byte. random instructions -43%, +43% (1450→2060, overtaking libzstd L1). `cdc70c0`
- literals entropy lower-bound pre-check (exact histogram + Shannon bound + 8% margin; skip the Huffman attempt when it cannot beat raw; histogram and table build share one scan): random 455→1178 (+159%). `a32d2c1`
- three-table histogram fused into a single pass (one traversal of packed codes updates the ll/ml/of counters). `6690a05`
- fused pass lane-split: 4 sub-histograms per channel (12×1KB) break store-forward serialization on repeated-code runs; gated at nb_seq ≥ 128 (12KB zero/merge doesn't pay off on tiny blocks). choose_tables_fast cycles 2.83%→2.00% on json.fast; gungraun -0.07%.
- **sequence-code histograms sized to the 64-entry code space** (`SEQ_CODE_SPACE`; wire codes: LL ≤ 35, ML ≤ 52, OF ≤ 31): the merged histograms, mode-selection max-scan and normalization scratch cover 64 entries instead of 256, and the small-block direct path masks codes with 0x3f so the u8 index stays provably in range. The lane arrays must stay 256-wide: with 64-wide lanes the per-sequence increments lose their provably-in-range u8 index and grow three compare-and-panic pairs per sequence (+0.3% instructions, first attempt). Byte-identical output (full-ladder dump gate); gungraun json fastest/fast −0.4%/−0.5%, text −0.4%/−0.5%.
- **lane histograms pooled with prefix-only clear (2026-09-18, the pooling round's recorded residue)**: the 12 KB lane zeroing in `choose_tables_fast` was the largest per-block memset left on the fast path — callgrind on a json-4K fastest call: 12.3K Ir of 240K (5.1%), `__memset_avx2` running rep-stosb at ~1 Ir/byte, engaged at nb_seq ≥ 128 so every block in the 4K-64K band pays it. The lanes moved into `FseBuildScratch` (`take_seq_lanes`), and only the 64-entry prefix of each 256-wide lane is cleared per block: wire codes never reach 64 (LL ≤ 35, ML ≤ 52, OF ≤ 31; `pack_seq` debug-asserts every packed code), so no increment touches and no merge reads an entry ≥ 64 — stale tails are unreachable, and the lanes stay 256-wide so the unmasked u8 increments remain bounds-check-free. Byte-identical output (full-ladder dump gate, 5 shapes × 6 tiers); gungraun small json-4K −5.0% Ir (x1.533→1.456 vs libzstd), text-4K −3.5%, json/text-16K −1.5%/−1.2%, 64K −0.4%/−0.3%; random cells +0.03-0.17% (lane path never runs — layout drift). Wall interleaved A/B: json-4K x1.671→1.647, text-4K x1.875→1.850 — rep-stosb costs ~0.1 µs of a ~10 µs call on real HW, so the wall follows at ~1% where Ir says 5%.
- log2 under no_std uses a linear-mantissa approximation (error <0.086, swallowed by the 8% margin). `9121215`

## Checksum (xxh64)

- **in-house 8-chain XXH64**: 2×32B per iteration, 8 accumulation chains in flight (4 chains +8%); shared with the decoder, twox-hash demoted to dev-dep, zero external runtime dependencies. `3c0bf97` `81d2119` Note: the AVX-512 vpmullq vector core was a negative result (serial dependency chain); 8 interleaved scalar chains is the right answer.
- **pass fusion**: the RLE uniformity scan runs XXH64 rounds while comparing (uniform-block hashing completed inside the single pass; on mismatch it returns a 32B-aligned resume offset to continue); raw-block copies absorb while copying; the zero-sequence-block gate rejects with early exit. zeros +22%, random +10%. `3c0bf97`
- **sidecar thread offload** (std+hash, input ≥256KiB, ≥2 cores): a thread_local SPSC ring (512 slots) posts block bytes, consumed by a dedicated worker; the `BlockChecksum` trait unifies inline/offload. random +13%, text +26%, skewed +14%, zeros +2%, json +4%. `df33295`
- worker **bounded-spin parking**: parks on a condvar after a ~300µs budget (80µs was not enough — the raw-block pipeline paces at worker saturation, so wakeup latency lands directly on the critical path); the head flip is published under the lock to prevent lost wakeups. Saturated runs see ~1 voluntary switch per frame, 0 ticks while idle. `70b5fd4`
- on the MT path the checksum is absorbed in parallel, inline in the calling thread; during streaming bursts the main thread hashes.

## Bit writing and miscellaneous

- BitWriter 64-bit flush uses a single unaligned u64 store instead of an 8-byte memcpy: all corpora +1-2%.
- sequence codes computed once and shared (choose_table and encode_sequences consume the same set of code arrays, META table lookup split from add-bits): identical output bits, cleaner code. `b0c2653`
- sequence code/add-bits single-pass precomputation (codes packed into a u32 stream, add-bits pre-merged into u64): json +3%. `d9a840b`
- the matcher pushes the packed sequence stream directly (`Matcher::start_matching_codes`, json -3.5% instructions) — see the matchers page for details. `6faba75`
- RLE detection chunked by u64 (the original closure's byte-wise indexing could not be vectorized); RLE blocks' skip_matching inserts only the first position (all 5-byte windows of a uniform run land in the same slot): zeros 7.3×. `6abb9ae` `d9f078a`
- after match emission, short-literal overrun-overwrite copy (≤8B literal run: `reserve(8)` + unaligned read/write + set_len; the read runs into the following match, write overflow ≤7B is overwritten by subsequent pushes).
- uniform block detection: 4×u64 batched compares + deliberate `#[inline(never)]` (inlining into large callers made the loop lose its data pointer and reload it from the stack every 32B, halving throughput — counterintuitive but measured).
- **Dictionary-seeded entropy selection on measured costs (2026-09-16)**: the formatted-dictionary preload was quantified net-negative for our encoder (cross-fed-dict matrix on the 54-file systemd holdout, -9, per-file frames: zstdx with libzstd's formatted dict 5,868 B vs its raw content 5,762 — the seeded tables cost more than they saved while libzstd gained 88 B from the same tables). Three components, all gated behind a per-stream `DictEntropy` flag so no-dict output stays byte-identical (full-ladder dump): cost-based huffman treeless (stream bits vs fresh stream bits + exact description bytes, with the "description never pays" escape), the single-stream literals form for treeless blocks below 1 KiB, and the lazy+ three-way FSE cost comparison replacing the predefined-threshold preemption. Result: zstdx + our own formatted dict 5,917 → 5,413 B vs libzstd's 5,256 (+3.0%, was +11.6%); the remaining gap is the item 7 small-band parse over dictionary history (raw-dict control unchanged at +7.8%). Speed: the dict path is not hot; the non-dict literals path gains one cold flag check.
- **Dense dictionary fill (2026-09-18, todo 10's parse-side gap)**: `load_dictionary` had reused `prefill_window` — the MT strip's geometry (stride-3 grid, Fast capped to the retention tail) — where libzstd's `ZSTD_loadDictionaryContent` indexes dict content far more densely (the CLI's small-file route is CDict-attach, so its tables carry the `ZSTD_dtlm_full` fill). With 0.5-4 KiB payloads parsed over a 16 KiB dict window, candidate coverage of the dict dominates the parse. `fill_window_grid` splits the two geometries behind a `FillGrid` enum: the dictionary grid gives fast/dfast the stride grid plus the empty-slot backfill of skipped positions (`backfill_empty`/`backfill8_empty`, oldest twin of an idle slot survives), chain every position densely with links (libzstd's `ZSTD_insertAndFindFirstIndex`), and the tree rows keep their lazy whole-content fill (`next_update = base`, already equivalent to `ZSTD_updateTree`); the chain arm also drops `acquire_seed` (the dense links make every position walkable — measured byte-identical, the seed never fires on dict content). Alongside, dict frames walk the chain at libzstd's full `1 << searchLog` attempt count (`search_depth *= 2` in `load_dictionary`): the base table halves slog-derived counts as a no-dict speed tuning (parity holds on big payloads), while a dict parse walks a long, densely filled chain where the extra candidates convert. Measured on the rebuilt fixture (systemd v257.8, 86 train / 54 holdout 0.5-4 KiB files, per-file frames, summed holdout bytes vs the zstd CLI): -9 own formatted dict 8,239 → 7,944 vs libzstd 7,945 (**-0.0%**, was +3.7%), libzstd's formatted dict +1.2 → **-2.0%**, raw-content controls +5.5/+5.9 → +1.9/+2.1%; -1 raw +3.2/+4.1 → +0.3/+0.5%; -3 (dfast, stride-3 both sides) flat at parity. Gates: full-ladder dump and 120-cell sweep byte-identical (the strip path is untouched), dict cells 1-12 roundtrip through both decoders. Residues: -9 raw-dict ~5 B/file spread over every holdout file (no dominant file; depth x4/x8 past libzstd's budget plateaus at ~10-20 B total — recorded as tuning headroom, not landed); -1 formatted +2.0% with raw at +0.3% is entropy-side (the fast row's `choose_tables_fast` dict-seeded cost model converts less of the seeding than libzstd); -5 greedy +4.7%.
- **Lazy-family dict semantics on chain rows (2026-09-18, the -5/-9 raw residues)**: `load_dictionary` re-knobs a chain-row dictionary frame to libzstd's small-input table behavior wholesale — 4-byte chain-hash width (`ChainHashWidth`, threaded through the dense dict fill, the scan's pipelined head reads, and the covered-fill inserts), accept bar `min_match` 4, the `pays_for_offset_lit` gate bypassed, walk budget x8. Decomposition first (seqstats `--dict --ref-frame` on the biggest -5 holdout file): the gap was parse-side — 19 of 25 missed ref matches were ml=4 at dict offsets 2^11-2^13, literals +61% while our sequence entropy was 2.7% cheaper; C's CDict route at -5 selects the <=128 KiB table row (greedy, searchLength 4) with W15 (dictSize+513) and the row matcher (windowLog 15 > 14 auto-enables it), i.e. 4-byte candidate classes and an 8-attempt walk. Per-knob attribution on the fixture (zstdx-raw, -5/-9): width-4 alone LOSES (8708->8736 / 8442->8451 — the dilute classes bury the 5-byte twins the accept bar takes); accept-bar-4 without the width is a no-op (never candidates); pays bypass 49/95 B; depth x2/x4/x8 = 8640/8278, 8551/8269, 8518/8259 (x16: 8499/8259 — -9 saturates at x8). Joint: -9 raw +2.1 -> **-0.2%**, -9 formatted -0.0/-2.0 -> -1.6/-2.1%, -12 raw +3.9 -> +1.9%, -5 raw +5.8 -> +3.5%. Gates: full-ladder dump and 120-cell sweep byte-identical, -1/-3 grid cells unchanged, dict roundtrips levels 1-12 both decoders, 222 tests pass. Residues: -5 +3.3-3.5% (the CLI's greedy beats its own -9 with dict; remaining mechanism unknown — parse-order or row coverage), x16 depth 19 B headroom at -5 only; the -1 formatted entropy residue is the next entry.
- **Dict-seeded literal gates must price the treeless candidate (2026-09-18, the -1 formatted residue)**: the fast rows' formatted-dict gap (+2.0% at -1, raw control +0.3%) decomposed fully onto the literals side (per-block diff on the holdout: literals sections +497 B — 24 raw blocks vs the CLI's 51 treeless — while our sequence sections were −259 B ahead; the dict-seeded `choose_tables_fast` cost compare already beats libzstd's fast heuristic, which repeats unconditionally below 1000 sequences — favorable deviation, not ported). Mechanism: both literal incompressibility gates charged a flat ~160 B fresh-table description into the reject floor, but a dict-seeded stream's leading candidate is treeless — no description rides in front of it — so every small-literals block on a dict frame raw'd where libzstd treeless-compressed it (36 B → 24 B single-stream at -3, identical sequences and repeat modes; C has no pre-gate for repeat candidates: the `preferRepeat` path encodes with the old table and compares exact sizes, floor `minGain = (srcSize>>6)+2`). Fix: the description term drops out of both gates while `DictEntropy.huff` is set; the exact encoded-vs-raw comparison still bounds the cost of trying, and no-dict output is untouched by construction. Fixture (per-file frames, summed bytes vs the zstd CLI, zstdx/zstd formatted dict): -1 +2.0/+1.9 → +0.6/+1.2%, -2 +2.6/+2.0 → +1.2/+1.2%, -3 +0.1/+0.1 → **−1.2/−1.2%**, -4 +8.2/+6.4 → +6.8/+5.1%, -5 +2.7/+1.1 → +0.3/−0.8%, -9 −0.0/−2.0 → −2.4/−3.9%; raw controls unchanged. After the fix the literal decisions sit at CLI parity (50 treeless + 4 raw vs 51 + 3, all single-stream); the remaining fast-row formatted residue is literal-volume parse (CLI fast/dfast leaves 225 vs our 306 literal bytes on the worst file) net of our cheaper sequence sections (−310 B).
- **Width-4 short-table hash and dense small fill on dfast dict rows (2026-09-18, the -3/-4 fast-row residues)**: the CLI's -4 on payload+dict ≤128 KiB frames selects libzstd's small-input table row — dfast with searchLength 4, so its short table hashes minMatch (4) bytes where ours hashed 5 everywhere; the seqstats decomposition on the fixture's biggest -4 holdout file showed 18/32 missed ref matches were ml=4 twins at dict offsets 2^11-2^13, invisible to a 5-byte hash. `dict_row` (the chain-row flag, renamed from `dict_chain`) extends to Dfast rows and every small-table site keys `ChainHashWidth::of(dict_row)`: the prefill grid, `skip_matching`, `catch_up_insertions`, `DfastEmit`'s inserts (a runtime field, not a generic — these sites run per sequence), and the scan body via a new `MM4` const generic that dict frames reach through the runtime-log instantiation unconditionally (dict is not a hot path; every no-dict instantiation body is untouched), the long probe staying 8-byte (libzstd hashes 8 there whatever the row). Second layer, same decomposition after the width fix: 13/15 of the remaining missed sources sat off the stride-3 fill grid — libzstd's `fillDoubleHashTableForCDict` leaves the small table on the stride grid (i=1,2 backfill only the long table's empty slots, so off-grid twins reach it only as mml-8 matches); the dictionary grid now dense-fills the small table's skipped positions too, beyond libzstd. Fixture (54-file holdout, per-file frames vs the zstd CLI): -3 formatted −1.2/−1.0 → **−1.9/−1.0%**, raw controls −1.1/−1.2 → **−3.6/−3.7%**; -4 formatted +6.8/+5.1 → +6.0/+5.3%, raw +9.7 → +6.9% (9311→9075 B summed; levels 3 and 4 coincide on this shape — both rows clamp to the same tables). Gates: full-ladder dump and 120-cell sweep byte-identical, levels 1/2/5/9 grid cells unchanged, dict roundtrips 1-12 × 4 dicts × 54 files through both decoders. The -4 residue left is parse fragmentation with no isolated mechanism (nseq 197 vs 155, avg_ml 12.4 vs 15.9; one tape file carries 97 B of the 587 B raw residue). Falsified companions in [negative](../negative/entropy.md) and [negative](../negative/matchers.md): C's blind repeat below 1000 sequences (+8/+9 B/file — this encoder's parses fit the dict tables worse than libzstd's own), exact fresh-table pricing (no decision flips), +4-log oversized tables against shadowing (−19/−23 B — the frozen-CDict/fresh-CCtx dual-table probe is not worth porting).
