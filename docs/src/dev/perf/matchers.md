# Performance Optimization · Matchers and Compression Levels

## Strategy ladder (current)

| Level | ≈zstd | Strategy | Key parameters |
|---|---|---|---|
| Uncompressed | 0 | raw blocks | — |
| Fastest | 1 | fast: hash5 single-probe table | window 768KiB, table 2^15 u32, ml≥6 threshold, miss step `(miss>>2).min(255)`, rep0 pre-probe + rep1 chain |
| Fast | 3-5 | dfast: hash8 2^17 + hash5 2^16 dual-table single-probe | window 2MiB (W21), 4-anchor backfill after matches, stateless anchor miss step |
| Balanced | 6-9 | hash chain + lazy (+ LDM: ≥64 MiB windows, ≥32 MiB frame-continuous) | window W26 / chain reach W22 / H21 / C20 / depth 8 / lazy2, u32 tables |
| Best | 13-15 | btlazy2: lazy2 selection over the DUBT tree (+ LDM: ≥32 MiB frame-continuous) | window W26 / tree domain W22 / H22-23, S4/5/6 search (C's L13-15 rows), mls 5, literal-aware lazy margins |
| Opt | 16-17 | btopt (full port, + LDM: ≥32 MiB frame-continuous) | window W26 / tree domain W22-W23 / H22 / C22 (L17 row, ring capped one below libzstd's C23) |
| Ultra | 18-22 | btultra(+2), + LDM | window W26 / tree domain W23 / H22 / C23, 2-pass first-block statistics seeding |

## Fastest (fast)

- **zstd-fast style rewrite**: contiguous window + single-probe u64 hash table (originally `epoch<<48|pos` slots, reset only bumped the epoch to skip clearing; later `327bc99` switched to u32 and retired the epoch), hash5 (u32 + the 5th byte), u64-chunk compare forward/backward extension (backward extension goes into pending literals), increasing miss step. Matching ~3×, text ratio leapt 54× (the old matcher was nearly defunct on long-repetition data). `a2cc982`
- **repcode**: the matcher maintains rep[3] (sharing `do_offset_history` semantics with the decoder); ll==0 uses the **pos+1 probe** (the current byte acts as that 1 literal, backward extension guarded by `start > anchor+1`); emits of_value (1=rep0, new offset=offset+3). json rep hit rate 0.07%→7.6%. `1d25a26`
- rep1 immediate chain probing (zstd rep_offset2 loop, ll==0 auto-swaps rep0/rep1): json ratio 4.93→5.23. `49de097`
- **sparse indexing**: ≤16B insert everything, >16B insert only the start+2/end-2 dual anchors (keeps json's long-distance matches better than zstd's pure dual anchors; pure anchors cost json -2.1%/text -6.3% ratio). json +6%. `4e79929`
- table 2^16→**2^15** + strided insertion on the miss path: small table with high contention + newest-wins = strong proximity bias (zstd -1 is 2^14 slots against a 512K window = 32:1 coverage). `21fb329`
- hash path **ml≥6 threshold**: a 5-byte match's sequence cost ≈ the cost of the literals it covers, so rejecting it lets the scan advance toward longer match starts (tried 7/8: json saturated, text collapsed; 6 is the balance point). json 5.44→5.72. `be2bfe9`
- window 448K→**768K**: the text corpus's tile period falls in 512K-768K, unlocking cross-tile matches. text ratio 6.46→239. `7a2a388`
- **select-based probing** (libzstd selectAddr): invalid candidates are selected into the scan position itself; byte compare + single-branch rejection via `cand != ip` replaces the epoch/win_base/distance triple compare; the rep0 pre-probe folds into the single comparison `probe >= win_base + rep[0]`. json 455→466 MiB/s. `29bcb02`
- `TableEmit` context struct replaces the 12-14-parameter free function (guards against parameter stack spilling); scan-side table access via raw pointers. `c528c57`
- **emit split**: the coverage-indexing loop moved out of `TableEmit::emit` into an `#[inline(never)] insert_covered` — with it inline, emit exceeded the inliner budget and each of the scan loop's five call sites paid a full spill/reload of the emit context (literals/seqs/rep) plus a hash-constant rematerialization around the call (the call prologue/epilogue alone sampled ~9% on json.fastest). Splitting leaves emit small enough to inline at every site. Byte-identical output; gungraun json.fastest -2.7%, text.fastest -1.1% instructions.
- **two-phase scan loop**: the job-start seed/gate state (`seed_offset`/`seed_hits`/`seed_budget`, `rep_pending`) only ever winds down, so the body is a macro instantiated twice — a seeded phase until convergence, then a steady phase with all four values constant-folded out. One loop kept them live across every iteration (pure dead weight in bulk-ST and post-convergence MT jobs), and ~24% of the scan's cycles sat on stack spill/reload traffic. Byte-identical; gungraun fastest json/text −10.1%/−10.0% Ir, random −2.0%; wall json.fastest 477→532 MiB/s (x1.78→1.60 vs zstd-1), text.fastest 11279→12855. Companion: `rep1_chain` moved to `inline(always)` — duplicating the body halves the inliner's budget per copy, and left to its own judgment the helper flips to an outlined call whose argument setup dominates dense-match emits (json was at −3.1% Ir before that fix, the emit-split lesson running in reverse); forcing it doubled the win.
- **steady-body slimming (2026-09-11)**: three pure overhead cuts, byte-identical, wall-neutral on json (the loop is branch/latency-bound, IPC ≈ 3): the pair-prep u64 feeds hash5, the 4-byte prefilter (its low half) and the rep compares (two loads per pair gone); the rep probes compare in window-index space (`pidx >= rep[0]`), deleting the per-position `rep0_lim = win_base + rep[0]` u64 chain and its spill; and the fast/dfast `resolve` rebuilds the candidate as a *distance* — `dist = pos − entry + 1` (double-wrapping, no `entry−1` underflow) wraps huge for the empty sentinel (dist = pos+1) and previous-4-GiB-cycle entries, so the single compare `dist <= reach` (reach = min(pos−win_base, max_window)) replaces the entry==0 / wrap / window triple check and returns `idx − dist` directly. gungraun fastest json/text −5.9%/−5.7% Ir; random +1.35% (miss path pays two extra ALU per empty probe, wall unmoved at 2.3 GiB/s). The `pos1 == anchor` select in the second probe is provably dead (anchor ≤ pos < pos+1) and folded out. A steady/tail loop split folding `pair_len` to a constant 2 was dropped: −3.3% Ir but text.fastest wall −7% — the third macro instantiation displaces the hot blocks (see negative.md).
- **OR-folded pair rep prefilter (2026-09-11)**: the pair's two repcode compares (one per position) collapse into one unpredictable branch — XOR prefilters `a`/`b` over the same loads, taken when either is zero (adjacent-position rep hits are correlated, so the fold predicts no worse than either compare alone); a taken fold whose first position misses arms the second position's compare-free full probe, and probe/emission order is untouched → byte-identical output (dump-gated). The branch-bound regime's expected trade: gungraun fastest json/text +1.5%/+1.4% Ir for wall json.fastest 530→545 MiB/s (x1.62→1.57 vs zstd-1); text.fastest −1.4% sits inside that cell's layout-noise band (untouched fast cells moved ±3-8% in the same binary), skewed/random flat. The probe-*count* cannot drop instead: halving it (libzstd's ip2 one-compare-per-pair policy) costs json.fastest +4.75% size — see negative.md.
- **hash-compare OR-fold (2026-09-16, the fastest tier's mid-size band)**: the pair's two hash-match compares (`cand != ip && read4(cand) == cur` at both positions) are resolved and pre-compared up front and share ONE branch — `m = (cand == ip) as u32 | (read4(cand) ^ cur)`, fold `(m0 == 0) | (m1 == 0)` — the same transformation class as the rep OR-fold above. LBR attribution on json/text-256K (checksum-parity builds): the two compare jne's carried 5.3+4.3% / 6.4+6.3% of ALL process samples (the mispredict inventory's top pair, the class item 3 called inherent); after the fold the scan's branch profile is flat (max 1.6%). The pre-reads are loads only (table writes happen in emit), and emission order is untouched — rep0@p0, hash@p0, rep@p1, hash@p1 — so output is byte-identical (full-ladder dump ×30 plus L1/L2 × ST/MT emitframe gates). Fold-only A/Bs (3-4 interleaved rounds, medians): json-256K 622→677 MiB/s (x1.528→1.400), json-1M 573→614 (x1.507→1.406), text-256K 354→370 (x1.718→1.650), text-1M 814→841 (x1.604→1.554), json-4K/16K flat, 32MiB matrix json.fastest x1.502 (was 1.52-1.57), text/skewed/random/zeros wins intact. The one regression: json-64K −5% (744→696, reproduced across three sessions; perf: instructions −3%, branch-misses +37%, cycles/MiB +7%) — in the fully L1/L2-resident single-block regime the merged branch's streak pattern loses to the two originals' separate biases; v1's variant folding `rep1_armed` into the same condition was worse (see negative). Post-fold there is no branch lever left in this loop; the remaining mid-size gap is instructions (1.5x libzstd per probe event) and insert_covered's store floor.

- **per-block dense-mode scan instantiation (2026-09-16, todo 3)**: the acceptance-bar hosting the negative notes demanded — a second `const DENSE: bool` instantiation of `start_matching_fast` (dispatch keys on a `ScanDensity` field alongside `RampGate`, so four function instantiations), selected per block from the previous block's parse: ≥512 sequences, <4 literal B/seq, and the fed-back literal table covering <32 symbols. That last arm is the load-bearing one: parse statistics do NOT separate the classes — text's four match-dense blocks carry 99.5% of its sequences and overlap json's nseq band (2133-9420 vs 6560-6799) and avg_ll band (1.37-2.64 vs 2.60-2.83), so a stats-only gate either leaks them (output changes, stepping damage) or excludes json; the literal alphabet does separate (json's residual stream covers 11-25 symbols at 3.47 bits/B, text's dense blocks 78-83 at 5.4+, dll's sparsest stats-passing block 43 — the bar sits mid-gap). The dense body hosts the two falsified-as-unconditional levers: the **fed-back-literal-price acceptance bar** (`dist >= 1024` pre-gate, `pays_for_offset_lit` after backward extension at both hash probes, declines fall through to the remaining probes and feed the miss ramp; prices come from `note_literal_costs`, which already fires for the fast tier, flat-4 default at frame/job starts) and **miss-run stepping** (pair advance doubles once the miss run exceeds one full missed pair, `miss_count > 3`). The plain instantiation's body is source-identical with the levers folded out. Results: json.fastest −3.91/−3.83/−3.67/−3.34% size bulk-st/stream-st/bulk-mt/stream-mt (5,466,381→5,252,606 bulk-st), 32 MiB wall 559→590 MiB/s interleaved 3 rounds (x1.528→1.443); json-1M −2.72% size at 608→630 MiB/s (x1.425→1.374); json-4K/64K dormant (±0.5%, single-block payloads never gate). Everything else byte-identical: 120-cell ratio sweep moved exactly the four json.fastest cells, full-ladder corpus dump only json.l1, dll100 l1/l3 emitframes bit-equal, dll100 fastest wall flat (358 MiB/s, xor-equal). Decomposition: bar alone −1.35% (reproducing the negative entry's number), stepping alone −3.16%, combined −3.91% — the levers stack. The step threshold's size response is non-monotone trajectory chaos (see [negative](../negative/matchers.md) for the sweep). Cost: text.fastest −1.8% wall at byte-identical output — the plain instantiation's placement in the doubled-code binary, inside that cell's documented ±3-8% band; not chased (the placement-hunt lesson).
- **dead-mask drop in the hash helpers (2026-09-13)**: `>> (64 - log)` already leaves exactly `log` bits, so the `& ((1 << log) - 1)` in `hash5_log`/`hash_at_log`/`hash8_at_log` was provably dead — but with a runtime `log` LLVM cannot prove that and rebuilds the mask (`mov -1; shl cl; not; and`) per hash: hoisted out of the steady scan loops, but per-call inside any outlined body (the chain walk rebuilt it every search). Dropping the AND is byte-identical by construction (full-ladder dump gate); gungraun fastest json/text −5.2%/−6.4% Ir, fast −4.2%/−7.5%/−6.1% (json/text/skewed), balanced −6.1%/−5.9%/−0.6%, opt/ultra ≤0.1%.
- output bitstream side: u64-spelling bit writes (see the encoding page); final ratio json 6.15 (vs zstd-1 6.11).

## Fast (dfast, counterpart of libzstd level 3)

- **mis-tiering lesson**: Fast initially used chain+lazy (zstd-3 is actually dfast) — a 2.5MB hot randomly-accessed table + per-position insertion + lazy's doubled search; json lagged 2.73× and text was 3× slower than Fastest. After the dfast port, json +80% (175→314 MiB/s, ratio 5.47→5.33 overtaking zstd-3's 5.31), text +154%. `19077f3`
- skeleton: dual tables (hash8 2^17 prime 0xCF1BBCDCB7A56463 + hash5 2^16 reusing the chain buffer), insert-then-probe, two-position pipeline, ip+1 long-probe upgrade after short hits, backward catch-up, repcode ip+1 pre-probe, **zero table insertion inside a match**, 4-anchor backfill into both tables after a match + immediate repcode loop.
- **emission path slimming**: `SeqWord` 16B single stream (codes/add_bits/add_nbs three streams into one, 3 push→1 push; push/pack must be `#[inline(always)]` — with plain #[inline] it stays uninlined and still costs 6.8%); ll==0 sequences (50% of json, rep chains) skip the literal copy; probes become cmovs. json 314→366 MiB/s. `1398d23`
- **stateless miss step** `1 + (ip-anchor)>>8` (drops the per-256-jump counter pair) + exact backfill predicate ("backfill only when the match covered the probe position", replacing the step<4 proxy): json +1.7%, text +2.6%. `0b46f21` `06b67dc`
- **two-phase scan loop + `inline(always)` on `rep_chain`**: the same seeded/steady split as the fast strategy (see the Fastest section); this loop's live set is even larger (two tables, two hash logs), so the dead weight cost more — gungraun fast json/text −5.7%/−5.4% Ir, skewed −8.1%, random −5.7%; wall json.fast 371→387 MiB/s (x1.29→1.25 vs zstd-3), text.fast 10821→12033, skewed.fast x1.37→1.31.
- **distance-resolve + idx-space rep probe (2026-09-11)**: the same two rewrites as the fast strategy's steady-body slimming (see the Fastest section) land bigger here — the dfast body carries two tables and two resolves per position. gungraun fast json/text −9.7%/−8.4% Ir; wall x1.22-1.25 vs zstd-3 (was 1.25; between-run drift is ±5%, the Ir cut is the solid part).
- window 1MiB→**2MiB (W21, 2026-09-12)**: libzstd's L3-9 all run wLog 21, and on large real binaries the window — not the matcher — decides the ratio. Control experiment on the 100MB ELF-concat corpus (`bench/gen_big.sh`): libzstd-3 capped to wlog=20 lands within 0.03% of our old 1MiB output (42,050,022 vs 42,063,166), i.e. same-window our dfast was already at parity; the 2MiB window then takes both sides forward — dll fast 42.06MB→30.19MB (−28%, and 8.1% *denser than libzstd's own L3* at 32.86MB) while running +39% faster (236→329 MiB/s; longer matches shrink both the scan and the literal stream). Corpus: json ratio −1.2% (6262491→6339383, now exactly libzstd parity — both use W21), text/skewed bit-identical sizes, speeds json +4% (387)/text +11% (12045)/skewed +12% (183); the pre-u32-tables falsification (json −1.1%, −4% speed) did not reproduce under the current matcher.
- dll-shaped corpus facts for future A/Bs: avg_ll 3.58 vs json 0.72, avg_ml 10.9 vs 15.3 (literal-heavy short-match shape); L1-load-misses 1.72/B vs json 0.79/B (near-uniform hash spread — table probes stop hitting L1); at matched windows the residual speed gap to libzstd-3 is x1.35 (359 vs ~480-560 MB/s, after the emit inline below). Instruction-side decomposition (callgrind dll32, 2026-09-12, vs the zstd CLI's `ZSTD_compressBlock_doubleFast` with storeSeq inlined): the miss-loop bodies are near-identical (~45 vs ~47 inst/iter at the same `1 + run>>8` step — the earlier "3× more scanned positions" reading did not reproduce; nseq/avg_ll differ because our output is denser); the excess was (a) `DfastEmit::emit` at ~170 Ir/seq — outlined (8-arg call, rep via `&mut` memory, Vec fields loaded through self, unprovable-order sub checks) vs libzstd's inlined storeSeq+anchors — **landed 2026-09-13, see the next bullet**; (b) the literals pipeline, whose huff0 stream half is now closed (dual-accumulator port, see the encoding page). We conversely win 2× on encode_sequences (74.6M vs 158.1M Ir). Wall on this shape is cycle-parity with the CLI's compress worker; the CLI's headline number pipelines I/O on a second thread.
- **`DfastEmit::emit` inlined into the scan loop (2026-09-13)**: `#[inline]` → `#[inline(always)]`, the same treatment `rep_chain` already documents (the phase-split macro instantiates call sites twice, halving the inliner's budget per copy — plain `#[inline]` flips to an outlined call in the steady phase). The calling convention itself was the tax: three stack-passed args, rep round-tripping memory, Vec ptr/len/cap loads through `self`, prologue/epilogue — ~50 of the outlined body's 170 Ir/seq over 1.21M calls (26% of the whole fast encode on dll32). Inlined, the seq push and anchor writes fold into the loop like libzstd's storeSeq; no layout lottery materialized (all four shapes improved). Byte-identical output; gungraun fast: json −8.2%/text −7.5%/skewed −1.3% Ir, fastest tier bit-for-bit unchanged (`TableEmit::emit` was already `inline(always)`); interleaved wall dll100 335→359 MiB/s (x1.47→x1.35), json 386→421, text 11055→11616, skewed 184→199; balanced/best/opt ±1% (their `emit_chain` was already inlined — no separate symbol at level 6).
- **RAMPED const-generic scan instantiation (2026-09-16)**: the fast/dfast loops carried the MT ramp gate as three live u64s plus per-site never-taken branches on every disarmed frame (all bulk-ST paths; MT jobs without `ZSTDX_MT_RAMP_BYTES`) — the dfast loop paid the spilled `end` reload on its match path (`cmpq $0x0,0xc0(%rsp)`, 1.4% of dll100 samples). The loops are now `const RAMPED: bool` (dispatch keys on `RampGate::is_armed()`, chain keeps the dynamic form); false instantiations fold `ramp_blocks`/`ramp_ext_floor` to nothing and free the registers. Byte-identical (full-ladder dump, dll100 l1/l3/l9 emitframe gates, 188+16 tests); dll100 interleaved: fast 359→362-381 (median 372 over 9 rounds, +3.5%, x1.211→x1.16), fastest flat (its dll wall is entropy+insert-bound, not this loop's registers); matrix json.fast x1.075 / text.fast x0.548 / both fastest cells in-band — the doubled instantiation count cost no layout regression.
- **const-generic log instantiation for the dfast scan (2026-09-16, todo 8)**: the dfast scan body owns the fast tier's whole remaining dll100 gap — 1.42x C's Ir on a 4 MiB dll slice (145.0M vs 101.8M callgrind Ir under the same toggle as `ZSTD_compressBlock_doubleFast`; ~63 vs ~45 Ir per miss iteration), and both hash shifts fed that loop's register famine: the small-table shift reloaded from the stack every iteration (`shr rax,cl` + `[rsp+0xa8]`), the long-table shift held a register and still reloaded per pass — gcc's C loop pays the same class. `start_matching_dfast` now takes `const LONG_LOG`/`SMALL_LOG`; the dispatch keys on the tables' actual lengths and instantiates the two full rows (17/16, 18/18) with immediate shifts (two shift values gone from the live set — the window pointer stays live across the loop), while clamped-window inputs (logs below the row values) take a `RUNTIME_LOG` instantiation identical to the old code. Byte-identical (full-ladder dump + test suite); callgrind dll4 145.0M→138.0M (−4.9%, 1.356x C), json4 73.2M→70.0M (−4.4%); interleaved wall dll100 fast +3.4% (198→205 MiB/s), json fast +3.6% (446→462, 9 rounds each). Two C-parity restructures of the same loop are falsified the same day (see [negative](../negative/matchers.md)): the loop's saturated register budget re-rolls allocation on every added live value, so only register-NEGATIVE changes convert.
- **dll100 shape facts refresh (2026-09-16, post-regeneration)**: avg_ll/avg_ml 9.56/11.98 (l1), 3.59/15.45 (l3), 4.52/26.42 (l9) — the recorded avg_ml 10.9 fast-tier shape is the pre-2026-09-15 corpus; sequence counts run at parity with libzstd at l1/l3 and 29% below at l9 (the parse-density lever todo 8 was premised on does not exist). Sizes vs the zstd crate bulk ST: 48.89 vs 47.92 MB (l1, +1.88%), 30.19 vs 30.87 (l3, −2.23%), 22.39 vs 22.70 (l9, −8.40%).
- **density-gated stride-2 covered fill (2026-09-16)**: the fastest tier's dll100 decomposition put `insert_covered` (dense ≤16-match interior fill, ~36M inserts per 100 MB) at 8.1% of cycles while C's fast pays exactly two anchors per match (`start+2`/`end-2`) and nothing more. Value probes: dual-anchor-everywhere (C's exact policy) costs dll100 +0.46% size for +7.3% wall, json +1.58% / text +3.04% (regenerated-corpus numbers; the pre-regeneration falsification in [negative](../negative/matchers.md) measured worse); stride-2 costs dll100 **+0.042%** — the pair-pipelined scan probes adjacent positions, so any two consecutive probes span both stride parities and a phase-shifted future scan still hits within one byte — but json +0.90% / text +1.40%: the second parity's value is 20× shape-skewed (dll's duplicated mass has many earlier copies to hit at any phase; a json/text tile's only copy must be hit at the shifted phase or the match is lost). Landed policy: `CoveredFill::{Dense,Strided}` selected per block from the previous block's parse — ≥64 sequences and ≥4 literal bytes per sequence (dll fires on 645/799 blocks carrying 77.7% of the fill coverage; json's per-block avg_ll is tightly 2.60-2.99, text's 1.00-2.64, skewed only ever fires nseq≤4 literal-run tails barred by the floor) — with the branch inside the outlined `insert_covered`, never in the scan loop. Results: dll100 fastest 48,825,383→48,804,295 (**−0.043%** — halved newest-wins churn keeps older far entries alive) at 354→358 MiB/s interleaved (+1.1%, 11 rounds; +1.7% in an earlier 7-round session) — about 45% of the ungated ceiling converts, the rest pays the rolled strided loop's per-insert overhead and the dormant-branch cost; json/skewed wall flat, text.fastest +7.7% (a layout-lottery roll in this build's favor, the same ±7-12% class the loop is documented to swing — not a mechanism claim); corpus byte-identical everywhere (30-file full-ladder dump, json/text-1M, all three emitframe modes × json/text/skewed, dll100 l3/l9, dll100-mt4 roundtrips at 48,817,644); gungraun small-band dormant Ir +0.22-0.52% json/text, +0.09-0.13% random. The unrolled stride-2 pair form loses 1.2% wall to the rolled one and the gated dual-anchor extreme (+3.7% wall, +0.54% size) is rejected on the dll100 size axis — both in [negative](../negative/matchers.md).
- **zero-add-bit fast path in `pack_seq` (2026-09-13)**: LL codes 0-15 are ll itself and ML codes 0-31 are ml-3, both with zero add bits, so `ll < 16 && ml < 35` degenerates the whole pack to `bsr` + ors — no LUT+META load pairs, no variable-shift add merge (LLVM builds the `(1<<log)-1` mask as `0x80000000 >> log; dec`). Coverage: json.fast 99.997% of ll / 96.5% of ml; dll-shaped avg_ll 3.58/avg_ml 10.9 sits deep inside; text's 557+ avg_ml sits outside but emits per MiB are ~30× sparser. skewed's ll<16 share is only 63.5%, yet branch-misses measured exactly equal (6.89M both builds) — the gap sequence is locally regular enough to predict. Byte-identical output (full-ladder dump); gungraun: fast json/text −4.5%/−4.2% Ir, fastest −1.7%/−2.1%, balanced −0.3%/−0.6%, skewed/random/zeros flat; wall within noise everywhere re-measured (json 431, dll100 358-363, skewed perf-stat cycles −1.2%) — the fast tiers' wall on the 32MiB shapes stays branch/latency-bound, the cut is for literal-volume shapes and drift-free machines.

## Balanced / Best (hash chain)

- **gain-aware lazy walk (libzstd lazy port)**: lazy positions compete on `ml*4 - highbit(offset+1)` (reps price 0) instead of raw length, a rep0 probe rides each step (`mlRep*3/4` gain forms), and the walk advances while improving with one second-chance probe at depth-2 margins (+7) — pure-length comparison had kept far chain candidates whose offset exponent cost more than the length saved, the root of the text.Balanced deficit. json 6.08→6.55 (+13.8% vs zstd-6), text 361.4→366.3 (deficit −2.35%→−1.05%); sequences drop (json −11%). Cost: the second-chance search adds ~30% matcher instructions on rep-dense json (149→120 MiB/s); the depth-2 margin itself is load-bearing (depth-1-only collapses to json 6.18/text 361.5), and the depth-0 rep bias stays `ml+3 > best_len` (more rep-biased than C, kept).
- **literal-cost-aware lazy walk**: the walk's gain comparisons (rep probe and chain probe) price a candidate's displaced literals at the fed-back code lengths (first four bytes — every candidate is ≥ MIN_MATCH — clamped to 6 bits: the walk is a *local* selection and uncapped 11-bit prices let cap-priced bytes dominate whole walks; clamp sweep on balanced bulk-st json 4726875/4701990/4697420/4725425, text 91243/91232/91200/91140 at none/8/6/5 — the json swing dwarfs the text one at every step) instead of the flat `ml*4` scale; at default lengths both formulas reduce bit-for-bit. Cheap-reject gain caps (≤6 bits/byte) skip the gathers when even the best case cannot beat the incumbent. json.balanced 6.66→7.14 (+15.7%→+24.1% vs zstd-6), text.balanced flat (368.07→367.92, −0.57%→−0.61%), the other 104 ratio cells bit-identical. Cost: better pricing accepts more walk steps on json, +13% matcher instructions (json wall 119→~106 MiB/s, still ×1.72 ahead of zstd-6) — same exchange pattern as the lazy rework (+7.7% ratio for +30% instructions), better rate.
- **literal-cost-aware store gate**: the block encoder feeds the Huffman code lengths of each block's literal table back to the matcher (`Matcher::note_literal_costs`, per-frame/job reset keeps MT output content-deterministic), and the chain gate stores a match only if the displaced literals — priced at those code lengths, uncovered bytes at the 11-bit cap — cost more than `highbit(offset) + 7`. The flat 4-bit constant traded json against text monotonically (+4/+7: json 6.21/6.55, text 368.1/366.3); the whole swing is 5-byte matches at 8-32 KiB offsets, whose displaced bytes are nearly absent from text's hyper-skewed residual literal stream (the *average* residual entropy, ~0.2 bits/B on text vs 3.3 on json, anti-predicts the desired direction — the marginal code length is the right price). text.balanced 366.3→368.1 (−1.05%→−0.57% vs zstd-6) AND json.balanced 6.55→6.66 in one move; skewed (flat 4-bit alphabet) and random (no Huffman blocks, default lengths) reduce to the static gate bit-for-bit; speed neutral. Only the chain levels consume the feedback; the fast/dfast seed gates keep the static +7.
- position-indexed chain table + lazy (hash-log/chain-log/depth/lazy/window parameterized, a single `ChainMatcher` reuses emit/extend/repcode logic). `f8cc66d` Porting pitfalls: repcode backward extension must keep ≥1 literal; pos==anchor must probe rep (otherwise it loses to its own Fastest by 8×) — details in the pitfalls notes.
- **miss-run acceleration**: miss acceleration on incompressible runs (climbing in steps over long literal runs), random ~2.1 GiB/s at all levels, Best faster than zstd-12's 918 MiB/s. `47b60b0`
- **reach=window principle**: the chain table is position-indexed, so chainLog is the match reach; `reach<window` (C18/C19/C17) collapsed on skewed — long-distance repeats lost → matches shorten → position-by-position scanning slows. Balanced settled at W20+C20 (same 9MB table yet better everywhere: >1MB high-cost offset encodings eliminated). `3c88244`
- **broken-chain fix (major)**: insert used window indices while walk used absolute positions, so once win_base advanced, walks ran over truncated garbled chains (effective depth ~2-5) — Balanced/Best's earlier "speed lead" was an artifact of burning fewer probes, and the ratio was off too; the 4× collapse of streaming Best 18 vs bulk 74 MiB/s shared the same root. After the fix, the whole ladder was retuned on real chains + **beat-check** (a 4-byte beat check on candidates avoids full extension). `36203c1` `4ff2b7b`
- **Best core swap**: depth 16→64 bought only +0.9% ratio for a 2.7× speed difference — deep search has terrible cost-effectiveness, so Best switched to a low-config optimal parser (16 compares / targetLength 32). json Best 5.77→6.88 (overtaking zstd-12's 6.14), text 270.8 (overtaking 256.1). `b39a192`
- **table entries to u32**: store the low 32 bits of `abs+1` (0 = unwritten sentinel); on read the high bits are rebuilt from the scan position, wrapping on a 4GiB period; stale entries are covered by window-domain validation + byte verification as a backstop. Table working set halved (Balanced 16→8MiB, aligned with libzstd), json.Balanced +18%, text +20%. The epoch mechanism retired (the opt parser keeps its epoch u64). `327bc99` Comparison: with the Fastest 2^15 table (L2-resident), u64→u32 had no effect — **entry width is a lever only when the table exceeds L2**.
- **window 1MiB→2MiB (W21, 2026-09-12)**, chain table stays 2^20: positions beyond 1MiB alias in the position-indexed links exactly like libzstd's cLog-below-wLog chains (its L6 runs wLog 21 against cLog 18 — 8× aliasing), while the head table's newest-wins probe still serves candidates across the full window. Ratio: json.balanced 5516920→4711529 (**−14.6%**, +23.7% vs zstd-6 — the json corpus's tile repeats live at 1-2MiB distances), text 92833→91200, dll 40.41MB→28.22MB (−30%, 7.6% denser than zstd-6); skewed/random/zeros unchanged. Speed: dll +17% (72→84), skewed +9%, random +30%; json −14% solo (107→92 MiB/s — colder far candidates: L1-misses 1.57→1.77/B, instructions only +3.5%; the ratio win itself is real work spread over 2× the reach) and text −10% in the interleaved matrix; json x vs zstd-6 now 2.10 carrying a +23.7% density lead. C21 (8MB chain table, no aliasing) bought back only +5 MiB/s on json at dll-equal speed and +4MB/encoder footprint (×MT workers) — rejected. The old "reach=window" principle stands corrected: W4M-on-32MiB-corpus was the wrong test shape; what W21 needed was a corpus whose redundancy actually sits at 1-2MiB (large binaries, tiled json).
- **chain-walk search inlined + distance-resolved (2026-09-13)**: the `search` closure sat outlined at its two call sites (scan loop + lazy walk) and its calling convention was the tier's top hotspot — 66% of json.balanced cycles paid on 7 closure-env loads, ~12 stack round-trips, and a per-call runtime mask rebuild, on top of hashing and head-reading every position **twice** (search once, the insert block once). Now a module-level `#[inline(always)] chain_search` (the `DfastEmit::emit`/`rep_chain` precedent): the caller hashes and reads the head once, passes the entry in, and the insert block links the chain slot to that same value (nothing writes the table in between); the walk resolves entries as distances — `dist = pos - entry + 1` wrapping — with `dist - 1 < reach` admitting exactly [1, reach] and replacing the `unpack_pos` Option plus the separate candidate-floor compare (the u32-entry resolve rewrite the fast/dfast loops already took). One correctness trap on the way: a stale pooled-table slot (cross-frame residue) can hold `pack_pos(pos)` of the current position — dist 0, the candidate byte-compares against itself and `(idx-cand).ilog2()` panics; the old wrap check covered it implicitly, `dist - 1 < reach` restores the rejection (see the pitfalls page). Cumulative with the mask drop: gungraun balanced Ir json 134.6M→109.9M (**−18.4%**), text 64.3M→52.9M (**−17.8%**), skewed −1.8%; this change alone −13.0%/−12.7%/−1.3%, fast/fastest Ir bit-identical to the mask commit. Wall interleaved: json.balanced 80→87 MiB/s (x1.50→1.40 vs zstd-9), text.balanced 2637→2818 (x0.63→0.59); dll100 solo 96-97 flat. The perf profile after: the outlined symbol is gone, `start_matching_codes` owns 94% with everything inlined.
- **emit_chain forced inline (2026-09-16)**: at a plain `#[inline]` LLVM outlined the chain scan's only emit call — 10.9% of dll100 balanced cycles and 14.2% of its L1-dcache-miss samples sat in the outlined body paying the ten-plus-arg calling convention per sequence (3.39M sequences per 100 MB; the outlined search closure's disease at a new site). `inline(always)`, byte-identical: dll100 balanced 89→95-99 MiB/s (+7-10%, quiet-gated interleaved rounds, x1.70→~x1.55), matrix json.balanced 140→159 (x0.86→x0.758), text.balanced ~flat (496 vs ~505 band on the current corpus). Note the model interaction: the walk itself is latency-bound on dll (L1-miss samples track cycles, 65.4% of misses vs 64.9% of cycles in the scan), yet the emit convention cost was real wall — per-sequence straight-line work between walks does not hide under the chase latency the way per-step operands do.
- **two-phase seeded/steady split of the chain scan loop (2026-09-13)**: the same macro split the fast/dfast loops already have — the job-start state (`seed_offset`/`seed_hits`/`seed_budget`, `rep_pending`) only winds down, so the steady instantiation constant-folds all four (plus the rep-gate branches) out of the body. Byte-identical (full-ladder dump); gungraun balanced Ir json/text/skewed −0.38%/−0.78%/−0.44%, random/zeros unchanged; wall flat on every shape (json solo 86-88, text 2981-3218, skewed 1933-1961, dll100 106-107; interleaved 87/x1.395 and 2826/x0.598, at parity with the recorded 87/2818). Kept for structure and the deterministic Ir cut, but it **falsified the spill-relief theory**: the four live values were supposed to be why the walk's params (win_base/pos_abs/reach/depth/mask) sat in stack operands — removing them only reshuffled allocation. Post-split profiling closed the model: cycles AND L1-miss samples concentrate in the same two steady walk copies (**82% of cycles, 93% of L1 misses** on json 32MiB), the instruction-level profile is flat (hottest single instruction 2.5%, the old "~7% beat-check loads" concentration died with the outlined closure), and ~66 cyc/walk-step ≈ one serialized L3-class load — the walk is a **latency-bound chain-link chase** (4MiB chain table + beat-check reads at 1-2MiB candidate distances), the per-step stack operands hide under the load latency. Ir-side work on this loop no longer converts; the remaining levers are latency-side (see todo 5).
- **cross-position pipelining of the lazy walk's hash+head read (2026-09-14)**: each chain search paid one more serialized L3-class load — the head read — in front of its walk. The scan body now issues the pos+1 hash+head before the incumbent walk (the dfast ip0/ip1 pattern); the lazy walk consumes it at the first search and each search refreshes for the next under the walk's shadow. Two invariants carry the correctness: no table writes happen inside the lazy walk (a pre-read cannot go stale), and every path to the next search steps exactly one position (the refresh is always the right one; its guard equals the next attempt's break condition, so a skipped refresh is never consumed). The scan-body insert's newest-wins write is the one exception, fixed up on a slot collision (`ph == h` → `pack_pos(pos)`); the depth-0 rep probe advancing pos (first search at pos+2) and the block tail fall back to fresh compute. **Issue placement is load-bearing**: the same pre-reads issued after the incumbent walk (gated on the lazy walk actually running, killing long-match waste) kept only +0.5% dll and flipped json to −2% — loads placed after a loop whose exit branch predicts poorly decode behind it and lose the shadow; OOO overlap does not rescue bad issue placement across a data-dependent loop exit. A dual pre-read (pos+1 and pos+2, covering the rep-advance case) took dll +4.5%/json +2% but cost text −2.5% wall from the scan-body code presence alone (gating the waste off did not recover it — layout); the landed single pre-read keeps dll +4.5% (105-107→110-113 solo), json +1.5-2% (89→90), text +1.4% (3251→3295 6-pair solo; interleaved matrix text +2.5%), paying skewed −2% (the wasted loads on its long-match iterations: +18% RAM hits at +0.5% Ir). Ir is up json/text +2.6% — the issued-early loads are the cost, the hidden latency the pay; a latency-side lever exactly as the walk model ordered. Byte-identical full-ladder output.
- **gungraun's 1MiB slice under-measures the chain tiers (2026-09-13, model input)**: json.balanced measures ~100 Ir/B on the slice but ~463 Ir/B on the full 32MiB file (4.6×) — the 2MiB window never fills in 1MiB, so walk depth and the far-candidate regime barely appear; Ir deltas on chain tiers validate instruction-count changes only, wall conclusions need the full corpus (`prof`/`matrix`). Corollary: the chain-walk rework's −18% Ir bought 87 vs 80 MiB/s json wall, and the split's −0.4..−0.8% bought nothing — the conversion rate was set by the slice/full-file regime gap all along.

- **cold-start DUBT head (2026-09-15)**: row 9's frames parse the first `HEAD_LIMIT` (1.125 MiB, 9 blocks) through the btlazy2 driver before the chain takes over, closing the text.Balanced deficit (todo 9): the whole deficit lived in the first cold-start tile, where the chain's newest-first selection accepts nearer-shorter candidates (ml 15@212 vs the tree's 18@809) — exactly the span where the DUBT tree's oldest-first order wins, and the chain parses at parity after it. The head rides the existing btlazy bridge at `bt_knobs(4, 20)` with an H20 heads table (24 MiB, pooled across frames); the handoff sets `gap_start` to the frame start so the chain's `catch_up_insertions` dense-indexes the whole head region into its tables before the next scan probes — later blocks match into the head at full resolution, no grid sparsity. Three gates keep the head where it wins: only genuine frame starts (reset, or mt job zero's empty strip — `prefill_window` disarms it, so dictionary frames and strip-warm jobs never run one), only declared-or-unknown lengths ≥ 4 MiB (a smaller input would be all head; small payloads keep the chain parse json-class selection favors), and an alphabet gate — ≥ 48 distinct bytes in the first parsed block. The alphabet gate is load-bearing twice: small-symbol data collapses the tree's batch sort at any search depth (skewed, 16 syms), and the in-corpus json is 39 distinct bytes — both stay on the pure chain parse, byte-identical in every mode. Knob data (text bulk-st, vs zstd-9): the search depth is the whole value — S4 −0.32→+0.70% at 768 KiB, S3 −0.32%, S2 −1.97%, S1 −4.36% (worse than no head: a shallow tree both parses worse and hands the chain a worse rep history); span 9 blocks/S4 = **+2.24%** (denser than the full-DUBT row's +0.45% — the chain tail beats the tree tail on tiled repeats). Results (regenerated corpus, whose text.raw tiles the c1dfb42 sources — ratio 138.5 for zstd-9, not T6's older 378-corpus): text.balanced −2.49→**+2.24/+2.26%** (st modes), mt cells −2.73→**+8.82/+8.83%** (job zero's head covers the entire per-frame deficit; strip-warm jobs were already at parity), json +19.46/+19.06 and skewed +8.83/+8.77 byte-identical to the chain-only build (same-bytes gate check); dll100 −0.07% size at parity speed (binary cold starts mildly prefer the chain's literal-aware selection — the price of the text flip). Full 120-cell ratio gate: geo-mean +3.71%, no losing cell outside the pre-existing item-9 list (worst json.opt −0.31%). Speed: the head is a bounded per-frame cost — text interleaved 1324→531 MiB/s (x1.10→x2.74 vs zstd-9; ~22 ms DUBT parse + ~6 ms handoff re-index per 32 MiB frame, amortizing on larger files), text solo 1305→~505; json 91 (x1.341, unchanged), skewed ~2010, dll100 106-108 all untouched. The text speed cell is the trade: the tier's speed identity is anchored by json/dll (the slow cells), and text at 531 MiB/s remains an order above the best tier's slow shapes. Refreshed 2026-09-17 by the probe-side cuts (next section): the head now parses ONCE per frame (the probe's keep and shrink parses previously re-ran it — three btlazy parses per text frame, ~40 of the ~61 ms machinery), and a shrunk frame never runs it; text.balanced measures 867 st / 701 mt8 MiB/s at unchanged output.
- **LDM candidate source for row 9 (2026-09-15, `encoding/ldm.rs`)**: a port of libzstd's gear-hash long-distance matcher (`zstd_ldm.c`) as an *extra candidate source* for the chain scan — not C's raw-sequence skeleton: a split whose 64-byte window matches a retained twin ≥ 64 bytes forward injects `(split, offset)` and competes with the chain's own candidates on plain length (the store gate and lazy walk then price the far offset; backward extension is recomputed live by the scan's shared path). Deviations from C: `pack_pos`-biased u32 entries (the chain-table idiom, so an unwritten slot resolves out-of-range instead of aliasing position 0), the rolling state carried across blocks (C re-arms per 1 MiB chunk; the arm position is the entry floor), and table+state restart per frame/job (pooled-matchers determinism). Row 9 ships window W26 / chain reach W22 / H21 / C20: LDM owns exactly the offsets beyond the chain's reach (the scan-side filter keeps only those — nearer candidates are the chain's class, and probing them measured as pure interference).
  - **dll100 (regenerated corpus, baseline 23,482,813 @ 112 MiB/s)**: W26 reach lands **20,804,735 (−11.4%)** unlatched; the shipped latch/canary keeps **21,186,488 (−9.75%) @ 106 MiB/s**. zstd -9 reference 23,781,504 (−12.5% ahead), zstd -9 --long=26 20,122,786 (−3.4% behind C's own long mode). Reach ladder: W23 −1.54%, W24 −4.6%, W25 −5.45%, W26 −11.4% — the 32-64 MiB distance class (214K sampled 64 B repeats) is the payload. Chain reach cannot shrink below W22 for this shape: reach W20 costs +4%, W21 +9% (the dense 1-4 MiB class is chain territory; LDM's split-sampled candidates cannot replace it).
  - **Corpus anatomy kills the json premise (todo 5)**: json.raw has *zero* exact 64-byte repeats anywhere (verified at 4-byte sampling); its far class is 32-63 B fragments — below LDM's window — and net-negative (supplying them via minMatchLength 32 made json *worse*: +13 KB at equal reach). The chain-reach shrink curve (W22 4720677 → W21 4712971 → W20 4697535 → W19 4683645 → W18 4670158, all with LDM on, json solo 112→145 MiB/s at W18) is byte-identical with LDM *off* — the far chain candidates were the ratio loss, and text's tiles ride rep0 chains (identical bytes at every reach). The shrink lever is recorded in todo 5 as chain-only follow-up; LDM does not carry json.
  - **The split pass cost ~9 cyc/B at landing** (gear chain ~2.5 + 64 B xxh64 per 64 B + two random-write arrays), so always-on LDM taxed the corpus cells −15..−30% (json/text/skewed, where it never wins). **Cheapened 2026-09-15, in three cuts** (split-pass self share 8.4%→5.3% of encoder cycles, dll100 balanced −3.9% instructions, solo 90.5→93.5 MiB/s median of 8 interleaved rounds; latched 21,186,488→21,180,504, unlatched 20,806,075→20,797,673, corpus 120-cell byte-identical): the window fingerprint is an **8-lane multiply-xor fold** (`split_fp`: eight independent u64 lanes × odd constants, one avalanche multiply; false checksum matches 0.17/kilo-split vs XXH64's 0.015, twin retention equal — a fraction of XXH64's serial-multiply latency, and trigger-independent, which matters: the fold-derived variant that reuses the gear state is falsified below); the **gear chain runs re-associated 4-byte groups** (`gear4`: checkpoints `(h<<k) + pk` with group-local prefix folds `pk`, exact mod-2^64 so splits and folds are bit-identical — one dependent add per four bytes instead of per byte; the re-association is asm-pinned because every source shaping reassociates back into the one-byte serial chain with its table loads folded into memory-operand adds); and the **bucket checksum scan is AVX-512** (two shifted 8-lane compares replace sixteen scalar load-shift-compare steps, ascending hit order keeps the first-longest tie-break). Attribution lesson: the removed compute was largely latency-hidden — the xxh64 cut alone moved instructions (−4%) but not wall (the pass is bound by the random table traffic); the wall came from the gear chain's serial adds and the scan's op count. The armed-but-never-winning tax on json 32MiB (forced arm, no latch) dropped 16.4→14.0 I/B in instructions, cycles flat — on a chain-latency-bound shape the split pass hides under the chain walk's misses either way. The shipped configuration: a **size gate** (now an arming context, `LdmArming` — see the mid-size extension below: the full W26 window for the frozen W26 population and MT jobs, a clamped ≥32 MiB window on frame-continuous paths), a **quiet latch** (8 winless blocks past the reach horizon shut generation off; any depth-0 LDM win resets it), and a **canary** (one block per megabyte re-generates while latched; a canary whose beyond-reach candidates exist revives — far-match density ramps up mid-file on dll100, where the latch's first kill cost 1.6 MB until the canary at ~6 MiB). Residual corpus tax is layout-band noise (json −2..−6% solo; text/skewed ±5%); the 120-cell ratio gate is byte-identical to the chain-only build everywhere.
  - **MT**: the job strip decouples from the frame window (`strip_for_level` = chain reach) — a full-window strip would floor job size at 64 MiB and starve parallelism; LDM state restarts per job (strip fill) so outputs stay worker-assignment-independent. Gated/RLE/DUBT-head blocks LDM-fill (state stays fed, bytes stay far-matchable history).
  - **Mid-size gate extension (2026-09-15)**: the size gate became an arming *context* (`LdmArming`) instead of one window bar — frame-continuous paths (bulk and single-threaded streaming) arm at a clamped window ≥ 32 MiB, multithreaded jobs keep the 64 MiB bar (unclamped row window; the prefix-strip class below excepts), and the reach probe's parses never arm. Results: **dll32 bulk-st 5,462,510 → 4,384,966 (−19.7%) at +6-8% speed** — the full always-on figure survives the latch/canary (680 B short of the unlatched 4,384,286); dll100 st+mt and dll32-mt byte-identical (the W26 population and the MT job path are frozen); the 120-cell corpus sweep byte-identical. Four load-bearing rules keep the corpus at its chain-only speed:
    - *Shrunk frames abandon LDM*: the reach probe decided near-locality from parses LDM cannot influence (a probe span cannot surface a candidate past the keep reach, and the shrunk configuration now never arms), so the measurement and the executed parse stay the same object. Without this the probe's shrink parse carried LDM candidates at 4 KiB-2 MiB offsets and taxed skewed/text solo −25..−35%.
    - *An alphabet gate on the mid-size population* (`LDM_SYMS_MIN` 128 sampled distinct bytes at the first parsed/filled block): low alphabets (skewed 16, text ~102, json 40) produce structural 64-byte repeats whose far twins never survive their offset price — candidates exist, transiently out-length the chain incumbent at injection, reset the quiet latch, and never emit; skewed solo measured −35% before the gate, −1.5% (layout band) after. Binary content (256 symbols) clears the bar with margin. The W26 population skips the check — its bytes are frozen.
    - *Skipped blocks stop filling* (mid-size only): incompressibility-gated and RLE blocks never reach a scan, so the quiet latch cannot fire through them — random (all gated, alphabet 256) paid −53% solo in gear fills whose max-entropy windows can never match; head blocks keep filling (the far class sources the frame start through them). dll32's size is unchanged by the exemption.
    - *The bulk-MT capture (2026-09-17, `LdmArming::JobPrefix` + the shared prefix fill)*: dll32-class frames (chain row, Keep verdict, clamped window in [32, 64) MiB, a wide-alphabet head that parses — `ldm_head_parses`, the gate-probe evidence class as an engagement screen) run their mt jobs on whole-frame-prefix strips: the grid keeps the reach-based job size (a 32 MiB window floor would starve parallelism) while each job's strip covers the clamped window — exactly the history the frame-continuous path holds at the same position, so the mid-size bar arms for the jobs and the far class survives the job split. The per-job whole-prefix fill tax (why `Job` sits at the 64 MiB bar) is paid once: the calling thread builds the median prefix boundary's fill while the below-median jobs encode (segmented at LDM batch-freeze points, `build_prefix_snapshot` in `encoding/mt.rs`), tail jobs wait on the build and adopt its snapshot (`adopt_strip_snapshot` + remainder fill, the r8-strip rules), and a build that overshoots a job's start leaves it stock. dll32 bulk-mt **5,474,537 → 4,390,255 (−19.84%)**, mt4 == mt8 bytes, ≡ stream-mt's 4,391,708 and 5.3 KB off bulk-st; +7% wall on the engaged class (the split pass over the prefix strips; st parity holds at 121 vs 120 MiB/s) and no tax elsewhere (dll100 w4/w8 and text-mt walls in-band, 120-cell sweep and full-ladder dump byte-identical). The gate-probe strip seeding is load-bearing: without it a fresh worker's incompressibility gate kills the far class before LDM sees the block (the synthetic far-repeat test caught it as an mt4/mt8 byte split).
    - *MT stays at the 64 MiB bar*: the per-job table restart makes far candidates strip- and own-span-sourced only, and the strip fill is an unconditional per-job split pass that far-less shapes pay in full — corpus mt8 would tax 25-90% while dll32-mt8 (4 MiB jobs) could at best cover the 4-8 MiB distance class (dll100-mt already loses 1.29 MB to the same strip truncation). dll32-class MT capture needs a far-class-preserving job model (bigger jobs or cross-job LDM history), left open.
    - dll32-mt reference points (2026-09-18 re-measure): bulk-mt8 4,390,255; unpledged stream-mt8 4,390,251 — bulk's parse less the 4-byte FCS-less header. The earlier 4,391,708 reference does not reproduce on any landed build back to the r8-strip merge (a dev-build artifact), and the todo-11 open question it fed is closed: the unpledged full-window W26 semantics **dominate** bulk-mt at every dll scale (dll16 2,929,187 vs 2,937,032; dll100 20,869,254 vs 23,459,589 — the whole-window strips carry the far class cross-job), so the mid-size clamp stays off there; engaging it below 32 MiB would disarm LDM outright (bulk's semantics) and hand dll16-class streams back ~7.8 KB.
    - *The pledged stream-mt port (2026-09-18, todo 11)*: a pledged keep-class frame whose clamped window lands in the capture's class re-grids at the reach verdict onto the bulk schedule (`engage_midsize_capture` in `stream/encoder_mt.rs`): reach-based job size (the whole-window stream strip had floored such frames at one job), whole-prefix strips (the stream overlap already is the clamped window) and `LdmArming::JobPrefix` — the clamped window sits below the `Job` bar, so without the flip the far class disarms and the pledged stream paid a chain-only single-job parse where bulk-mt captures it: dll32 pledged stream-mt 5,460,884 → **4,390,255** (mt4 == mt8 == bulk-mt, byte-identical — the pledged-equals-bulk contract now holds on the class; the per-job prefix fills stay stock, bit-identical to the bulk shared build). The `ldm_head_parses` screen mirrors the bulk planner's, so low-alphabet heads keep the stock single-job schedule (their LDM dies at the alphabet gate anyway — only the job split would move). Unpledged and non-capture cells untouched.
  - Re-gated on the merge with the cheapened split pass: dll32 st stays byte-identical at 4,384,966 (the fold fingerprint picks the same far twins on this shape); dll100 st re-measures 21,180,508 via emitframe — the −5,984 fingerprint effect reproduces exactly, the few-byte absolute offset vs the numbers above is measurement-context drift (same class as the unlatched 20,804,735→20,806,075 note); corpus 120-cell sweep byte-identical.

- **shape-adaptive chain reach — prefix dual-parse probe (2026-09-15, `encoding/reach_probe.rs`)**: row 9's stock reach W22 serves the dll class (its dense 1-4 MiB distance class is chain territory — reach W20 costs +4%), while near-local shapes (json logs, tiled structured data) pay twice for it: the far-walking chain takes 2.3× the time and its far matches displace nearer ones whose repcode history the following positions would reuse — a net ratio loss (the regenerated corpus's json is monotone in reach, W12 optimal: 4.69M vs W22's 5.22M at 174 vs 77 MiB/s solo). No static reach serves both classes, so the frame decides from its own head: before the first block is matched, the first 2 MiB are parsed twice through the row's own pipeline (DUBT head riding its alphabet gate, LDM its size gate, only the reach differing) and the cheaper parse wins the frame — a pre-entropy cost (order-0 literal entropy + 15 bits/sequence + packed add bits) that separates the classes by 8% at a 0% noise floor (text/skewed prefixes byte-identical at every reach); shrink needs a 2% edge. Engagement: only row 9 with its stock reach unclamped by the frame's shape, only declared lengths ≥ 8 MiB (the two probe parses are a fixed ~36 ms tax), and only entry points that see the frame's head before the first block — bulk single- and multithreaded (probe decides the MT strip/overlap too, so bulk-st/mt stay byte-consistent: same input, same decision), and streaming (the ST core stages 2 MiB before its first block). **The stream-mt core landed (2026-09-16)** as a deferred staging gate: a pledged stream decides at construction (the pledge is the head), an open-ended one holds jobs until the frame holds 8 MiB and probes that span; a flush or finish ahead of the gate keeps the stock reach, and after a Shrink the Fixed schedule re-grids against the shrunk overlap (`job_size_for`). A pledged Balanced stream on a shrink-class head stays byte-identical to bulk mt (shared strip, job floor and reach); kept frames keep the stream core's whole-window strip. The unpledged ST core stays excluded — its blocks flow at block size, so small-pull consumers never assemble a head. The determinism of the deferral itself surfaced the chain walk's last landmine: the stream pool's persistent worker threads reuse their `CompressState` across jobs of one frame, and the rep1 covered fill's link-less head inserts let a walk read a previous job's chain slot — the job bytes varied with the worker-claim order (fixed the same day: every chain-strategy covered insert now writes its link; see [pitfalls](../pitfalls/encoding.md)). Results: json −1.14% st / −1.39% mt size, solo 76→140 MiB/s, matrix json.balanced x1.34→**x0.86 vs zstd-9** (ahead at +21% density); dll32/dll100/skewed/text/zeros/random byte-identical everywhere (dll100 106→101 MiB/s — the probe tax is ~5% on 100 MB); full 120-cell ratio gate: no cell moved outside the known residue list. The tax on the fastest keep-shapes is the design's price (skewed solo 2010→1703 MiB/s): in-loop far/near discriminators are falsified (near-twin displacement counting, far-search frequency — [negative](../negative/matchers.md)), only a real second parse measures the trade.
   **The tax is dismantled (2026-09-17, three cuts; output byte-identical across the full-ladder dump, the 120-cell sweep and emitframe cross-checks at workers 4/8)**:
   - *Shrink side headless*: a shrunk frame never runs the DUBT head (the shrink class IS the chain-selection class — json's 39-symbol gate already kept it headless); the probe's shrink parse and the executed shrunk parse are the same object. The head-in-shrink-parse was the single most expensive item on text (18.6 of the 26.7 ms shrink parse: a W12-domain tree parses *slower* — its worse matches rescan more).
   - *The gate in the probe*: `parse_cost` now calls `skip_if_incompressible` between the uniform check and the parse — it was missing, so the "measurement" fully parsed blocks the frame emits verbatim. random paid two ungated 2 MiB parses per frame (the 09-16 random.balanced 2317→1220 regression, now repaid: 951→2076 MiB/s interleaved).
   - *Keep side donated*: bulk-ST parses the first span blocks through the real pipeline (feedback, LDM, gate — the probe driver re-parses nothing) while the matcher accumulates the cost off the parse (`probe_stats`); bulk-MT hands the same state and encoded prefix to job zero — a donated continuation must skip the strip prefill, which clears the tables the donation built (skewed's emitframe showed a −4 B divergence before that fix). Only a Shrink verdict re-parses: `restart_shrunk` + `reset_slice_state` rebuild the state — the keep parse's same-bytes table residue would alias a fresh parse (cross-frame pool residue is byte-verify-arbitrated; same-bytes residue is not).
   - *The verdict's feedback calibration*: the donated keep cost carries the entropy feedback — worth −4.75% on json's model cost, more than double the shrink margin, so the flat comparison the margin is calibrated on is reconstructed. `decide_donated`: degenerate ties (bit-identical costs — skewed/random/zeros) keep outright; a shrink already ahead of the true keep cost is contained in the flat verdict; a landslide (+25%) keeps; the contested band converts the true keep cost back to the flat scale through the feedback gain measured on a twice-parsed 512 KiB prefix (Shannon lengths stand in for the encoder's tables; the approximation reproduced json's full-span gain within 0.4%).
   - *No realloc on reach flips*: `apply_level` re-derives tables only on table-shaping changes (strategy/logs/window — sizes are reach-independent). The reach flips three times per probing frame (keep, shrink, executed) and the pooled probe driver alternates across frames; each flip was a 12 MiB realloc-plus-first-touch. The flip sites clear residue explicitly (`clear_parse_tables` in the probe, `restart_shrunk` on the frame state).
   Results (32 MiB): text.balanced bulk-st 499→**867 MiB/s (x3.47→x2.0, n=65 interleaved)**, mt8 444→**701 (x2.81→x1.81, n=48)**; skewed solo ~1390→1750-1850 (the residual over its 2040 noprobe ceiling: the shrink-side parse + model overhead ≈ 2 ms — the second parse is the probe's irreducible core); random 951→2076; zeros ~9.3k→17k; dll100 +3% (the probe amortizes over 100 MB); json −5% st (a shrink-class frame pays the donated keep parse it then discards; it is ahead at x0.62 mt / x0.9 st regardless). The ST stream core keeps the stock probe (its cross-frame-carried entropy tables cannot be mirrored by an in-place shrink restart) with the two cuts. **The stream-MT core donates (2026-09-17, r7)**: at its staging gate the pump posts the keep side to a pool worker as job zero's own span blocks and the shrink side to a second worker concurrently — a serial donation measured keep 16.3 + shrink 15.3 ms, more serial time than the cell's tail can hide on one worker — and the verdict is consumed lazily where the schedule first needs it (a due post, or flush/finish), overlapping the pump; a process-global kit pool keeps the donation's state and probe driver warm (ephemeral per-encoder pool threads measured 4-5x the warm parse cost on cold tables). The verdict itself runs on the pump (its contested-band feedback reuses the thread's pooled driver). text.balanced stream-mt8 366→445 MiB/s (x3.28→x2.16) at json ratio parity (x0.33; the discarded keep parse hides under the pump), byte-identical output. The cell's residual against its ~700 bulk ceiling is NOT the probe: an unpledged row-9 stream posts nothing during write (the growing grid's epoch 0 spans 512 MiB at the whole-window strip), so at finish 8 tail jobs prefill strips [0,4),[0,8)…[0,28) MiB — 243 ms summed (jobdecomp), ~50 ms critical on the last job vs bulk's flat 4 MiB strips; those strips are the cross-job LDM far-class carrier (byte-load-bearing), so the lever is fill speed or a worker-state pool, not the probe (a no-probe toggle build measures the 549 MiB/s / 58 ms floor).

## Best (btlazy2, counterpart of libzstd levels 13-15)

- **u32 entries + the skewed.best cost model (2026-09-15)**: the stubborn x2.87 skewed cell is a memory-footprint artifact, not an algorithmic one. Taint-free attribution (perf + instrumented event counters on the 32 MiB corpus): 83% of cycles sit inside `find_all_matches` at IPC 0.44, with 200M L1-dcache and 100M dTLB load misses per pass over a 96 MiB ring+heads working set — the u64 entry form doubled libzstd's 48 MiB (U32 `chainTable` + U32 `hashTable`). The event table per pass: 19.4M searches, 14.1M filled, 12.5M chain-walk steps and batch sorts (26.4M sort descents), 49.8M sorted-descent steps, 76.2M `count_from` calls averaging 4.5 verified bytes — ~460 cycles per tree visit, i.e. a serialized ~DRAM-latency load per step while the compares themselves are trivial (16-symbol data makes every 5-gram bucket ~14 entries deep, so every search walks and sorts a backlog). The port truncates positions to u32 and rebuilds them against the scanning position (candidates are window-bounded, the high bits are implied; a stale entry from an earlier 4 GiB period rebuilds as an in-window phantom that dies on the range check or the byte verify — the same accepted class as the chain tables). `POS_BIAS` stays 0: position 0 reads as the null link and 1 as the unsorted mark, the identical sentinel collisions the u64 form carried, so the full-ladder dump stays byte-identical; a +2 bias (libzstd's `ZSTD_WINDOW_START_INDEX` reservation) measured +543/+97/-224 B on json/text/skewed and was not taken. Best-tier footprint halves to 48 MiB (×MT workers). Measured solo perf-stat on skewed: cycles 41.2G→26.3G, dTLB misses 99M→62M; interleaved matrix skewed.best x2.865→x2.16-2.22, json.best x1.627→x1.38-1.50, text.best x1.184→x0.82-0.94 (ahead of libzstd for the first time).
- **literal-scale hoist in the btlazy2 selection (2026-09-15)**: every candidate of one search prices its length against the same four `lit_lens` bytes (the position is fixed), but `lazy_value` re-gathered them per candidate behind the matches-array loop and LLVM does not hoist that. Split into `lit_scale` (per position) + `scale_value` (per candidate): driver Ir 4.50G→3.48G on skewed (-23%, against libzstd's 1.12G for the same events — the remainder is the literal-aware pricing itself, the +2.5% density mechanism), whole-encode Ir -8.2%; output byte-identical.
- **selection fused into the finder (`find_best`, 2026-09-15)**: the selection no longer collects the improving set into the shared matches array — the descent prices each strictly improving candidate where it is found (value-minus-offset-price against the position's literal scale) and returns the two argmaxes the lazy loop consumes (`Found`: longest repcode at price 0, best non-rep by score). Reps precede tree candidates in collection order, so the driver's tie rule (`rep_v >= cand_score` keeps the rep) reproduces the array loop's first-strict-max argmax bit-for-bit. The driver drops its per-candidate loops and three of its four per-position `lit_scale` gathers (the incumbent recompute, the best-position recompute — the score now carries across the rep-probe override); a per-block clamped price table (`min(len,6)` folded once, so `lit_scale` is four L1 loads and three adds) replaces the per-gather clamps; the x4 lazy arm folds its `/4`s away (`v*4/4 == v` exactly). Output byte-identical (full-ladder dump); whole-encode Ir json 9.58G→8.85G (−7.6%), skewed (same-methodology toggle view as the hoist entry) 3.48G→3.05G; wall json.best x1.450→x1.380-1.392 (interleaved n=9-16, quiet windows; a co-tenant window re-measured x1.45 at ±0.04 — the machine-state caveat the matrix carries), text/skewed flat (skewed is the latency-bound cell).
- **the remaining json.best gap is per-event codegen + event volume, not pricing (2026-09-15, instrumented event counters on both sides, json 32 MiB)**: ours/C — searches 6.84M/5.67M, fills 26.8M/27.9M, unsorted-walk steps 12.5M/6.1M, batch-sort steps 33.0M/25.9M, sorted-descent steps 53.3M/31.1M, count calls 77.8M/57.6M. Events run 1.2-2.1× C (the denser parse searches 21% more positions and walks deeper bucket backlogs — parse-intrinsic), yet instructions run ~4× (whole encode 8.9G vs 2.2G CLI) at IPC 1.29 vs 0.46: our loop is throughput-bound, C's is latency-bound, so instruction cuts convert ~1:1 for us and hardly at all for C. The per-node differential is the 64-bit unpack arithmetic plus register pressure (the fused selection's live set spills loop state to stack operands); two restructurings to close it regressed (see negative.md) — the index form LLVM lowers today is a local optimum. Cutting events instead would cut the +2.5% density itself (more searches IS the mechanism), so no safe lever is identified; ~1.2-1.3× is the honest ceiling of this shape short of an event-side rethink.
- measurement pitfall this uncovered: `callgrind --toggle-collect` under-collects this function family — the previous "~2.4G Ir" pricing attribution (hoist entry above) came from a toggle-limited view that missed most of the inlined finder body; the array round trip the fusion removed was really 0.7G of a 9.6G encode. Cross-implementation cost models need whole-program callgrind or perf-stat instruction counts.
- **DUBT finder (2026-09-14, `encoding/dubt.rs`)**: a faithful port of libzstd's doubly-unsorted binary tree (`ZSTD_updateDUBT`/`ZSTD_insertDUBT1`/`ZSTD_DUBT_findBestMatch`), replacing the retired shallow-insert architecture below. The fill pays **three stores per position** (hash-head update, chain link to the previous head, unsorted mark) with zero compares; sorting amortizes into the searches — a search walks its bucket's unsorted chain (reversing slot[1] into a backtrack link), nullifies the chain's far end (libzstd's deliberate ratio-for-speed cut), batch-sorts the walked nodes oldest-to-newest with one budgeted descent each, then descends the sorted tree collecting strictly improving candidates (C returns one best; our literal-aware selection consumes the whole improving set, worth ~+0.5% over zstd-13 at equal knobs). Match interiors and stepped-over positions never pay beyond the fill — that is the entire speed point over a fill-time-sort tree.
- slot discipline carries the soundness: `[0]` is the chain link while unsorted and the smaller child once sorted, `[1]` the mark and the larger child; children are always older than their parent and every sorted node's slots were written by a completed descent, so a descent never crosses an unsorted node. Our `UNSORTED` sentinel is bit 63 (positions stay < 2^47), so unlike C's value-1 mark it can never collide with a real entry. Entries are raw positions (no epoch tag): job prefill clears the heads only (the strip fill rewrites every ring slot — strip = full window = full ring), and the per-frame reset clears both tables.
- **the collection cap must be block-scale, not the row's targetLength**: an early-out at `sufficient_len` (32) stops the descent's threading with the walk, and on match-rich data the tree starves — the tiny-vocab ladder synthetic collapsed to 28110 B (vs L9's 24142) until the cap moved to `OPT_NUM` (4096) like the optimal parser's own walk: 19390 B, past even the old Keep-retention rescue (22761). json moved 5472046 → 5364906 the same edit.
- **lazy2 selection loop** (port of `ZSTD_compressBlock_btlazy2`): each position's repcode and tree candidates compete on value-minus-offset-price, a two-deep lazy walk chases a better start under libzstd's alternating margins ((rep x3, +1, +4) then (rep x4, +1, +7)), literal-offset matches backward-extend into the pending literals, and the offset-2 chain follows every emission. Replaced the tier's old low-config optimal-parser rows (json x4.22/skewed x6.22 — an algorithm-class mismatch).
- **literal-aware lazy margins** (the chain tier's measured win, ported): displaced literals price at the previous block's fed-back huffman code lengths (`lit_value`, clamped to 6 bits/byte; flat 4 bits before the first feedback = libzstd's raw `ml*4` scale bit-for-bit).
- retired with the DUBT port: the 2026-09-13/14 shallow-insert architecture — the opt tree with a split `insert_log` budget, `FillTerm::Keep` link retention and the STALE/taint recount machinery (the tiny-vocab ladder rescue). The DUBT tree beats the rescue outright (19390 vs 22761 B on the synthetic); the one measured loss is json's retention edge (ratio 6.27 → 6.25), traded for +24-52% tier speed.
- rows 13/14/15 ship C's exact S4/S5/S6 ladder (H22/H23/H23); the old json edge of the Keep tree (+2.2% over zstd-13) was retention-only — far candidates beyond the walk cap — and is deliberately not ported: S5 deepening buys no json ratio (5472469, flat) for −24% speed, and keeping the frontier links on budget-exhaustion buys +0.05% for −12% (both measured, not taken).
- **LDM via the gap-parse model (2026-09-15)**: rows 13-15 take the far class exactly as `ZSTD_ldm_blockCompress` does for the lazy family — each long-distance candidate is emitted **wholesale at its split** and the lazy loop parses only the literal gap ahead of it, bounded there like C's per-gap compressor call (`finder.block_end_idx` retargets per segment; match extensions, the lazy walk and the offset-2 chain all stop at the gap end). The seam carries C's `ZSTD_ldm_limitTableUpdate`: the lazy tables catch up at most 512 bytes behind an emission, so the candidate's interior stays unindexed. A block with no candidates is one segment — the stock parse, byte-identical (row 9's frozen head included: it runs with empty seqs and never counts quiet). Results on the regenerated corpus: **dll32 st 5,302,808 → 4,236,108 (−20.1%) at 4.2→52 MiB/s; dll100 st 22,690,080 → 19,595,747 (−13.6%) at 3.8→28 MiB/s — within 0.02% of `zstd -13 --long=26` (19,591,714) and ~6-7× the tier's own chain-only speed** (the duplicated mass moves from tree scans to the gear-hash stream; whole-encode scanned bytes dll32 241G→6.7G). dll32-mt frozen (Job bar), dll100-mt4 −4.8% (strip+own-span class, like every LDM row). Corpus cells byte-identical except the l13 window-descriptor byte (W22→source clamp); skewed.best pays +5% wall (code layout around the segment scaffolding, instructions +2%), json/text flat. The depth-0 price-competition alternative (candidates injected into the selection like the chain row) is falsified in [negative](negative/matchers.md) — 2.1× slower *and* sparser; price competition stays in the opt parser, which visits every position anyway.
- measured (2026-09-14, interleaved): json.best **x1.627** (was x2.163) at ratio 6.25 vs zstd-13's 6.10; text.best **x1.184** (was x1.596) at 384.2 (−0.45% vs 385.9, was −2.0%); skewed.best **x2.865** (was x3.709) at parity; streaming: json.best stream ST **x1.69** (was x4.05), stream-mt8 **x0.93 — ahead of zstd** (was x3.31), text.best stream ST x1.79 (was x2.60); numeric ladder json l13/l14/l15 = 6.25/6.31/6.31 (x1.74/x1.61/x1.07). Full 120-cell sweep geo-mean +8.27% (was +8.23%); best mt/stream cells within +0.08% of ST (the head-clear + chain-rebuild prefill holds ratio). Levels 1/3/9/17/19 byte-identical (dump gate); 161 unit tests; 6 min ASAN fuzz clean on the new finder (the one crash found was a pre-existing fast-tier overread, fixed right after — see pitfalls/encoding.md).

## Opt / Ultra (optimal parser, full port of libzstd zstd_opt.c) `c726dfd`

- lazy-filled binary match tree (insertBt1 / insertBtAndGetAllMatches, epoch tags absolute positions); forward DP over stretches + fixed-point fractional bit prices (×256, highbit+linear interpolation approximating -log2(p)); cross-block frequency adaptation (rescaleFreqs decay + updateStats); btultra match+1-literal recheck; 2-pass first-block statistics seeding (the epoch bump invalidates the pass-1 tree).
- correctness-critical details: `ZSTD_count` returns a pointer difference (continuing a count must **replace**, not accumulate); rep history is updated **once per series** by the path traversal (per-sequence updates on the emission side are the decoder-equivalent semantics; pick one); tree-insertion counting is capped at 4096 within the DP window — otherwise regions skipped by the parser (long matches at far offsets) get recounted from stale heads, text 45→313 MiB/s.
- results: json Opt ratio 7.57 overtakes zstd-16's 7.28; json Ultra 7.52 vs zstd-19 7.49 (same-window 1MiB comparison: 7.53); text Opt 271.5 vs 272.0. Speed: json Opt ≈ zstd-16, text Opt 313 vs ~440 (behind).
- **windows W20→W22/W23 (2026-09-12)**: Best takes libzstd's L16 row (W22/H22/C22), Opt/Ultra the L17-L19 rows minus the ring slack (W23/H22/C23; Ultra hash3 stays 2^17). dll100 interleaved A/B: Best 37.06→21.57MB (**−41.8%**, 8.5% denser than zstd-12, −2% time), Opt 36.93→20.91MB (**−43.4%**, 4.0% denser than zstd-16, −16% time), Ultra 35.60→20.03MB (**−43.7%**, 7.7% sparser than zstd-19, −1% time). Unlike fast/balanced W21 (which got *faster* on dll), the opt parse pays real walk/DP cost for the reach — and on near-repeat corpora it pays for nothing: json solo Opt 3.77→7.44s (−50% speed) for −0.02% size, Ultra −43% for +0.28% size, Best −29% for +1.23% (the far-candidate offCode handicap penalizes exactly json's 1-2MiB tile repeats; the tier's json margins vs libzstd stay +11%/+5%/0%). The price model needed no change: our `match_price` is an exact port of `ZSTD_getMatchPrice` including the `offCode≥20` handicap, so libzstd's own L16/L19 wide-window semantics apply. 32MiB corpus sweep: text/skewed/random/zeros cells byte-level near-identical; the one real casualty is json.ultra.bulk-mt 4.50→4.61MB (per-job `opt_state` reset × 8MiB strips — see todo 9). Parameter probes: Opt W22 collapses the tier into Best (dll 21.43 vs 21.57MB — the 2-4MiB reach *is* Opt's product); Ultra C24 is **byte-identical** to C23 on dll — ring slots beyond the window floor can never resolve, libzstd's L19 C24 slack is dead weight. Table footprint per encoder (×MT workers): Best/Opt 96MiB, Ultra 160MiB — `alloc_zeroed` keeps gated/small-input paths untouched (4KiB ultra call 0.15ms, unchanged); 1MiB random ultra pays the first-block parse over sparser tables (41→58ms/call, random-tier residue).
- **Opt ring C23→C22 (2026-09-12)**: the speed half of the C22 probe above, landed after the json cost firmed up. Window stays W23 (the reach is the tier's product; only the ring shrinks): json solo 7.52→6.05s/iter (**−19.6%**), skewed 20.2→15.8s (**−22%**), text 440→457 MiB/s (+4%), dll100 −10% time for +0.16% size (20.909→20.942MB, still 3.9% denser than zstd-16); 32MiB sweep json.opt −0.04% *denser* (the offCode handicap already priced those far candidates wrong), text/skewed ±0.02%, geo-mean Δ over 120 cells +9.02% (worst cell still the pre-existing text.balanced.bulk-mt −2.79%). Ultra C22 measured too: json −18.6% for +0.22% dll — **not taken**, Ultra is the density flagship (see negative.md); table footprint Opt 160→96MiB.
- **fill-lag clamp — the opt family's block-boundary refill cut (2026-09-16)**: instrumented event counters against instrumented C (`zstd_opt.c` temp-built with matching counters) decomposed the opt row exactly: on json 4 MiB the two sides' events are **identical to the last digit** (fills 2,183,659; fill walk steps 33,867,570; searches 1,930,043; DP positions 4,191,082; series 9,939; emissions 136,500) — the DP fixed costs (price updates, rep loop, emit, per-block resets) are at parity, and json's whole x1.3 gap is per-event codegen. On text the events are identical on the search side but the **fill diverges 45×**: `next_update = match_end - 8` writes the last improving *candidate's* end, which on block-tiled data sits exactly one 128 KiB block back — every block's first search re-indexed the entire previous block through the tree (17.4M inserts = 52% of the file, 397M count bytes vs C's 46M; fill 60.7% of cycles; `ins_cnt_bytes` 8.6× C). C never pays this: `ZSTD_buildSeqStore` carries a "limited update after a very long match" clamp (`curr > nextToUpdate + 384 → nextToUpdate = curr - min(192, lag - 384)`) that the port had silently dropped. Landed in `opt::run_block` (applied at the real parse's end — the next block's start) gated three ways, each gate backed by a falsified ungated form: only the **alphabet-gated population** clamps (LDM armed then alphabet-disabled; dll100 keeps −0.65% of its size under the unconditional form — the lagged region's in-domain candidates pay when a far class is in play), **not on job/dictionary strip parses** (stream-mt text cells regressed +15-16 B: job-restart parses measurably use the lagged candidates), and never on the ultra seeding parse (its cursor is reset right after; C's `initStats_ultra` likewise bypasses `buildSeqStore`). Results: **output byte-identical everywhere** (120-cell ratio sweep, full-ladder dump, dll100/dll32/dll16 st+mt emits) and text solo l17 0.249→0.145 s/iter (x1.72 interleaved), l19 0.299→0.185 (x1.62); matrix text.opt **x1.4→x0.622** (733 vs 456 MiB/s — 1.6× ahead of libzstd-17 carrying the known −0.33% density residue), text.ultra **x0.706** (383 vs 270); json/dll/skewed untouched. Attribution notes for the record: the lag histogram separates the shapes only coarsely (text 131/135 blocks at lag 16K-128K; dll100 319/800 in the *same* bucket — no threshold splits them, the LDM-arming identity is the discriminator that works); the clamp placed at `run_once`'s tail cost json −3% wall (layout lottery), placed in `run_block` after the real parse it is layout-neutral.

- **u32 tree entries — the opt rows' per-insert cost cut (2026-09-16, todo 6's open lever)**: the fill-lag clamp and the tail-half tree strip left the per-insert latency as the fill's remaining cost — the u64 tables put a 96 MiB working set behind every opt job (heads H22 32 MiB + ring C22 64 MiB; ultra 160 MiB), the same memory-latency-bound class the Best tier's u32 conversion halved. The opt tables are now u32 like every other matcher table: entries are `(position + origin + 1) mod 2^32` on **per-state monotone coordinates** — `opt_origin` advances past every position the state has written at each invalidation point (the per-frame/per-job reset adds `last_pos + 1`, ultra's frame-start pass-1 adds `block_end + 1`), replacing the u64 form's 16-bit epoch with O(1) invalidation and no clears: a stale entry rebuilds as `cand = q - advance < 0` and dies below the window floor, live entries rebuild exactly (the true distance sits far below 2^32), and output is independent of the origin value, so worker scheduling cannot leak through. The 1-bit parity tag tried first is falsified in [negative](../negative/mt-stream.md) — the MT job path resets each pooled state twice (`take_slice_state` + `run_job_with`), which is invisible to a monotone epoch but neutralizes a 2-period tag, reviving the previous job's entries wherever adjacent jobs' windows overlap (dll32 l16 mt4 +332 B). Accepted edge, documented: past 4 GiB of cumulative positions on one pooled state the wrapped advance can resurface an old entry as a byte-verify-arbitrated phantom — the same class the u32 chain/fast tables carry at 4 GiB of stream. Footprint halves: Opt 96→48 MiB, Ultra 160→80.5 MiB (×MT workers). Output byte-identical: full-ladder dump (30 files), 120-cell ratio sweep, dll100/dll32/dll16 × l16-19 × {mt4, st, mt8} emitframes, all equal to the tagged build. Fill rates and walls: [mt-stream](mt-stream.md).
- **optLdm — the gear-hash far class for rows 16-19 (2026-09-15, landed with the btlazy2 row below)**: port of libzstd's optLdm. The frame window widens to W26 (C's `--long` shape) while the tree keeps its stock search domain through `chain_reach` (W22/W23); the split pass's beyond-domain candidates (`retain(offset > reach)`) feed both match-collection sites through a one-active-span cursor (`ZSTD_optLdm_t`'s model — a position inside a span is offered the remaining length; positions ascend, so one cursor serves the DP and the outer loop). `LdmSeq` carries the generate-measured span length for this; the chain consumer keeps its live re-derivation. Ultra's seed parse consumes the same seqs (absolute positions — job-boundary seeds are naturally unreachable). The arming/gating population widens accordingly: the alphabet gate applies to opt/btlazy rows at *every* window (a >32 MiB json must not skip it — json.opt/ultra would pay the split pass for nothing), Skipped blocks fill for the non-chain rows, the MT job strip is the tree domain (`stream_overlap_for` — a full-window job floor would starve parallelism for reach the per-job restart loses anyway), and `reach_probe_eligible` stays Chain-only (row 16's W22 domain equals `KEEP_REACH`). dll100 bulk-st: opt 20,941,888→18,780,148 (−10.3%), ultra 20,029,304→17,932,355 (**−10.5% — ultra flips from 0.8% sparser to 9.8% denser than zstd-19**); dll32 st opt/ultra −14.9%/−15.0%; bulk-mt gains the strip+own-span class only (opt mt4 −3.3%; dll32-mt frozen at the Job bar); speed neutral on dll100/dll32/json.opt (the alphabet gate holds the far-less shapes). zstd-19 --long=26 makes 16,821,475 — still 6% ahead of us there (its 128 MiB tree); corpus 120-cell sweep byte-identical, l16/l19 dumps differ only in the window-descriptor byte.

- **RLE-frame fixed costs — the zeros.balanced/best regression family (2026-09-17, todo 12)**: the 09-16 matrix's 2.9×/5.1× zeros regressions (46,548→16,080 / 43,534→8,515 MiB/s enc-st) were **not** the suspected rep1 covered-fill linking (`677684c4` exonerated: `insert_covered` long-match fills insert two anchors regardless, and a link-less revert moved nothing). An RLE-class frame's encode is O(1) per block — one `is_uniform` scan (~40 GB/s) plus an RLE block header — so any per-frame O(tables) cost dominates it while amortizing invisibly on real shapes. Perf attributed three, all landed 09-15/16: (1) **the DUBT rows' eager per-reset clear** — `reset()` zeroed both dubt tables (48 MiB at l13) every frame because "u32 entries carry no origin"; `dubt_stale` now defers the clear to the frame's first *searching* btlazy block, so all-RLE/all-raw frames pay nothing and every frame that searches sees exactly the same clean tables (zeros.best 9,984→41,143 solo with the clear toggled off; matrix 8,515→42,918); (2) **the reach probe's same-size table re-zero** — `apply_level` keyed its table realloc on `params != self.params`, and the probe's Keep/Shrink `chain_reach` alternation (a walk bound, not a table size) re-dropped and re-zeroed the chain family on every parse (observed as glibc calloc memsets on dirty-recycled 8+4 MiB chunks under `apply_level`); the realloc now keys on table *lengths*, and kept tables carry the previous parse's position-guarded, byte-verified entries — the same accepted residue as the equal-params path (probe-disabled solo toggle 18,112→40,312; post-fix matrix 16,080→41,768, the ~10% residue vs 09-14 being the probe's by-design 2×2 MiB head parses); (3) **the LDM alphabet gate dropping the pooled `LdmState`** — on <128-symbol shapes the gate's `self.ldm = None` freed a 2 MiB table every frame and `apply_level` re-allocated it (calloc memset + `restart`'s eager 2 MiB fill + kernel fault churn; trace-counted 3 new+restart in 3 iters at l13); the gate now *parks* the state beside the pool and `restart` defers its table clear to the first `fill`/`generate` (zeros opt/ultra 30,874/29,958→41,208/39,633, best carries it too). Gates: full-ladder dump and 120-cell sweep byte-identical (zeros included — the probe's 0≤0 Shrink edge and every parse decision unchanged), `pooled_state_reuse_is_output_neutral` green. Lesson: the "limited update"/clamping family is not only about *insert volume* — every per-frame O(tables) fixed cost is a hidden RLE-shape tax, and deferring clears to first use keeps their determinism contract byte-intact (clear-before-first-read ≡ clear-at-reset when nothing reads in between).

## Tuning lessons (general across levels)

- **ratio is tightly coupled to table pressure** (Fastest): any change that raises insertion volume (full-density probing, slow ramp) crashed the json ratio (down to the 5.5 tier); only after dual anchors cut long-match insertion volume to 1/8 did table pressure gain headroom.
- **depth has terrible cost-effectiveness**: json d16 vs d64 is only +0.9% ratio for a 2.7× speed difference. The road to high-end ratio is the opt parser, not deeper chains.
- **the hash table must match the data**: on skewed (16-symbol alphabet) H17 has ~256 chains per slot that can never be fully walked, H20/H21 terminate at chain tails (zstd-9 is H21); H18 instead collapses on skewed (finer head buckets → chain-head hits thin out).
- **locking periodic corpora relies on direct rep0 probing, not the hash table**: a fixed grid is mathematically hopeless on clustered 5-grams (twin slots with equal values recursively burying each other / phase-alignment traps) — hence the MT job-start seeding mechanism.
- **gain gate** (store decision): C's +7 is a "replacement gate", not a store gate; our tables are denser than C's (stride-1 prefill + walk takes the longest), so the selection bias toward weak long-distance matches is larger and the gate is deliberately stricter. The flat `ml*4 ≥ ilog2(offset)+7` form false-killed short/medium-distance matches on text (-0.55% at T=7) — resolved by pricing the displaced literals at the previous block's Huffman code lengths instead of the flat 4 (see the Balanced section's store-gate entry).
- **W4M is a double loss** (32MiB-corpus shape; superseded as a blanket rule by the W21/W22-23 landings): a window only finds more distant matches, not longer ones — bare widening wins exactly when the corpus's redundancy sits inside the added reach (dll's 1-8MiB, json's 1-2MiB tiles); for farther distance classes the carrier must be LDM, not the window.
