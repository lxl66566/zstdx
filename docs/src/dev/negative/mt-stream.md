# Falsified directions · MT / Streaming

> Every entry is backed by A/B data. If premises change (table structure, window, corpus, CPU), re-evaluation is allowed, but new decomposition evidence must come first; differential-testing discipline in [benchmarking methodology](../bench/methodology.md). Rules: [index](../negative.md).

| Direction | Result | Conclusion |
|---|---|---|
| LDM strip-fill segmentation at arbitrary call boundaries (shared prefix fill, first form: snapshot/continue at any offset) | byte-divergence: the asm gear-feed's 4th checkpoint (c4) diverges from the portable reference (differential harness: c1-c3 match on 1000/1000 random inputs, c4 differs), so the split set depends on where a call boundary lands inside a batch — segmented and one-shot fills of the same span disagree | only LDM batch-freeze points are exact cut points (batch count zero by construction; the gear group grid restarts there inside a stock fill too — `LdmState::fill_to_freeze`); fixing the asm c4 would change LDM-armed frame bytes and stays a separate, non-byte-neutral follow-up |
| admitting the opt/btlazy rows into the shared prefix fill (release build, unpledged >=16 MiB l17-l19 streams) | segfault (core dump): `chain_grid_fill` ran on an opt driver whose `chain` table is empty — `len - 1` underflows and the unchecked chain store writes wild memory; the strategy `debug_assert` is compiled out in release (found by `emitframe text.l17 --stream`, core dump) | eligibility gate at the entry (`MatchGeneratorDriver::spf_strip_fill`: chain rows only) plus the adopter prefix bound (`start <= overlap` — a larger start's strip is mid-stream and adopting diverges); lesson: release builds have no debug_assert tripwires — caller-side gates carry release safety, callee assertions only debug it |
| stream-MT reach-probe donation variants (2026-09-17, r7): (a) one donation task doing keep+shrink serially on a pool worker, (b) the donation task holding a FrozenSrc into the accumulate buffer and counting in `n_incomplete` like an encode job, (c) pre-reserving the keep-side buffer scale (capped 64 MiB, poolable) at the gate alongside the donation | (a) wash vs the stock probe on text (372-392 vs 366 MiB/s, n=10 matrix rounds): keep 16.3 + shrink 15.3 ms serial lands after the ~8 ms pump either way, and the keep side is real work job zero owed — only the shrink parse is exposed time, so one worker cannot beat the ~29 ms stock dual-parse by enough to matter; (b) json 349→296 MiB/s: the pump fills the ~10 MiB pending-scale buffer, then `make_space` waits for quiescence on the counted donation and stalls behind the verdict a shrink-class stream will not deliver until the whole keep parse finishes — the pump must stay recycling-independent of the donation; (c) text 446→405 ±0.28 (best rounds 800+): a fresh 64 MiB mapping per stream re-introduces the first-touch fault bimodality the pooled-buffer fix removed, worse than the incremental growth ladder it replaced | the landed form: two parallel donation tasks on owned head bytes (uncounted, no views), lazy verdict, kit pool — see [mt-stream](../perf/mt-stream.md); buffer growth stays incremental through `make_space` |
| frame-per-job MT encoding | rejected at design time | multi-frame output burdens the decode side (unfriendly to single-frame consumers); stage-2 single-frame overlap jobs are the right answer |
| enlarging overlap only, without strip prefill | ineffective | insertion paths only insert job-local positions; strips never get indexed; enlarging overlap is mathematically ineffective |
| fixed-grid prefill to find periodic repeats | mathematically unsolvable | clustered 5-grams recursively bury twin slots with equal values / phase-alignment traps; dfast/chain suffer the same burial; **seeding at job start is mandatory** |
| huge pledge without a job_size cap | memory blowup | MAX_JOB_SIZE=1GiB (same as zstdmt) |
| thread_local pool across thread::scope bursts | always misses | scope threads are fresh each time; the pool must be owned by the encoder and carried across bursts |
| reading directly into Read's spare capacity (beyond the read-pump double copy) | dropped | handing uninitialized buffers to arbitrary Read impls violates the trait contract |
| streaming-MT checksum absorb off the calling thread, four placements (2026-09-15; XXH64 has NO chunk-level merge — the spec's `mergeAccumulator` is a finalize fold, the avalanche is non-invertible and the lane rounds chain non-linearly, so per-job digests cannot combine and one serial state must absorb in order; zstdmt's SerialState is an ordered per-job `XXH64_update` under a mutex, not a merge) — (a) pool-job pickup like zstdmt (our bursts freeze the whole range at post, so no ordering is even needed), (b) sidecar thread via the existing `AsyncChecksum`, (c) hash fused into the pump memcpy (`write_appending`), (d) baseline = absorb on the calling thread at fire time | in-process interleaved 3/4-way A/B, 32MiB w8, write AND read (64KiB) paths: caller absorb is **free** (write text.fastest x1.005-1.041, read-path every cell x0.958-1.013 on/off); sidecar x0.96-1.25 (never beats caller, text.fastest x1.09-1.25), pool pickup x1.15-1.23 text.fastest / x1.05-1.11 text.fast, fused pump x1.08-1.21 text cells; the caller's absorb cost shows only in slow machine states (first session measured on/off 1.19-1.20 at text.fastest, `Xxh64::absorb` = 16.65% of whole-process cycles) | the fire-time caller absorb already rides the fire→drain window that overlaps the encode span on a thread that would otherwise wait at the barrier; every offload pays the same 1.6ms of hash PLUS a new concurrent memory stream competing with 8 encoders on the bandwidth-saturated fastest-tier cell (sidecar extends the burst; pool pickup starts after the first job finishes and pokes past the last; fused copy runs slower than memmove and pays on the pump directly). The residual on/off tax at text.fastest is a **resource tax** (serial hash sum ≈ pump memcpy sum ≈ encode span), not a placement tax — no thread placement can remove it; only a cheaper hash or de-saturating the cell (the pump/burst item) can. Nothing landed; the pre-change placement stands |
| ping-pong buffer recycling (regenerate the accumulate buffer into a spare when the dead prefix is not enough, retiring the old allocation behind an in-order assembled watermark) — the "pump never waits on workers" form | interim interleaved A/B (2026-09-16) measured it **equal** to the simpler quiesce-gated wrap+grow on every fastest/fast stream-mt8 cell, while the audit found three lifecycle defects in the WIP form: the spare was reused without clearing the retired buffer's length (stale-prefix corruption plus runaway capacity at the 256 MiB ceiling), and the poison abort completed unclaimed jobs without setting `done` or decrementing the posted count (an infinite spin in the quiesce wait) | the in-buffer wrap is a move of the live tail whose destination **overlaps** the views of still-running jobs — it cannot run ahead of quiescence at all without ring-buffer job views (zstdmt's model; a different job-contract), and the measured overlap value on the epoch-sized buffer is zero: the pump laps the workers about once per epoch and the quiesce wait is bounded by the in-flight encode span. Landed the quiesce-gated form |
| larger read-side pump pulls (64 / 128 KiB staging chunk instead of 16 KiB) | interleaved A/B, mt8 read path, text/json × fastest/fast (2026-09-16): 64 KiB median ratio vs 16 KiB 0.89-1.00 (below 1 on 5 of 6 cells), 128 KiB 0.85-1.00 (below 1 on 5 of 6) | a pull larger than the grid's posting granularity overshoots the epoch boundary inside one append, so the buffer pressure shifts to mid-epoch recycle points (quiesce-gated) instead of boundary-aligned ones; the 16 KiB chunk stays (kept as a persistent per-encoder buffer — the zeroing, not the size, was the cost) |
| 1-bit parity tag replacing the opt tables' epoch (2026-09-16; bit 31 of a u32 entry, flipped at every reset point — the u64 form's 16-bit epoch compressed to O(1) invalidation with no clears) | all corpus gates passed (dump, 120 cells, dll st+mt8) but **dll32 l16 mt4 +332 B** (deterministic on both builds; st and mt8 of the same file identical): the bulk-MT job path resets each pooled state TWICE (`take_slice_state` then `run_job_with` each call `reset_slice_state`), which the monotone epoch shrugs off but a 2-period tag neutralizes — consecutive jobs on one thread_local state share parity, and the earlier job's entries revive wherever it ran within the later job's window band (dll32 l16's 8 MiB jobs / 4 MiB tree domain; the corpus cells' same-state job pairs sit window-far apart and mt8 ran one job per state, so only this geometry exposed it — the revived entries were strip-tail fill-skip interiors whose head slots the tail-half tree fill never rewrites) | a width-2 invalidation domain cannot back a pooled state's unbounded lifetime; the landed form advances a per-state monotone **origin** instead (`opt_origin += last_pos + 1` per reset, `+= block_end + 1` at ultra's pass-1): stale entries rebuild as `cand = q - advance < 0` and die below the window floor, double resets only add extra advance, and output stays independent of the origin value. Lesson: any tag narrower than the state's reset count needs the reset count bounded or the tag monotone |
| discovering the encoder job grid from restart-block positions (2026-09-16; job starts are restart blocks by construction, so their output positions should pin the job size: seed candidates from the first restarts, keep a seed whose every multiple is a restart) | json.lvl3 32 MiB: **92 of 128 blocks are restart points** (this encoder re-sends entropy tables densely), so the restart lattice carries no job-grid signal; the seeds all fail the multiples check except a coincidental 1.75 MiB periodic alignment whose "pieces" carry near-boundary crossing reads (lag 0-8 B), and the true 4 MiB grid never enters the seed set | restart density destroys the signal — grid candidates for the piece executor come from the job-size formula's lattice instead (`ceil(fcs/2w)` over worker counts + overlap-raised sizes), cross-checked by measured per-piece lag; landed with [mt_pieces](../perf/mt-stream.md) |
| MADV_HUGEPAGE over the opt/dubt tables' fresh `alloc_zeroed` allocations (2026-09-16; the accumulate-buffer precedent, aiming the fresh-state Fast→Opt build's first-touch faults at the stream cells' reset spans) | jobdecomp json stream-mt reset spans **rose** ~45→~95 ms summed (ultra 133→294) on this 59-day-uptime fragmented box with the advice in, while the same build without it sits below the old u64 baseline (the u32 halving alone); the walls stayed dominated by the fill and moved −16..−22% regardless | huge-page allocation under fragmentation pays direct-compaction stalls that exceed the 4 KiB faults they replace — the advice helps dense steady-state buffers (the accumulate buffer's win) and hurts once-per-encoder cold mappings; dropped |
| halving the opt rows' MT job strip (tree **and** LDM, strip_for_* → reach/2, C zstdmt's btopt overlapLog-8 parity) | dll100 opt mt8 20,391,886 → 21,234,397 (**+4.13%**); dll32 opt mt8 −0.13% and dll16 −0.27% (denser), corpus json.opt.bulk-mt −0.17% (job re-split interplay) but json.opt.stream-mt +0.13%, text/skewed ≤ +0.021%, st byte-identical (2026-09-16) | the [4,8) MiB band of job-start history is load-bearing exactly where the far class lives (dll100's mass twins; LDM is the server there — shrinking the strip cut LDM's ingestion too). The decomposition's strip-fill cost (40-73% of job time) is attacked through the **tree fill only** (the tail-half bound in `prefill_job_strip`), leaving the LDM strip whole — see [mt-stream](../perf/mt-stream.md) |
| the job-start seed/LDM probes comparing candidates against a stale scan index (2026-09-17 P0: unpledged row-9 stream-mt frames corrupt deterministically past 32 MiB — text.raw x1.5/x2/x3 at w2/w4/w8; clean at w1 and at l3/l13/l17) | the rep probe may advance `pos` one byte past the iteration's `idx`; a seed (or LDM) offset of exactly that advance resolves the candidate onto `idx` itself — read4/extend trivially "match" the position against itself and the store gate prices the zero offset with `ilog2(0)` (release backtrace straight into `pays_for_offset_lit`). The panic then rode two amplifier defects: the stream's `drain_all` returned with the poison unsurfaced (aborted jobs assemble empty and drain like completions), so the frame shipped missing whole jobs — decoded short by exactly the poisoned job's span, first wrong byte at a block start as later far matches read the shifted history — and the bulk pool's workers all exit on poison leaving unclaimed slots forever `None`, deadlocking the ordered-assembly wait (a 14-min futex hang with every worker thread gone) | fixed at both layers. The probes take strictly-older candidates only (`ci < idx`) — byte-neutral by construction, since a self-compare either panicked or never updated best (full-ladder dump and 5 shapes x 6 tiers emitframe stream-mt8 plus mt2/mt4/bulk-mt8 byte-identical vs ext); `drain_all` surfaces the poison once the pending queue drains; the bulk assembly fills unclaimed slots under poison so the panic resumes instead of hanging. The w1-vs-w2 asymmetry: a carried worker state's repcodes are job-tail values that do not trivially match a period-1 run, while a fresh state's format-default rep[0]=1 does — parallel jobs on fresh states are the corrupting geometry. Regression: `strip_tail_period_run_roundtrips` (384 KiB word-run head + zeros + repeat, w2/w8; the generator is fixed so the anchor alignment reproduces) |
| posting the growing grid's last epoch incrementally as its bytes arrive (2026-09-17; the read-pump round exposed json.fast's two-wave floor: epoch N's jobs post only when fully buffered, epoch N+1's tail only at `finish`, so a 32 MiB stream runs ramp-read -> wave -> wave with ~1.5 ms of read unhidden) | rejected at design time | the stream-end tail re-slice is a function of where `job_start` sits at EOF: incremental posts advance it (e.g. 4 MiB grid jobs posted at 20/24/28 MiB vs today's 8x2 MiB re-slice of [16,32)), and the frame bytes change — the byte-identity gate (dump/120-cell) vetoes it outright; a pledge gets the pipelined fixed grid instead |
| zero-copy `&[u8]` source for the read pump (skip the last remaining copy by borrowing the source's bytes straight into jobs; 2026-09-17) | rejected at design time | `read::Encoder<R>`'s pump is generic over `R: Read`; detecting "the source is a slice" inside the generic needs specialization (unstable) or `Any`-downcast — the bench's `&raw[..]` borrows a local, so it is not `'static` and downcast cannot fire either; the single copy is the Read-trait floor for an opaque source |
