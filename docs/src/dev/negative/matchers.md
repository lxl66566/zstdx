# Falsified directions · Encoding · Matchers

> Every entry is backed by A/B data. If premises change (table structure, window, corpus, CPU), re-evaluation is allowed, but new decomposition evidence must come first; differential-testing discipline in [benchmarking methodology](../bench/methodology.md). Rules: [index](../negative.md).

| Direction | Result | Conclusion |
|---|---|---|
| depth-0 price competition as the btlazy2 LDM integration (inject each far candidate into the lazy selection like the chain row, plus a miss-step clamp so the scan lands exactly on splits) | dll32 best st: 4,269,832 (gap-parse: 4,236,108) at 16.0 s/iter vs chain-only 7.6 s (**2.1× slower**) — instructions 245.7G→544.6G while find_best *calls dropped* 4.93M→3.97M (per-call 50K→137K); perf: ~98% of cycles in count_from's 8-byte equal-run scanner, whole-encode scanned bytes 241G→540G | the tree's parse after a far win resumes at the far twin's divergence point, where near twins still agree for megabytes — every post-resume search pays near-block-cap extensions against them (the stock parse only ever searches at its own tree-divergence points, where the first scan is short by construction); the clamp compounds it by capping miss steps to split granularity. C's lazy-family LDM (`ZSTD_ldm_blockCompress`) emits candidates wholesale and parses only the literal gaps precisely so the tree never touches the duplicated mass — price competition stays in the opt parser, which visits every position anyway |
| indexing the LDM match interior into the lazy tables (no `ZSTD_ldm_limitTableUpdate` cap at the gap-parse seam: bulk-fill everything the emission skipped) | dll32 best st whole-encode scanned bytes 6.7G→1,592G at 26.5 s/iter (worse than the falsified depth-0 form) — restored to 6.7G/0.61 s by C's cap (tables catch up at most 512 bytes) | freshly-filled interior entries sit at exactly the near-twin period behind the resume point, so the first bucket candidates scan to the segment cap; the interior is already far-matchable history through the LDM table itself — the lazy ring gains nothing but the worst candidates |
| hash4 revivals (@2^13 short-table side table / 16KB distance cap / @2^14 single table / rep0 fallback; 4 attempts) | all failed (json 4.18-4.93, all worse) | table pollution from high-frequency 4-byte patterns is irreversible; **hash5 is settled** |
| hash4 near-distance side table (ml≥6 gate; cures pollution) | speed -7~-14% across the board, ratio flat | ml≥6 does cure the pollution, but the gains cannot cover the dual-table cost |
| 3-rep probing (probe rep1/rep2 as well) | hit rate 7.6%→26% but json/text/skewed ratios all worsen | weak rep matches steal positions from longer hash matches; libzstd-fast probing only rep0 is right; a trailing rep2 probe also degrades slightly |
| rep0 threshold 4→5 | neutral | ml=4 rep matches are too few |
| HASH_LOG 17 | json +0.6% but speed -8%, skewed ratio worsens | |
| tiered MIN_MATCH (large offsets require ml≥5) | zero effect | far ml=4 matches are rare anyway |
| Fastest window <448K or 2MB | 256K/384K catastrophic (text ratio 301→7.88); 2MB worse | cutting the tile period is catastrophic; 512K workable but 0x70000 is more cache-friendly (768K later unlocked cross-tile) |
| dfast window 2MB | **re-evaluated + landed 2026-09-12** (json −1.1%/−4% speed did not reproduce under the u32-table matcher: json now parity ratio at +4% speed, text +11%) | the original falsification was corpus-only (32MiB, no large-binary shape) and pre-`327bc99`; the 100MB ELF corpus showed the 1MiB window itself cost 26-28% ratio there (libzstd-3 at wlog=20 reproduces our old bytes within 0.03%) — see matchers.md before re-testing windows on new shapes |
| chain window 4MB (W4M) | double loss on every shape, skewed 14 MiB/s | the conclusion holds for the 32MiB corpus shape; **W21 (2MiB) landed 2026-09-12** as a different point on the trade (dll −30% size, json.balanced −14.6% for −14% solo speed) once the corpus set included large binaries — window A/Bs are corpus-set-sensitive: re-run both extremes before citing either direction |
| chain window 4MB (W4M) | double loss on every shape, skewed 14 MiB/s | finds farther but not longer; offset code-position loss > length gain; window expansion must come with LDM |
| porting C's anchor-distance miss formula to fast/chain | regression across the board (skewed scan description ~80× slower) | we use stride-1 dense insertion + newest-wins single-candidate tables; per-byte probing overwrites good far entries with garbage; the C formula's premises (sparse table + large hLog) do not hold. Holds for dfast only (`0b46f21`) |
| full lazySkipping port (chain) | flat over 11 interleaved rounds (skewed +3.1%, text -0.7%) | the C mechanism's two levers (dense→sparse insertion, slow→fast stepping) are already spent in our design; **miss-segment strategy directions are closed** |
| pure dual-anchor table insertion (libzstd fast default) | json 5.71-5.90, text 275-290 | severe ratio loss; ≤16 dense insertion must be kept |
| in-match insertion step 1→2 | json ratio 6.02→5.97 | not worth it for json +2% speed |
| zero-slope full-density probing / slope relaxed to mc>>3 | json/skewed/random all collapse | insertion volume↑ → 2^15 table aging↑ → far matches die; the dynamic miss slope is essential |
| hLog16 (512KB table, 2^15 era) | speed -8-14%, ratio no gain | beyond-L2 locality |
| full u64 hashing (no 40-bit truncation) | json wins both ways but text ratio -7% | the 40-bit mask keeps mathematical equivalence; global ratio takes priority |
| table entry u64→u32 @Fastest 2^15 (2-bit epoch scheme) | output identical but instructions +0.09% | entry width is not a lever while the table lives in L2; contrast: at Balanced's 16MiB working set, u32 is a big win (`327bc99`) |
| lazy-walk cheapening variants (depth-1-only margins; cap 4 steps; skip the second-chance search after an empty probe; rep probe when the incumbent is already a rep) | depth-1 collapses the ratio (json 6.55→6.18, text back to 361.5); cap and empty-skip change nothing / trade 0.05 ratio for 2.5% speed; rep-vs-rep probe never alters selection (output byte-identical without it) | the alternating depth-1/depth-2 margin structure IS the gain; the walk is not deep, it is the 2-searches-per-failure baseline that costs the ~30% json instructions |
| emit skipping positions already inserted by scan pairs (idempotent insertion) | output DIFFERS / instructions +2.6% after the fix | not unconditionally idempotent (hash-collision overwrites during backward extension; redundant reinsertion serves to restore slot values); the cost also exceeds the benefit |
| steady/tail scan-loop split (fold `pair_len` to constant 2 via a full-pairs steady loop + single-position tail epilogue) | fastest json/text Ir −3.3%/−2.7% but text.fastest wall −7% (12201→11347 MiB/s, reproducible in both prof and interleaved matrix) | the third macro instantiation displaces the hot blocks — at 12 GiB/s the loop pays pure layout noise; instruction-count wins that don't survive contact with the layout lottery are not wins |
| dfast position-u64 carry (ride v0 = read8(ip) across the inner-loop back edge; feeds the next short hash, the long/short probes' current-side compares and the rep pre-probe — 4 loads per position saved) | fast json/text/skewed/random Ir +3.6..+6% (json 31.80→33.12M), wall flat | the removed reloads are L1 hits; the pinned register displaces hotter pipeline state into spills — same lesson as SeqWord 24→16B: **operand-supply savings must not be bought with a live value inside a register-saturated loop** (contrast: deriving `cur` from the hash load *within* one iteration, where no new live value crosses the back edge, is a clean win) |
| naive Predefined/Repeat thresholds in choose_table | negligible | later handled by the cost-comparison approach (selectEncodingType port) + repeat mode; do not use naive thresholds |
| store gate priced by average residual literal entropy (per-block feedback of bits/byte, gate `ml*cost ≥ ilog2+7`) | text.balanced 346 (−6.5% vs zstd-6), json +16% | the average anti-predicts: text's residual stream is hyper-skewed (~0.2 bits/B — the matcher hoovers up everything matchable) yet wants the LOOSE gate; the displaced literals' marginal cost (their code length under the block's Huffman table) is the right price and IS the implemented form |
| fast-loop rep-probe halving (drop the first-position probe, importing libzstd's ip2 one-compare-per-pair policy) | json.fastest 5459976→5719320 bytes (+4.75%, r 6.146→5.867, below zstd-1's 6.11) for +3.6% wall (530→549 MiB/s); text/skewed/random ratio unchanged | libzstd's fast covers the ml∈[4,6) band with its hash4 probe, so halving its rep compares is free; our fast is hash5 + a ml≥6 bar (hash4 falsified above), making the every-position rep probe the only detector of that band — json's short structural-token repeats live in it, and the first-position probe's of_value-1 emit also keeps rep history for the chains. The rep probe is load-bearing: cut its *branch cost* (the OR-fold), never its count |
| emit outputs as raw seqStore cursors (block-start reserve in `start_matching_codes`; lit/seq cursor fields replace the `literals`/`seqs` Vec fields in TableEmit/DfastEmit; `push_seq_packed` writes through cursors, lengths commit via `set_len` at the tail) | byte-identical output (full-ladder dump gate); gungraun fastest Ir +0.2..+0.6% (random +1.5% — the extra work is in the miss path), fast Ir **exactly unchanged**; wall json.fastest 540→532-538, json.fast 391→374-375 (−4%, reproduced twice) | the "Vec fields pin 6 registers" premise is false: LLVM already rematerializes the len/cap at the cold emit sites, so only the two pointers were live — the cursors codegen to the same two pointers (fast's identical Ir is the proof), and there was no spill traffic to reclaim. The block reserve plus the reshuffled allocation added miss-path Ir and rolled the layout die (fast −4% at unchanged Ir). libzstd's seqStore discipline is enforced by an **outlined** `ZSTD_storeSeq` behind one pointer; with `inline(always)` emit LLVM promotes any cursor back into registers — memory residency is unreachable on this route, and the outlined-emit variant would pay per-match call overhead on json's ~1-emit-per-16-30 B density before any speculative win |
| sampled entropy decision for the table-strategy gate (reuse the probe pass's ~2016 sampled bytes as a count histogram + Miller-Madow estimate, drop the exact pass; rationale: a wrong keep costs one ordinary scan, not an opt parse) | falsified on paper before building | the MM correction fixes the estimator's **mean** (≈8.000 for uniform) but not its spread: std of a 2016-sample plug-in entropy over 256 bins ≈ 0.11 bits, so the 7.97 bar sits at ~0.3σ — a per-block coin flip, the exact failure mode the opt gate's exact histogram was built to kill (their measured 3×). σ scales 1/√N, so a clean 6σ margin needs ~1M samples = the whole block. The exact pass is mandatory at every level; cut its cost only via better binning, never via sampling |
| gate-extension placement hunt (4 builds: watermark store in the 3 scan-loop epilogues; gap-side-only bookkeeping with catch-up before/after the scan loops; struct-tail field) | every build's instructions +0.1..+0.5% on text cells, yet cycles +3..+8% on exactly one fast-tier cell — which cell moves with the build (fastest with the gap variants, dfast+chain with the epilogue-store variant); branch-misses and icache-misses measured EQUAL, the loss is pure scheduling/decode placement | confirming the layout-lottery lesson at instruction parity: even zero-touch scan bodies (the gap variant changes nothing inside the loops) re-roll the dice — the perturbation enters via the per-block dispatch path and outlined-fn placement. No placement dominated across 4 tries; shipped the variant with one bad cell (text.fastest −7% wall, still 12% ahead of libzstd-1). Do not chase per-cell wall with source-order or field-order tweaks: each is a fresh roll, not a fix |
| btlazy2 via a direct DUBT port, first attempt (2026-09-13: unsorted-mark unwind grafted onto the opt tree's threading) | json roundtrip corrupts at ~4.2 MiB (first ring wrap): tree-threading writes passed through still-marked chain nodes and a later sort's inherited common-prefix claims went false (a match trusted a 15-byte prefix that was never compared); the hole was never pinned after ~6 instrumentation rounds | **resolved 2026-09-14 by a fresh port** (`encoding/dubt.rs`): the failure was the grafting, not the scheme — a standalone finder keeping C's exact slot discipline (chain in `[0]`, mark/larger-child in `[1]`, batch sort oldest-first, nullify the boundary) makes "a descent never crosses an unsorted node" hold by construction and landed clean (see matchers.md). Lesson stands: port the architecture whole, never patch one tree's threading toward another's invariants |
| Ultra tree ring C24 (libzstd L19's cLog-beyond-wLog slack) | dll100 output **byte-identical** to C23 | ring slots beyond the window floor can never resolve as candidates (the walk's `resolve`/floor checks reject them before any ring read matters); libzstd's C24 is dead weight, C23 carries the full-linking invariant at half the footprint — ring > window is pointless without a window-floor change |
| Ultra job-boundary statistics seeding with the strip reachable (throwaway parse of [job.start−K, job.start) against the prefilled strip to warm the job's `opt_state`; K = 128KiB and 1MiB) | json.ultra.bulk-mt −1.85%→−1.92% at K=128KiB, worse at 1MiB — recovers none of the boundary loss | the boundary cost is a price-feedback **basin**, not a calibration gap: a BASE-priced parse with the 8MiB strip reachable picks near-offset/ll0 chain shapes and `updateStats` reinforces them, so stats sampled from more reachable-strip parsing deepen the wrong basin; the seeding that works re-bases the window to the span itself (empty-window frame start, landed 2026-09-12) — sampling the right distance classes is worthless if the seed parse itself runs in the wrong shape |
| Opt tier at W22/C22 (libzstd L16-exact, ladder differentiation argument) | dll Opt 21.43MB ≈ Best's 21.57MB (tier gap collapses to 0.7%) | the Opt tier's dll value *is* the W23 reach (20.91MB, 3.0% under Best); a cheaper json speed (−22%) is not worth deleting the tier's reason to exist |
| Ultra ring C22 (the Opt-side C22 relief applied to rows 18-19) | measured 2026-09-12: json −18.6% (13.35→10.88s) for +0.22% dll (20.03→20.07MB) — not taken | a deliberate tier-policy rejection, not a falsification: Opt pays the trade because its product is the *reach at speed* (and json.opt came out denser anyway), while Ultra is the density flagship — its 7.7% zstd-19 margin is the tier's entire identity; revisit only if Ultra's speed deficit becomes the product question |
| post-walk issue of the lazy walk's pre-reads (gated on the walk actually running — zero waste on long matches) | dll +4.5%→+0.5%, json +2%→−2%, text unmoved | the head loads must issue *before* the incumbent walk: placed after its loop they decode behind the walk's poorly-predicted exit branch (mispredict recovery re-issues them) and the shadow evaporates — OOO overlap does not rescue bad issue placement across a data-dependent loop exit |
| dual pre-read for the lazy walk (pos+1 and pos+2, covering the depth-0 rep probe's advance) | dll +4.5%/json +2% but text −2.5% wall, reproducible | the scan body's added straight-line code alone re-rolls text's layout badly — gating the pre-reads off on long matches (the waste theory) did NOT recover text; only removing the code did. Halved to a single pre-read, text returns to +1.4% (adding code to a hot loop is never free, executed or not) |
| chain-scan spill relief via the seeded/steady split (remove seed_offset/seed_hits/seed_budget/rep_pending live values so the walk's stack-operand params win registers) | split landed 2026-09-13: balanced Ir json/text/skewed −0.38/−0.78/−0.44%, wall flat on every shape (json 86-88, dll100 106-107) | the four live values were not the spill driver: cycles and L1-miss samples concentrate in the same two steady walk copies (82%/93% on json 32MiB) at ~66 cyc/step ≈ one serialized L3-class load — the walk is a latency-bound chain-link chase (4MiB chain table, 1-2MiB candidate distances) and the per-step stack operands hide under the load latency. Post-inline the instruction profile is flat (max 2.5%); **Ir-side edits to this loop no longer convert to wall** — the split is kept (structure + deterministic Ir), but do not expect wall from further register-budget work on the chain loop |
| chain miss-step → libzstd's slow grid (`1 + miss>>8`, kSearchStrength) | text.balanced −2.78→−2.12% (+0.66pt, all in the cold-start tile) but json 90→76 MiB/s and skewed 2094→19 MiB/s | dense probing implies dense insertion (every probed position inserts), and dense insertion over skewed's literal half dies on the 12MiB head+chain random-store floor (~2 stores/B) — the same floor as the fast-tier insertion falsification. The chain tier's sparse speed *is* the skip; the fast tier's >>2 ramp is load-bearing here too |
| dense back-fill insertion of chain miss-skip gaps (libzstd's insertAndFindFirstIndex model: sparse probe, dense insert, >>2 ramp kept) | text −2.78→−2.42% (+0.36pt), skewed 2094→80 MiB/s | the back-fill itself pays the store floor per skipped byte; the ratio gain is also smaller than the probing-side variant — insertion without probing only lets *later* probes find the skipped content, and the reference matches start *at* the skipped positions |
| cold-gated variants of the above (freeze miss-step growth when the head slot is empty — cold tables probe at one-load price; branchy and branchless) | text −2.78→−2.12%, json 89 (flat) but skewed 2094→1309 MiB/s | the empty-slot signal does not separate "cold table" from "sparse table": under sparse probing skewed's literal-run 5-grams are mostly novel-in-table, so the freeze keeps the step at 1 there too and every literal byte again gets probed+inserted — store floor. No cheap discriminator between cold-start misses and garbage misses was found inside the scan loop |
| insert_covered parity-guard removal, counted forms (plain `for k in 0..count`; hand-unrolled `while k + 2 <= count` with `start + k` addressing) | plain counted +3.3% Ir on json/text (LLVM keeps it rolled — full loop overhead per insert); hand-unrolled +4.3% (per-iteration u64 re-derivation of the absolute position) | the mod-2 unroll with its mispredicting parity guard was *already* the cheap form; the landed peel-then-pairs variant is the only shape that keeps the 2x unroll without the guard at ≈flat Ir |
| fast-loop rep pair prefilter as `(a \| b) == 0` | output DIFFERS (+1238 B on json): `(a \| b) == 0` tests *both* xor-flags zero, not the OR of the two zero-tests — the probe fires less often and the parse changes | when folding two zero-tests into one branch, the boolean fold is `(a == 0) \| (b == 0)` on the *comparison results*; folding the *operands* silently changes the predicate's meaning |
| Fast strip-prefill retention cap without seed confirmation | text.fastest bulk-mt8 108607→163586 B (ratio 309→205): one job's strip tail carried a 194-byte duplicated statement at offset 2244, the nearest-first seed picked it over the 441226 tile period, burned its budget, and with the head entries capped away the job had no far candidates at all (other jobs at parity — the seed carried them) | the retention/churn model (~8 head inserts buried per slot) only holds for aperiodic content: a period≈window corpus's period-old twins hash their slot once per period and *survive* newest-wins, so the strip's far-match payload lives in both the table's lucky head entries and the seed — cap one only when the other is confirmed to carry the class (landed together in `3ba7335`+`27e8d52`) |
| DUBT search-descent early-out at the row's targetLength (`sufficient_len` 32, transplanted from the opt walk) | tiny-vocab ladder synthetic 28110 B (worse than L9's 24142; C's own btlazy2 does 19336); json 5472046 vs 5364906 after the fix | the collection walk's break stops the *tree threading* with it — on match-rich data the tree starves within a few KiB; the walk cap must be block-scale (`OPT_NUM`), and the 25→21 MiB/s the early-out bought was the starvation itself |
| DUBT walk/batch cap ×4 (sort deeper backlogs per search) | json 5473898 (slightly *worse*), speed 25→20 MiB/s | the far-candidate loss vs the old Keep tree is not chain-cap starvation — more sorting just re-threads/prunes more; the old tree's json edge was link *retention*, a different mechanism |
| DUBT row13 at S5 (search descent 32, C's L14 knob) | json 5472469 (flat vs S4), speed −24% | on this corpus the json candidates sit within the first ~16 tree steps; deepening buys text +0.3% only — keep C's S4/S5/S6 ladder split across rows instead of paying S5 on row 13 |
| DUBT as the balanced tier's whole-row matcher (row 9 → `btlazy(...)`; swept S1-S5, ring C20-C22, heads H20-H21) | text −2.49→+0.45% (S3/C22/H21) but json +19.46→+5.23% at 29 MiB/s (chain: 82); S1 floor 54 MiB/s with json ratio worse than S3's; any row placed at 10-12 would invert against its own chain rows (their json ≈ 7.1 vs the tree's ≈ 6.3) | the json balanced density is the chain's literal-aware lazy selection (+19.5% over zstd-9), not the strategy class — the best tier's own S4 tops out at 6.25 on json vs the chain row's 7.11, so no tree knob buys it back: a DUBT-balanced row is a best-tier replica on speed or a json regression on ratio. What survives is the cold-start slice — the landed head takes the tree only for the span where it measurably wins (see matchers.md); the S1 datapoint adds that a too-shallow head is actively harmful (−4.36%, worse than no head: it parses worse AND hands the chain a degraded rep history) |
| DUBT frontier retention on budget-exhaustion breaks (null the trailing links only when the ordering claim broke — equal tail — and keep them on a pure budget stop) | json 5469237 (−0.05%), speed 22 vs 25 MiB/s | keeping unvisited continuation links preserves little reach at this shape — the pruned candidates are the worst-ordered ones; not worth −12% speed (the full Keep/STALE class stays retired) |
| LDM as an always-on balanced candidate source (probe every generated split, minMatchLength 64, window W22) | corpus speed json −15%, text −31%, skewed −43% (interleaved manual A/B, 3 rounds) for **zero** ratio change: json has no 64 B repeats (its far class is 32-63 B fragments), text's far tiles ride rep0 chains within reach, skewed has none | the split pass costs ~9 cyc/B measured (gear chain + 64 B xxh64 per 64 B + two random-write arrays); a candidate source may not tax shapes that never win — LDM ships size-gated (≥64 MiB inputs) with a quiet latch and canary revival (see matchers.md) |
| probing LDM candidates inside the chain's reach (no offset filter) | text −30% at parity ratio: every split's far read plus the miss-step clamp stopping the scan at each candidate, all losing to chain/rep incumbents | LDM's exclusive class is offsets *beyond* the chain reach; in-window candidates are duplication, and the modal repeat distance (dll's 1-2 MiB) is already served by the newest-wins heads |
| LDM minMatchLength 64→32 (to carry json's 32-63 B far class) | json +13 KB at equal reach (the far fragments are net-negative — supplying more of them loses size); dll100 at small reach also worse | json's far class fails its own offset price; the 64 B window is the right bar for *this* tier's far candidates |
| content-latch variants for the split pass: K=16 no-horizon, canary every 8 MiB (sparse) | dll100 −11.4→−3.5% (first far class only reachable ≥4 MiB in; sparse canaries miss the 4-8 MiB class entirely on dll32: ratio back to baseline) | a latch that fires before the class can exist, or a canary too sparse to find it, deletes the feature; the horizon (count quiet only past the reach) + 1 MiB canaries keep dll100 at −9.75% |
| pre-horizon fill-only generation for the mid-size gate extension (blocks below the reach horizon index splits without matching: no candidate can survive the filter there, "so fill must be byte-exact") | dll100-st moved −41 B: **generate is not fill plus emission** — a raw in-span candidate advances the anchor and triggers the overlapping-pattern re-arm, so the gear hash samples *different positions* downstream; table content diverges even when every sequence dies in the filter | the anchor/re-arm path is part of the split-sampling semantics, not the emission side; any change to it touches the W26 population's bytes (frozen). The pre-horizon tax stays and is bounded by the alphabet gate instead |
| win-keyed canary revival for the mid-size population (revive only when a canary candidate actually out-lengths the incumbent, instead of on candidate existence) | dll32 −19.7→−13.5% (ramping far regions revive late) while skewed stayed ≈ −35%: low-alphabet candidates *transiently* win the injection (`ml > best_len`) and get displaced by the lazy walk without ever emitting, so quiet resets either way | ldm_won is an injection-time flag, not an emission flag — revival keys cannot separate "candidate exists" from "candidate pays" on low alphabets; the alphabet gate is the discriminator that works (both directions measured the same day) |
| LDM mid-size arming at W24 (16 MiB sources, dll16 = first 16 MiB of dll32) | dll16 2,930,674 → 2,914,632 (−0.55%) at −6% speed | the win concentrates where the source actually spans its far class: dll16's 4-16 MiB twins are too sparse inside a 16 MiB window (the pairs that pay sit at 16-32 MiB distances, present only in dll32); the bar stays at 32 MiB |
| chain reach shrink with LDM as the json premise (todo 5's LDM-carried-tiles theory) | W22→W17 json sizes **byte-identical with LDM off** at every reach (4720677→4661039); text identical at all reaches (tiles ride rep0) | the json shrink curve is a pure chain-reach effect (the far chain candidates were net-negative), NOT LDM coverage — item 5's premise is falsified; the shrink itself (json −1.07% size, +60% solo speed at W18) is a real chain-only lever, blocked only by dll needing reach W22 (+4% at W20) |
| fast miss-run stepping (double the pair advance after N consecutive misses, miss tail only, insert_covered untouched) with three rescue/gate variants: dense-insert of the skipped positions; span-EMA gate (arming below 96 B/seq); match-length-EMA gate (arming above ml 11) | no-insert N=4: json 5,466,381→5,292,466 (−3.2%) and wall 567→617 MiB/s (x1.510→x1.389) but text 108,531→116,519 (+7.4%) at −9% speed; N=8: json −1.25%, text +2.9%; dense-insert: text still +2.3% and skewed.fastest wall 2786→1270 (−54%, the 2^15 random-store floor); span-EMA: text +7.2%; ml-EMA: text +3.6% | json's weak mid-run matches are net-negative (skipping them wins size AND speed) but text's short matches are its real payload, and text's damaged regions are locally json-shaped (dense short-match stretches between giant matches) — no parse statistic separates the classes, generalizing the chain-tier cold-gate lesson. Residue worth keeping: json.fastest's parse IS weak-match-polluted — a shape-safe acceptance-bar/fragmentation lever on match-dense data is the open follow-up |
| search-side batched probing of the fast miss tail (pair-of-pairs lanes sitting exactly at the scalar pairs' positions and ramp trajectory, pre-store gathers, one exit branch per four positions, first-hit lane handing back to the pair loop) | output ≈ baseline once the lane layout reproduces the scalar pairs (json +0.05%, text −0.3%, skewed identical; a strided-spread lane layout instead lost json +1.7% (r 6.04, under the zstd-1 line) and text +10.6% — lane placement, not probe density, is load-bearing); gungraun zx_fastest json Ir 29,369,831→30,484,521 (+3.8%); interleaved wall json.fastest 567→500-511 (−10..12%), text.fastest 13,368→11,522 (−14%), skewed 2786→2875 (+3%) | the mispredict budget is not concentrated where batching amortizes: json's miss runs are 1-2 pairs (avg_ll 2.67), so each run pays batch entry, one iteration, and the dropout re-probe instead of two cheap pair iterations — instructions up AND wall down; only long-run shapes gain. Batched/AVX-512 probing of the search side is closed for the fast tier's short-run regime (the vector flavor adds gather/pack cost on the same losing structure) |
| rewriting the DUBT descent/sort loops into C's data shapes to cut per-node instructions (attempt 1: full pointer form — a `count_ptr(a,b,len,end)` pointer-walking count, u32 node values with wrapping `delta = curr32 - v` bounds replacing the 64-bit unpack, candidate/end pointers hoisted into the loops; attempt 2: minimal delta form — the same u32-delta bounds but keeping the index-based count and window reads, no new pointers) | both byte-identical (full-ladder dump) and both regress: attempt 1 json whole-encode Ir 8.85G→9.55G (+8%, finder symbol 7.22G→7.92G), attempt 2 8.85G→9.04G (+2.2%) | the index form LLVM lowers today is already near a local optimum: the 64-bit unpack chain and stack operands are not free-floating fat but the register-allocation slack of loops whose live set (fused selection state + finder state) exceeds the GPR budget — every arithmetic reshuffle re-rolls regalloc and both rolls lost. Per-node instruction reduction needs fewer live values or fewer events, not different shapes |
| near-twin displacement latch for the chain's far candidates (instrument `chain_search`: when the best is far (>4 KiB), check whether a near (<4 KiB) best sits within +2 B; latch a shrunk reach per block when far-best searches are "near-twin covered" — the cheap classifier the reach probe needed before a dual parse was considered) | after fixing the instrumentation (the near best must be captured *before* the first far candidate overwrites it) json reports 99.4% of far-best searches near-twin covered, yet the shrunk parse wins net on both axes | the classifier is decoupled from the benefit direction: json's far-candidate harm is parse-level (displaced repcode history, literal-flow differences over the whole block), not per-search length myopia, so per-search accounting cannot price it — only a real second parse measures the trade (landed as the prefix dual-parse probe, matchers.md) |
| far-search frequency latch for the reach decision (the LDM quiet-latch pattern: count far-best searches per block, shrink when the count says the far class is absent) | json ~119K far-best searches per block vs dll 50-150K — the distributions overlap end to end | both classes search far densely (the chain probes whatever its reach admits); "far class present" is about *what the far candidates cost the parse*, not how often they win — frequency carries no signal, same conclusion as the near-twin latch |
| folding the DUBT walk/descent range pairs (`floor < cand < pos`) into one unsigned compare via precomputed `cand−lo < span` (plus the redundant null test dropped from the loop heads) | find_all_matches Ir +2.5% (11.06G→11.34G), wall flat | the two plain compares codegen cheaper than the wrapping-sub chain; loop-head branch *count* is not the cost on this walk (the null test itself was free to drop — the u32 rebuild of the null link always fails the range checks, kept in the landed port) |
| LDM split fingerprint derived from the gear fold at the trigger (`mix64(fold)`, or patched `mix64(fold ^ oldest_lane*K)`) | dll100 no-latch size 21,126,156 vs xxh64's 20,806,075 (+1.54%) while the latched number stayed −0.013% and the table simulation showed parity (false checksums 12 vs 25 per 1.6M splits, twin misses 1017 vs 1037 of 554K); the unpatched fold also collides outright on oldest-byte differences whose table entries share parity (the difference survives only in bit 63, which the trigger mask forces to zero — ~50% of byte pairs) | a fingerprint correlated with the trigger condition shifts with the split placement: the candidate stream differs systematically in exactly the low-yield regions the latch would kill, and the simulation (which models the table, not the anchor/skip cascade through the parse) cannot see it; the landed 8-lane window fold is trigger-independent and tracks xxh64's unlatched size to −0.04% |
| cheapening the split pass by compute removal alone (swap xxh64 for any cheaper fingerprint, keep everything else) | dll100 instructions −4% but wall flat across interleaved rounds; the pass is bound by the random 8 MiB-table traffic (bucket scan + insert), not the fingerprint arithmetic — the OOO engine hides the serial-multiply chain under those misses | instruction deltas are necessary but not sufficient for armed-LDM wall gains; the wall came only after the gear chain's per-byte serial add was broken (4-byte re-association) and the scan's per-entry op count was cut (AVX-512 checksum mask) |
| static-price acceptance bar on the fast tier's hash path (decline a hash match when its displaced literals at a flat price cannot cover the offset bits: `ml*3 >= ilog2(dist)+7`; declines fall through to the second-position probe) | json.fastest 5,466,385→5,367,850 (−1.80%) but text.fastest 108,535→110,158 (+1.50%); the 4-bit static form declines nothing new (ml=6 only past 256 KiB) | the shapes' displaced-literal prices straddle every static constant: json's run ~3.3-3.6 bits/B, text's ~4.3+ (its short far matches ARE payload) — the chain store gate's flat-margin lesson again. Only per-shape pricing (fed-back Huffman lengths) separates the classes |
| fed-back-literal-price acceptance bar on the fast tier's hash path (pays_for_offset_lit after backward extension, a `dist >= 1024` compare gating the gather, declines feeding the miss ramp; two hostings: gather inlined, and gather outlined `#[cold]` with the bar state carried as a raw pointer) | mechanism validated shape-safe: json.fastest −1.24..−1.27% in all four modes (bulk-st 5,466,381→5,392,573), text −14 B, skewed −55..−384 B, the other 108 cells byte-identical; but wall: json.fastest −4.5%, text.fastest −12..−13% (interleaved, 3-4 rounds, both hostings) — and a DORMANT build (gate `dist < 1<<48`: bar code present, never fires, output byte-identical to base) loses the same text −12%/json −4% | the fast scan loop cannot host even a never-taken bar: the compare/branch, the bar state crossing the back edge and the decline paths re-roll the register/layout dice of a saturated loop — text.fastest at 12-13 GiB/s pays pure code presence, the placement-hunt lesson at full strength. The pollution itself is recoverable (the size win is real and shape-safe); reopening needs a hosting that leaves the current loop body byte-for-byte, e.g. a separate dense-mode scan instantiation selected per block |
| the opt fill-lag clamp in C's unconditional form (`ZSTD_buildSeqStore`'s limited-update-after-very-long-match at every block, rows 16-19) | dll100 opt st 18,780,148→18,901,786 (**−0.65%**), ultra 17,932,355→18,045,646 (**−0.63%**), dll32 −0.17/−0.14%; text/json corpus byte-identical | where a far class is in play (LDM-armed dll, and the sub-32 MiB dll16 at −0.28% where LDM never arms) the lagged region's in-domain candidates measurably pay — the landed clamp runs only on the alphabet-gated population (see matchers.md) |
| the clamp extended to job/dictionary strip parses (streaming-mt text jobs) | text.opt stream-mt 82,124→82,140 (+16 B), text.ultra stream-mt 81,314→81,329 (+15 B) | a job-restart parse (tables, stats and price state reset per job) measurably selects differently with the lagged candidates present — unlike the frame-continuous parse where the clamp was output-neutral on every shape; jobs stay unclamped (`strip_parse`) |
