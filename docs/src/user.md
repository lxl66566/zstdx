# User Documentation (Reserved)

This section is reserved for content aimed at library users (API guides, feature descriptions, examples). This reorganization covers development records only; user documentation will be written once the API surface stabilizes.

Planned pages:

- Quick start: the three API forms bulk / streaming / compat
- Compression level selection guide (the `Level` ladder and its mapping to numeric zstd levels)
- Correct use of multithreading and `pledged_size`
- Feature support matrix and limitations (dictionaries, window size, checksums, no_std boundary)

Source material can be drawn from the corresponding entries in the root `Readme.md` and `Changelog.md`.
