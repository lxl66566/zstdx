# 用户文档（预留）

此分区预留给面向库使用者的内容（API 指南、功能说明、示例）。本次整理只做开发记录，
用户文档待 API 面稳定后编写。

计划中的页面：

- 快速上手：bulk / streaming / compat 三种 API 形态
- 压缩级别选择指南（`Level` 阶梯与 zstd 数字级的对应关系）
- 多线程与 `pledged_size` 的正确用法
- 功能支持矩阵与限制（词典、窗口大小、校验和、no_std 边界）

素材可取自根目录 `Readme.md` 与 `Changelog.md` 的对应条目。
